# Superseded Session Handoff

This underscore-named file is retained only so old links do not break. Its
former contents described the removed `textstep-worklet.js` prototype and are
not the current product state.

Use the canonical [`SESSION-HANDOFF.md`](SESSION-HANDOFF.md) and the concise
[`CURRENT-STATE.md`](CURRENT-STATE.md).
