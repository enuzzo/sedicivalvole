# Migration validation — September 11, 2026

Completed local documentation migration. No application execution or publication is claimed.

## Checks executed

| Check | Result |
| --- | --- |
| Restored instruction baseline | All three original working files matched commit 2c73cc5 byte for byte before editing; its parent relationship and timestamp were checked. |
| Original snapshots | All three SHA-256 values match the archived bytes; original paths and modes are recorded. |
| Full original coverage | 223 source blocks: 74 root, 107 Drive Lab, 42 recovery. All 675 substantive non-heading lines are covered exactly once; exact source spans/text compared to snapshots. No block lacks a disposition. |
| Semantic review | Compared original meaning, conditions, numeric units, exceptions and scope to each active destination. Complex blocks map multiple named responsibilities; explicit supersessions are recorded rather than inferred from dates/code. |
| Local references | 747 local file/section references across entrypoints, guides and audit artifacts resolved, including migration destinations. Zero missing files or headings; this final validation artifact was created after its checks passed. |
| Active routing | Root routes all ten guides and explicitly loads local AGENTS for subtree tasks; section-specific links connect audio/GPS/runtime/maps/diagnostics. No historical prompt is required for ordinary work. |
| Whitespace and diffs | Active-file trailing whitespace checked; git diff --check passed for modified tracked entry files. Session diff generated against captured working bytes, separately from initial differences against HEAD. |
| Outside scope | 1801 initial non-sensitive/non-ignored path states remain unchanged, including all pre-existing archives and all already-deleted experimental cards. No changed presence/hash/mode outside the three intended entry files. |
| Git | HEAD `9e3abe06687482e4163a8552f57973c7414a8be4` and branch main unchanged. Raw index SHA-256 remains `c919d63591802001ea32ca6d8f5b0e9d4119d3eab1ca12ad4801b38a5375077a`. No staging/commit/push/checkout/reset/clean/stash/worktree operation. |
| Product and configuration | Application source/tests/assets/dependencies/runtime/hosting, VERSION, existing CHANGELOG edits, ordinary product docs and specialist Engine prompt retained. No global skills/plugins/config changes. |
| Scope of newly created files | Only docs/agent-guide and this uniquely named archive. Disposable generation/verification helpers ran under /tmp; no audit or CI infrastructure added to the repository. |

Disposition counts: 5 retained, 62 moved with routing, 63 merged, 70 updated by explicit supersession, 2 unresolved source blocks describing **one** location-sidebar question, 21 historical. Mixed blocks retain their live clauses and explicitly identify their retired/scoped parts. These are source-block counts, not a count of individual logical predicates or product features.

## Review findings addressed

The first semantic pass preserved all source blocks but initially carried ambiguous stale-manual TAMARRO language. Comparing the previous corrected archive with ROAD-FEEDBACK / Implemented corrections established the no-GPS manual exception; the final Engine contract now distinguishes manual ready/unmuted/foreground from automatic exact-zero evidence. The corresponding mapping was corrected before final verification.

The broader review also removed stale OPEN/BLOOM requirements using explicit owner listening/removal evidence, retained surface-specific geometry, kept Atlas location/chart distinctions unresolved where necessary, restored small Music/Engine icon details, and routed JUNCTION's defect-specific eight-millisecond edge knowledge to MUSIC-CRAFT 5.9. The numeric-literal review found only date/build-format example literals absent from active prose; YYYY-MM-DD/HH:MM/hash/stamp semantics remain exact. Numeric presence checking was a review aid, not a substitute for semantic comparison.

All eight prior corrected cards were compared as historical evidence. Their useful cross-links and qualifications survive through current source-grounded contracts; their folder structure, broad headings and stale OPEN/BLOOM statements were not reinstated. The original 223-block scope is fully accounted for, with supplemental supersession evidence named in SOURCES-AND-DESIGN and the active references.

## Consultation simulations and limitations

The A/B/C simulations are in [Sources and design](SOURCES-AND-DESIGN.md#consultation-simulations). They were reviewed as routing paths, not run against the app or in fresh agent sessions. No application suite, build, server, browser product QA, hardware test or mail send was executed. No claim is made about measured skill activation, model quality, saved quota, runtime behavior or equivalence on the next real bug.

The Atlas **location-sidebar handle** remains a documented ambiguity in the active Maps section. The retired chart panel is not restored; changing a distinct location sidebar is the task that requires resolution. True-airway source admission, recording provenance follow-up and physical Tesla/iPhone/listening/inbox/GPU acceptance remain scoped open boundaries. Their uncertainty is retained, not silently discarded. It does not prevent declaring this documentation migration complete within the stated source scope.

Ignored dependencies/build output/private references and secrets were neither audited nor modified. The preservation inventory covers the tracked and pre-existing non-ignored non-sensitive checkout paths; it does not claim a full-disk or secret-content comparison. The documented Codex default limit was researched, but host configuration and actual next-run context injection were not changed/tested. A fresh run is needed to receive the new startup chain; this session already held the original instructions.

## Text dimensions

- Two AGENTS entrypoints: **76,105 → 11,917 bytes** (root 9,149, local 2,768 after migration).
- Ten maintained on-demand guides: **78,684 bytes**, read by relevant section; this is not an initial-load requirement.
- Three exact historical originals: **85,885 bytes**. Historical prompt's active replacement is a short pointer.
- Mapping/provenance/verification artifacts are additional audit-only material, not normal working context. Total repository documentation reduction was not the objective, and no token/quota savings are asserted.
