# Audio QA — 2026-08-28

Status: **local deterministic render and runtime verification complete; real-Tesla listening open**.

## FRACTURE

The production arranger no longer includes `riff` or `response`. Both names are
kept only for the explicit parked audition harness, with permanent zero live
goals. The reference drive still builds atmosphere, harmony, sub/reese, kick,
snare, hats and break detail with road energy.

Reference render:

```bash
node scripts/render-reference.mjs --drive reference \
  --out renders/fracture-reference-no-lead.wav
```

The 148-second `0 → 40 → 80 → 115 → 60 → 115 → 0 km/h` programme produced zero
clipped PCM frames. FFmpeg `ebur128=peak=true` measured:

| Metric | Result |
|---|---:|
| Integrated loudness | -16.0 LUFS |
| Loudness range | 3.8 LU |
| True peak | -0.8 dBFS |

## JUNCTION

The production bank is `SVJCTN04`: 24 complete eight-bar clips, three takes for
each energy state, 76 distinct authorized source recordings and 5,812,755 total
bytes. Every clip uses one `emin-afterdark` identity and the exact
`Emin9 – Cmaj7 – Amin7 – Bmin9` grammar. The active renderer imports no rave
multisample loader, exposes no automatic lead, and the browser schedules one
tonal performance at a time.

The deterministic road sequence test covers `0, 15, 40, 60, 90, 130, 90, 60,
40, 15, 0 km/h`, plus explicit high/medium/low braking states. It preserves the
native tempo map (`127, 135, 158, 164, 168 BPM`), complete eight-bar boundaries,
immediate-repeat rejection, a six-clip decoded-memory ceiling and four-second
entrance/release envelopes.

Offline production render:

```bash
npm run build:junction
ffmpeg -hide_banner -nostats \
  -i renders/junction-sketch-adaptive.wav \
  -filter_complex "ebur128=peak=true:framelog=verbose" -f null -
```

| Metric | Result |
|---|---:|
| Duration | 317.91 s |
| Integrated loudness | -19.5 LUFS |
| Loudness range | 14.2 LU |
| True peak | -2.7 dBFS |
| Encoded audio payload | 5,803,117 bytes |

Local browser QA at `773 × 601` verified JUNCTION in `FULL` at `130 km/h`,
`168 BPM`, Web Audio `running`, three active vertical lanes and zero runtime
issues. The initial exact-QA speed is now seeded into a newly created audio
engine before score selection, preventing a stationary `REST` block from being
scheduled at motorway speed.

Objective measurements can reject clipping, false state, uncontrolled memory
and invalid topology. They cannot establish musical taste. The remaining gate
is critical listening in the target Tesla across the full acceleration and
deceleration sequence.
