# Air Atlas — implementation and verification

The owner selected direction A, Air Atlas, on 2026-09-09 and then selected an expandable bottom aircraft detail and north/driving orientation. Schedules are explicitly deferred: no account configuration or AirLabs request is part of the experience. The owner replied “va benissimo” to the explicit rounded-location forwarding request, clearing the earlier automatic approval rejection. The same operation was resubmitted and approved before implementation.

## Runtime

- Lazy Visual 10, excluded from Lucky. No hidden renderer is mounted during preload.
- `radar-data.php` queries only `https://api.adsb.lol`: nearby traffic at a two-decimal user location and 27 nautical miles; route at the selected aircraft's callsign and three-decimal aircraft position. Strict query allowlist, canonical origin/custom-header checks, TLS validation, no redirects, identifying project User-Agent and 128 KiB response ceiling.
- Private temporary shared cache: at most 32 entries, eight seconds for traffic and five minutes for routes, one upstream request at a time. Failure cooldown honors the supplied Retry-After within a one-day server bound. No persistent location history or diagnostic coordinate fields are added. Standard server access logs may contain the rounded query; the cache stores hashed query keys and bounded response data.
- One client poll every eight seconds; cancellation on unmount, hidden/offline pause, bounded retry up to five minutes and foreground/online recovery without overlap. Only the selected aircraft triggers photo/route detail. Metadata cache holds at most 64 entries for ten minutes in page memory.
- At most 32 nearby measured aircraft per snapshot; stale indications after 30 seconds, expiration after 120 seconds. Position interpolation ends at the latest received observation, never extrapolating beyond it. Unknown type/direction uses a neutral dot. Aircraft targets remain 48 px.
- Zoom in/out and reset to location/default zoom. North-up or GPS driving-up, with a home marker. Driving direction requires speed above 3 km/h and a GPS observation younger than 15 seconds. Missing direction falls back visibly to north; no parked vehicle compass orientation is inferred. Camera center/bearing update together and aircraft shapes rotate with the map.
- Bottom bar expands to credited photo, full departure/arrival airport names, aircraft model, distance, ground track, vertical rate and ICAO address. Route plausibility is labelled, not represented as a confirmed flight plan. Unknown values remain unknown.

## Sources and attribution

ADSB.lol public API and source were inspected at https://github.com/adsblol/api and https://www.adsb.lol/docs/open-data/api/. The user's aircraft `3006b5` / `DLA6CM` returned I-ADJU / E195 and plausible FRA–MXP with full airport names already included. The route response has no scheduled times. No provider code is imported. Service data attribution remains ODbL.

The user's photo is Planespotters photo 1865104 by Matthias Becker. `radar-photo.php` fetches bounded metadata only; the browser loads the remote thumbnail directly. The 200 px thumbnail retains photographer credit and an original-photo link, consistent with the public Terms of Use inspected in Chrome. No full-size photo is bundled or proxied. Source-code ADSB photo proxy probing returned not found, so the direct Planespotters metadata service is used through the same-origin adapter.

All 182 AircraftShapesSVG originals were reacquired from official upstream archive at `0743718760c42a5e91801adc053b5b828a434a5e`, matching Meguru's study revision. Separate GPL-3.0 artwork, original editable SVGs, source hashes and normalized derivatives are retained. The original normalization script reproduces masks using browser bounding boxes. No ignored reference content or GPL program is linked into the host. LICENSE-SCOPE, NOTICE, licensing decision, third-party notices, README credits and unsent community thanks are synchronized.

The dormant `radar-flight.php` adapter remains available for a possible later AirLabs account integration. It reads only a server environment key, never `.env`; without configuration it returns `not_configured`. The frontend does not invoke it. No schedule or configuration is required of users.

## Validation and publication

Controlled-traffic Chrome 773 x 601 and WebKit 874 x 402 checks verify marker selection, real credited photo, route detail, full airport names, expand/collapse, zoom/reset and north/driving toggle with unknown-direction fallback. Short landscape uses a scrollable detail with a sticky collapse control. All 182 SVG hashes and finite positive viewBoxes pass. Direct browser traffic failed CORS; the authorized same-origin adapter now renders real nearby aircraft successfully.

Production publication and canonical evidence are recorded below when complete. Desktop timing and browser screenshots do not establish sustained Tesla 60 FPS or physical iPhone/vehicle acceptance.

Release gate update: 836 native tests pass, including fixed-destination/rounded-query rejection and response-field/row bounds. PHP syntax and 196 exact dependency credits pass. Chrome observed five live aircraft and a credited real photo of 9H-QEG by Kim Philipp Piskol; a live RYR44GP route returned MAN–MXP. Provider cooldown recovery was observed without forced requests. Physical acceptance remains open.

Pointer refinement: global chrome wake previously moved a resting control between pointer-down and click. Air Atlas interactive surfaces now retain their position during activation, while tapping the map can still wake global chrome. A controlled eastbound GPS callback verifies camera-relative home rotation of 0 degrees in driving-up and 90 degrees in north-up. The first upload attempt failed with a sanitized FTP permission-class error before root activation; build 20260909-2336 is not claimed as published. Public-file/status-only diagnostics are added for the retry, without exposing configuration.

Deployment recovery: the next preflight identified a size mismatch in `audio/illobo/sliced-zucchini-edit.mp3` after the incomplete upload. The official `--repair-illobo` mode admits only the recognized catalogue/master set, uploads the verified local master to a temporary path, verifies it, atomically replaces the corrupted file and verifies the final bytes. One master was repaired. New publication skips Illobo files whose full hash was already verified in the same FTP session and whose size remains unchanged. The unknown underlying FTP refusal is not attributed to quota or server technology. The canonical HTML remained on build 20260909-2221 until a complete release can pass. The final suite has 837 passing tests, including staged-repair failure isolation and foreign-file rejection.

Canonical build 20260909-2352.4380499 was published using deployer 59f2224. Official postflight passed, followed by 20 HTTPS identity/asset/cache checks. The canonical browser displayed RYR2YN / 9H-VUA / B38M, photo by Valentin Römer and plausible LPL–BGY. Canonical controlled-GPS testing confirmed the 0/90-degree orientation change. A final compact-layout refinement moves the list target clear of persistent chrome; all four target corners remain unobstructed. Static publication now reuses only byte-identical files recorded by the current preflight, while completed-upload verification remains mandatory.

## Air Atlas canonical release — 2026-09-10 00:34

Build `20260910-0022.5301ec2` is published at https://sedicivalvole.app/. Visual 10 includes real nearby aircraft, the full 182-SVG catalogue, credited photos, plausible routes with full airport names, expandable detail, zoom/reset and north/driving orientation. Schedules remain deferred and no AirLabs setup or request is required. All 838 native tests and 20 canonical HTML/asset/cache checks pass. Chrome verified a live RYR3EB / EI-DLY flight with a photo credited to Alexander Jeglitsch and BGY–EMA route; controlled GPS verified driving-up at 0 degrees and north-up at 90 degrees for an eastbound observation. The compact aircraft-list target is unobstructed at all four corners. WebKit landscape 874 x 402 passed with service workers blocked only for deterministic traffic interception; real traffic and the canonical cache contract were checked separately. No synthetic diagnostic mail was sent.

Official publication uploaded 38 files / 16,857,088 bytes, reused 539 byte-verified static files and all 29 hash-verified Illobo masters, retained prior assets and passed complete postflight. The earlier one-track repair was verified through FTP and normal/cache-busted HTTPS. No unresolved server error remains. Source/deployment checkpoints are pushed. Remaining acceptance: physical Tesla/iPhone touch behavior, driving orientation and sustained map performance; desktop tests do not establish vehicle 60 FPS.

## Air Atlas refinement — 2026-09-10

Refinement of the owner-selected direction A, informed by separate read-only frontend-design and UI/UX reviews of current canonical captures. No new visual direction or imported artwork.

- Stable horizontal controls reserve the persistent speed/report strip in awake and resting chrome; aircraft list and detail are mutually exclusive. The opaque detail has independently scrolling content and permanently reachable expand/close controls.
- 42 px palette-colored aircraft masks (+23.5% over 34 px), unchanged 48 px targets, selection ring, and 182 unchanged upstream shapes. The photograph grows to 152 x 96 px on Tesla, with intact credit and original link.
- Place labels default OFF on mount; ON limits lower zoom to cities, then towns and villages, with additional collision padding. Natural/Palette follows Atlas cartography; UI accents remain tied to the selected palette in both modes. These two map choices are session-local.
- A bounded response allowlist and numeric validation expose optional pressure/geometric altitude, ground/indicated/true speed, Mach, ground track, magnetic/true heading, geometric/barometric climb rate, bank, selected MCP/FCU altitude, altimeter setting, calculated wind/temperature, squawk and position source. Missing values remain unavailable; telemetry is the latest supplied field, not a claim that all fields share one timestamp. Geometric altitude is WGS84 ellipsoid referenced; selected altitude is not a destination or flight-plan claim. True heading may be provider-derived; weather values are labelled calculated.
- Semantics checked against [readsb JSON documentation](https://github.com/wiedehopf/readsb/blob/dev/README-json.md) and the [ADSB.lol API](https://www.adsb.lol/docs/open-data/api/). No upstream code, presets or recordings imported. Query/privacy/caching/retry boundaries remain unchanged.

Validation and new publication evidence follow below. Physical Tesla/iPhone acceptance and sustained vehicle 60 FPS remain unverified.
