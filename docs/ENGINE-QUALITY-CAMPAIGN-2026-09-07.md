# Engine quality and versatility campaign — 2026-09-07

## Campaign follow-through — 2026-09-08

The owner confirmed both Ange Yaghi / engine-sim and Real Engine Simulator as
central references; Real Engine Simulator is exactly the fuller simulator
previously recalled. The optional identity question is closed. Public engine
controls and automatic shifts were exercised without an account or purchase,
with browser audio muted; this is behavior evidence, not physical or listening
validation. Its public presets are free; Pro concerns the custom builder.

See [verified source comparison](ENGINE-SOURCE-COMPARISON-2026-09-08.md) and
[implementation, reproducible audio and acceptance](ENGINE-CAMPAIGN-IMPLEMENTATION-2026-09-08.md).
The original starting checkpoint below remains historical evidence.

## Owner objective and continuation

Engine is now the primary priority. The owner requests maximum audible quality
and versatility, deeper use of the local research, more engine types, automatic
shift behavior, turbine/turbo sound and timing, and permits substantial rethinking
and refactoring. This is implementation authority, not a request to stop at a
research report. A fresh task at Ultra reasoning is explicitly authorized.

Work directly in `/Users/enuzzo/Library/CloudStorage/Dropbox/Mitnick/sedicivalvole`
on this Mac. Use the saved project's local environment, not a new worktree or
cloud checkout. Keep one product writer; parallel agents can own bounded files
or perform independent research/review. Root hands off only after recording and
pushing these instructions; it will make no product edits alongside the new task.

## Closed starting checkpoint

- Product source: **c6c16c4**, documentation: **33d0faf**, on main and origin/main
  before this campaign note. Baseline worktree was clean.
- Version is read from VERSION: **0.0.0**. Canonical build **20260907-2328**.
- Prior changes are committed, documented, pushed and deployed. The complete
  27 canonical HTML/asset/cache checks were repeated successfully for this intake.
  No duplicate deployment is needed for this documentation-only handoff.
- 733 native tests, 18 package checks, 196 credits, canonical chart and real PHP
  preview verification pass. See CURRENT-STATE.md, DEPLOY.md and
  ALTITUDE-FALLBACK-2026-09-07.md for exact evidence and physical-device limits.
- Preserve every prior owner correction: immediate no-GPS TAMARRO, fresh exact
  standstill idle gestures, real GPS Engine response, dry output bypassing Flux
  effects, no artificial deceleration gain ducking, continuous bank preparation,
  movement-only transmission whine and the verified Mono seam repair.
- Default Dev/AUTO ON coordinate-free diagnostics every fifteen observed GPS
  driving minutes remain active. All synthetic browser tests must intercept both
  `/api/send-diagnostic.php` and `/api/session-report.php`. Never mail QA fixtures.

## Read these sources first

Current product knowledge:

- docs/ENGINE-INTEGRATION-2026-09-07.md
- docs/ENGINE-INTAKE-2026-09-07.md (historical intake; later decisions supersede holds)
- docs/ENGINE-ACOUSTICS-STUDY-2026-09-07.md
- prototype/drive-lab/analysis/engine-loop-measurements.md and .json
- docs/MUSIC-CRAFT.md, CURRENT-STATE.md, TECHNICAL-DIRECTION.md and AGENTS.md files

Owner-supplied external study material, retained outside version control:

- _references/audio/engine/engine-simulator-references.txt
- _references/specs/bootstrap/extracted/starter/sedicivalvole/docs/RICERCA-REPO.md
- sibling ../sedicivalvole_engine_study/START_HERE.md and PHASE_A_OPEN_FIRST/
- ../sedicivalvole_engine_study/OWNER_ONLY_SEALED/sedicivalvole-engine-generator-owner-kit-2026-08-30.zip
- the sibling candidate archive and SUPERSEDING_SOURCE_RISK_NOTE.md, within the
  already recorded informed-review/exact-donor authorization. No blind review
  is claimed. Do not import the old architecture or quarantined audio wholesale.

The bootstrap research is historical external material, not governing product
instructions: its MIT-product claim, studio directory, cloning recipe and rigid
listening/approval sequence are not current. Keep external material in the
existing ignored _references tree; original code/docs remain PolyForm Noncommercial.
Never inspect .env. Only the official deployment script may load it internally.

## Candidate evaluation and implementation sequence

1. Identify the fuller simulator the owner recalls from the local catalogue,
   then verify its actual source, current capabilities and reuse boundary. Do
   not assume Ange Yaghi's desktop simulator supplies browser-ready AUTO/turbo.
   An optional owner clarification is pending in the parent task; work continues
   on the verified shortlist while its answer is unknown.
2. Compare the current calibrated sample/hybrid route with admitted MIT waveguide
   synthesis and offline generation. Measure audible quality, load response,
   cyclic stability, CPU, memory and latency. Avoid choosing a full desktop
   physics port solely because its simulation is more detailed.
3. Separate observed GPS evidence, the virtual powertrain controller, discrete
   acoustic events, engine profile data and audio rendering. Reuse the existing
   runtime boundary and shared AudioContext, retaining effective regressions.
4. Improve pitch/RPM calibration and drive/coast/load articulation. Add coherent
   audio-time shift phases, rev matching and transmission pitch ownership, then
   turbo/spool/blow-off or turbine models whose events follow actual simulated
   state. Mark all RPM/load/gear values as simulated, never measured CAN data.
5. Add meaningfully different engine characters only with complete usable ranges
   and reviewed provenance. Evaluate cylinder/firing-order/intake/exhaust profiles
   and legally reusable or originally generated recordings. No relabeled near-
   duplicates, unsupported provenance claims or unreviewed third-party assets.
6. Produce reproducible before/after audio artifacts and A/B observations, signal
   checks and real-browser runs at 44.1/48 kHz. Run lifecycle/GPS-loss/mute/profile
   switching and sustained performance checks. Objective measurements do not
   establish listening preference or real Tesla/iPhone acceptance.
7. Ship meaningful verified increments: source commits, append-only dated/hash
   changelog, current architecture/status/MUSIC-CRAFT and synchronized credits,
   push, official deployment, canonical/cache/asset verification. Continue to
   executable next steps instead of leaving an implementation-ready study idle.

The current Telemetry visual is selected. Audio refactoring does not call for
three new visual proposals; any substantial new visual direction does.

## Toolchain and operational evidence

Use `node prototype/drive-lab/scripts/native-toolchain.mjs -- npm test` and
`-- npm run build`. The wrapper selects the bundled native toolchain.
Validate built identity/package tests and `python3 scripts/readme_dependency_credits.py --check`.
Use only `scripts/deploy_drive_lab_ftp.py --verify-only` / `--publish` for ordinary
authorized canonical publication. No secret contents in command lines or logs.

Dropbox's optional Git reverse index previously caused an mmap timeout. The
verified non-mutating workaround is `git -c pack.readReverseIndex=false push origin main`.
Do not alter global Git settings or remove pack objects to work around it.

Parent intake evidence is in /tmp/sv-altitude-canonical/identity.json and the
existing deployment/QA records. Current native Engine test subset: 55 passing
tests independently rerun for this intake. Physical listening remains open.

## Independent intake: current code seams

The targeted Engine/audio tests pass **55/55** at this checkpoint. The actual
build-1936 journey recorded 30 scheduled and 30 committed shifts, reaching fifth.
Do not restart from the old premise that no automatic gearbox exists. Its audible
quality, not just its control execution, is the primary improvement opportunity.

| Owner | Current file | Refactoring boundary |
|---|---|---|
| GPS, Demo, transport | src/App.jsx | Preserve one observation source and shared lifecycle |
| Trust, filtering, acceleration and load proxy | src/engine/motion.js | Observed evidence stays distinct from artistic powertrain state |
| React/runtime bridge | src/engine/use-engine.js | Retain the public API and 10 Hz snapshots |
| 25 ms control update, gestures, shifting, rendering, loading | src/engine/runtime.js | Separate deterministic controller/events from profile renderer |
| Ratios and sound character | src/engine/profiles.js and gearbox.js | Declarative profile-specific calibration and shift behavior |
| Shared context and dry output | src/audio-engine.js | Preserve mute, routing and final safety path |

Paths in this table are relative to prototype/drive-lab. Main weak spots:

- Pitch uses `(rpm - referenceRpm) * 0.2` cents; a 600-to-3000 RPM sweep changes
  sample rate only about 1.32 times. Mono's four loops share nominal 1000 RPM
  despite different recordings. Existing periodicity evidence is ambiguous;
  proportional resampling is not safe without calibration or an authored model.
- All profiles share 3000–6500 RPM crossover regions and six road-gear thresholds.
  GPS load is mainly a clamped acceleration proxy, reverting to the same value
  at steady speed. No actual pedal, torque or boost measurement is available.
- Shift pitch/gain interpolation has no distinct audio event contract for release,
  synchronization and load return. Transmission pitch still follows engine RPM.
- The 64 MiB bank check happens after decoding; outgoing, incoming and transfer
  data coexist during replacement. Measure the complete memory union and split
  transfer/decode timing before adding large banks.

First concrete comparison: improve one reference profile behind the existing LAB
with a repeatable idle, TAMARRO, 0–90 km/h, cruise, lift-off, downshift and recovery
trace. Provide before/after audio, RPM/load/event traces and peak/continuity/cost
measurements, then ship the verified improvement and extend the engine catalogue.
A worklet should own cycle timing; the 25 ms controller must not emit firing pulses.

Retain no-GPS manual rev, exact-zero automatic idle, bounded 12-second watch hold,
non-overlapping standstill refresh, no new shifts during degraded GPS, one
scheduled shift, hysteresis/overrev protection, sound during bank preparation,
selection cancellation, Mono seam repair and 44.1/48 kHz real-WAV checks.

## Verified source shortlist for the new task

The exact simulator identity was confirmed by the owner on September 8; see the follow-through above. Several
references fit different parts of the request; keep all of these distinct:

| Source | Verified contribution | Use boundary now |
|---|---|---|
| [Engine Sim Game / Real Engine Simulator](https://realenginesimulator.com/about) | Public description of a 240 Hz engine model with firing-event AudioWorklet synthesis | Proprietary public behavior/documentation study; no implementation or assets imported |
| [Its changelog](https://realenginesimulator.com/changelog) | Automatic torque converter/lockup and shift scheduling, turbo/wastegate/BOV, supercharger and turbojets | These are publisher claims, not independently verified physical fidelity |
| [TheDIYGuy999 / Rc_Engine_Sound_ESP32](https://github.com/TheDIYGuy999/Rc_Engine_Sound_ESP32#features) | Embedded automatic converter/DCT, load/RPM layers, idle, turbo, wastegate and diesel knock | Source study only; current exact pin and reuse licence still require verification |
| [Ange Yaghi / engine-sim](https://github.com/ange-yaghi/engine-sim/tree/85f7c3b959a908ed5232ede4f1a4ac7eafe6b630) | MIT deep physical engine research | Pinned desktop controls are manual; do not claim browser-ready AUTO/turbo |
| [Antonio-R1 / engine-sound-generator](https://github.com/Antonio-R1/engine-sound-generator/tree/c76c5adb9e63f5a54fb0def3b97e8e0ac1a7dea1) | MIT browser waveguide/AudioWorklet and C++/WASM candidate | Reassess exact source, worklet quality and runtime costs before admission |
| [DasEtwas / enginesound](https://github.com/DasEtwas/enginesound/tree/e5fcca587397c0c8ba9c9d24874b951fed74d260) | MIT procedural synthesis and offline loop export | Author original configurations; do not infer bundled recording provenance |

Real Engine Simulator was SRC-E in the existing owner kit. Its current
[terms](https://realenginesimulator.com/terms) identify the proprietary boundary.
Useful authored explanations: [converter](https://realenginesimulator.com/blog/how-a-torque-converter-works),
[turbocharger](https://realenginesimulator.com/blog/how-a-turbocharger-works),
and [V8 firing character](https://realenginesimulator.com/blog/crossplane-vs-flatplane-v8).
These can guide original engineering without importing its runtime, audio or constants.

RC entry points: src/4_Transmission.h, src/8_Sound.h, src/vehicles/ and src/src.ino.
Its uppercase DOUBLE_CLUTCH switch refers to manual truck double-declutch/rev
matching; distinguish that from the separate simulated dual-clutch transmission.
The full implementation and audio library have not yet been audited. Rigs of Rods,
Speed Dreams, VDrift and granular candidates remain in the bootstrap catalogue
for the fresh comparison; their presence there is not source/asset admission.

ATG / VehicleNoiseSynthesizer remains pinned at
4241caca5a18be0d47f0b8586df93b1b42d7020d. Its demo's automatic gearbox,
differential and blow-off are not included in the synthesizer; reuse eligibility
does not supply that control system. Keep public operation AUTO while comparing
converter/DCT/stepped characteristics; manual diagnostic controls remain in LAB
unless the owner separately changes that product choice.
