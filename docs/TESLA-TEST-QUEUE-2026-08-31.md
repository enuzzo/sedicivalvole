# Tesla test queue — 2026-08-31

This is the stable owner-facing test register for the evening drive. Each ID is
permanent and maps to one row of `MILESTONE-CHECKLIST-2026-08-31.md`. Report a
result as `R7-01 PASS` or `R7-01 FAIL — short observation`; add a photo, video,
or diagnostic attachment when the result depends on motion, timing, or frame
behavior.

Do not begin the run until the final canonical build stamp is recorded below.

- **Final test build:** `20260902-2322` from implementation checkpoint `4624f70` and published source checkpoint `5691f58`. It retains the `1ef48be` shared Soundtrack/adaptive AudioContext and bounded recoverable transport repair, mandatory insert `10C`, the motion-independent chrome-wake follow-up, the dark-drawer white Now Playing presentation with its original black icon preserved for future LIGHT appearance, and the GPS-calibrated DRIVEY road/traffic response.
- **Target viewport:** Tesla split view, nominal CSS `773 × 601`
- **Result states:** `PASS`, `FAIL`, `NOT RUN`, `BLOCKED`
- **Safety:** a passenger operates controls and records evidence; the driver
  only judges the experience when safe.
- **Canonical-test boundary:** codes marked `CANONICAL PASS <build> · TESLA PENDING`
  are implemented and live, but retain `TESLA PENDING` until the physical-car
  result is reported. Browser and automated PASS do not close that gate.

## Product-shell focus and chrome retraction — milestone row 1

| ID | Status | Test | PASS condition | Linked work |
|---|---|---|---|---|
| `R1-01` | NOT RUN | Open and close Music, Visual, REPORT, GPS help, and Performance FX one at a time. Then change a palette and toggle MUTE and FX. After each completed action, touch nothing for at least five seconds. | Every drawer closes normally; no trigger keeps a white focus stroke or selected state; header and footer retract automatically after about 4.2 seconds without tapping the visual. Keyboard focus remains contained only while a surface is open. | Central post-action focus handoff in `9daf8f6`, live build `20260831-2244`; exact local and canonical Browser QA already pass. |
| `R1-02` | CANONICAL PASS `20260902-0103` · TESLA PENDING | Open every closable drawer/surface. Tap the exposed backdrop; reopen, drag partially down or right and release, then drag decisively past the threshold. Repeat with nested scroll and controls. Finally begin a Back gesture in the first `28 px` of the left edge. | Backdrop tap closes immediately. A partial drag cancels; a dominant interior swipe closes by bounded distance/velocity, releases focus once, and never steals buttons or ordinary scrolling. The reserved left edge yields to Chromium so one swipe cannot close the app surface and navigate Back together. CLOSE and Escape remain equivalent. | Shared pointer capture, overscroll containment, dynamic `76–150 px` / velocity gate and native-edge reservation; exact local swipe/backdrop Browser QA and canonical artifact identity pass. |
| `R1-03` | CANONICAL PASS `20260901-2232` · TESLA PENDING | Select a visual, palette, Music mode/source/track and manual Performance FX depths; navigate away/back, then reload. Finally use RESET SAVED STATE from both splash and REPORT. | Reload restores only admitted product preferences and a truthful non-autoplay identity; reset visibly returns Play the Road/Aperture defaults. GPS, diagnostic evidence, transient media URLs and automatic vehicle envelopes never persist. | Versioned safe-preference schema and shared reset action pass deterministic privacy tests and a visible reset/reload Browser flow. |

## Route, GPS, and sampled-score corrections — milestone row 2

| ID | Status | Test | PASS condition | Linked work |
|---|---|---|---|---|
| `R2-01` | NOT RUN | Drive ATLAS for several minutes, then zoom out progressively. | The complete route from the session origin remains visible and highlighted; no earlier segment disappears. | Persistent in-session route accumulation and zoom-independent highlight. |
| `R2-02` | NOT RUN | Observe the ATLAS live position while stopped and moving. | One point exists only at the newest route end; it pulses about once per second with a restrained ripple; no animated line moves along the route. | Single consolidated marker source and pulse/ripple treatment. |
| `R2-03` | NOT RUN | Observe the navbar with accurate GPS, accuracy above `4 m`, and GPS unavailable if safely reproducible. | `GPS` plus accuracy only; green at `≤4 m`, orange above `4 m`, red when disconnected. | GPS confidence copy and thresholds. |
| `R2-04` | NOT RUN | At one fixed cabin volume compare NIGHTSHIFT, JUNCTION, then FRACTURE using representative energetic passages. | No surprise perceived-level jump; NIGHTSHIFT no longer dominates; FRACTURE is measured rather than adjusted by assumption. | Shared sampled-score output gain normalization. |

## Adaptive music and global effects — milestone row 4

| ID | Status | Test | PASS condition | Linked work |
|---|---|---|---|---|
| `R4-01` | NOT RUN | FRACTURE: PARK, smooth ascent, smooth descent, and two speed reversals. | Continuous authored behavior without click, silence, stuck layer, or unexpected level step. | Existing adaptive-score implementation plus normalized shared output. |
| `R4-02` | NOT RUN | JUNCTION: PARK, `20 → 21 km/h`, native-tempo entry, eight-bar changes, descent, and reversal. | PARK remains beat/bass-free; the quiet break enters near `13 km/h`; changes remain complete and click-free. | JUNCTION authored bank and network/readiness safeguards. |
| `R4-03` | NOT RUN | NIGHTSHIFT: PARK, all six tempo families on ascent, then descent and reversal. | Every family becomes reachable without a loud jump, dropout, or stale family. | NIGHTSHIFT family mapping, shared gain correction, and readiness safeguards. |
| `R4-04` | PARTIAL PASS 2026-08-31 · ILLOBO/JAMENDO RETEST IN PROGRESS | Toggle OPEN, BLOOM, and UNDERWATER across FRACTURE, JUNCTION, NIGHTSHIFT, Illobo, and Jamendo, then disable global FX. | Each effect is audible but controlled; UNDERWATER is unmistakably dark from its visible engagement while retaining usable level; disabling FX restores dry output in every source. | The owner now confirms that UNDERWATER decisively enters during braking in the active drive test. Illobo and Jamendo verification is still in progress, so the complete five-source matrix remains open. `8c53e8d` supplies the repaired audio path; `e77d939` changes MERIDIAN visuals only. |
| `R4-05` | NOT RUN | While music plays, trigger or wait for one normal vehicle alert/navigation prompt. | The vehicle alert remains clearly audible and is not masked by the product. | Alert-safe level and effects boundary. |
| `R4-06` | NOT RUN | Reveal the running footer, compare MUTE with FX, then toggle each one independently in both directions. | Both controls have the same width and `LABEL / ON–OFF / GLOBAL` hierarchy; each state changes immediately without changing the other control. | Shared MUTE/FX control anatomy and equal-width Tesla grid tracks. |
| `R4-07` | NOT RUN · READY ON LIVE `20260901-1943` | Open the `2 × 4` Performance FX deck in FRACTURE, JUNCTION, NIGHTSHIFT, Illobo, and Jamendo. Compare every authored tap with `100%`, especially manual Underwater dry → submerged → dry and the adjacent Bass Drive / Radio Cut / High Cut family; then combine deliberately hostile pairs and RESET. | The tap is immediately playful and the last `82–100%` becomes unmistakably more extreme. Manual Underwater produces progressive immersion, textured maximum depth and surfacing; Bass Drive adds low weight, Radio Cut isolates the middle, and High Cut progressively removes the top end without becoming Underwater. No state clicks, clips, silences, runs away, changes `1×`, disables braking Underwater, or leaks after RESET/source switching. | `0f4a501`, live build `20260901-1943`: `544/544`, exact local and canonical `773 × 601`, High Cut `8 kHz` attenuation `−12.94 dB` tap / `−59.68 dB` full with `300 Hz` at `+0.30 dB`, hostile sum peak `0.91672`, canonical byte identity and RESET/focus/log QA. |
| `R4-08` | REGRESSION FIX LIVE `20260902-1954` · TESLA RETEST | With global FX ON and manual Underwater at zero, play one Jamendo track and one Illobo track. For each, brake until the visible automatic UNDERWATER state is established, hold briefly, then release and regain speed. | Both recordings become unmistakably submerged during braking and audibly resurface on release; the effect never depends on opening Play the Road first, survives source switching, and does not change authored `1×` playback. | `1ef48be` shares one exact AudioContext between the audible Soundtrack graph and vehicle-macro detector in either launch order. Local/canonical REPORT proves `shared` topology, running effects/worklet, vehicle master enabled and changing underwater macro; cabin audibility remains this test. |

## Visual and performance acceptance — milestone row 5

| ID | Status | Test | PASS condition | Linked work |
|---|---|---|---|---|
| `R5-01` | NOT RUN | APERTURE from `0–40 km/h`, including a gentle acceleration and lift-off. | Motion becomes legible by about `40 km/h`; no blank frame, violent jump, or control obstruction. | Current APERTURE response. |
| `R5-02` | PARTIAL PASS 2026-08-31 · AMPLIFIED LIVE `20260831-2207` · RETEST | In MERIDIAN accelerate progressively from rest through urban, medium, motorway and `130 km/h`; brake firmly until UNDERWATER is visually established, then resume speed and watch the complete surfacing. Briefly confirm VERTIGO 02 at low and medium speed afterward. | MERIDIAN widens perspective perceptibly throughout `0–130`; braking visibly compresses/darkens/slows the field without steps; renewed acceleration produces an unmistakable continuous reopening, clearing and return of motion. Both visuals remain fluid, readable, correctly themed, and free of degraded audio. | The owner accepts MERIDIAN's beauty and confirms the first smoothing fix removed the prior braking problem, then requested stronger contrast. `e77d939` raises dry FOV to `124°`, strengthens UNDERWATER to `−11°` with deeper motion/glow/fog contrast, and gives the complete surfacing a frame-rate-independent `0.50 s` response after `0.24 s` engagement. |
| `R5-03` | NOT RUN | Use ATLAS controls, collapse/reopen the passenger panel, pan/zoom, and collect a fresh diagnostic after several minutes. | Touch targets are reliable; attribution remains subordinate; cadence is stably near the explicit 30 FPS target without thermal or audio regression. | Map framebuffer cap, request cancellation, world-copy removal, and marker consolidation. |
| `R5-04` | CANONICAL PASS `20260902-2322` · TESLA PENDING | Run DRIVEY at approximately `5`, `40`, `90`, and `130 km/h`; hold each speed long enough to judge road travel and opposing traffic, then cycle all available view/render controls. | Walking pace looks like walking pace rather than fast urban driving; road and traffic scale progressively with the GPS readout through the complete range. No launch overspeed flash, blank view, stuck camera, or audio degradation appears. | Checkpoint `4624f70` shares VERTIGO's quadratic response, compensates Drivey's native square-root cruise physics, synchronizes player/traffic before reveal, and proves exact `5 / 40 / 90 / 130 km/h` equilibrium without modifying upstream files. Build `20260902-2322` passes local rendered `5 km/h`, canonical muted/resting render, byte identity and clean-console QA; physical-Tesla judgement remains open. |
| `R5-05` | NOT RUN | Exercise every PRTCL type and effect long enough to expose frame or thermal drift. | Each type/effect is reachable and stable; no sustained thermal collapse, blank field, or audio degradation. | Existing PRTCL surface and diagnostics. |
| `R5-06` | CANONICAL PASS `20260902-1905` · TESLA PENDING | Open the running Visual drawer at `773 × 601` and inspect every admitted choice without scrolling. Switch renderers and open/close Discover. | Two balanced columns show all eight primary choices, each with one useful description; active and destination states stay distinct. Gradient appears once as `GRADIENT 08`; Japanese Mist, Acid Orchard, and Chromatic Silk do not add duplicate catalogue entries. | Exact local/canonical `773 × 601` Browser QA and catalogue tests pass; physical touch flow remains. |
| `R5-07` | CANONICAL PASS `20260901-2232` · TESLA PENDING | In APERTURE, repeat slow `0 → 5 → 15 → 40 → 15 → 0 km/h` movement while the wall advances and retreats; collect a diagnostic. | Every transition remains continuous with no sustained frame collapse; geometry/timing/audio/chrome remain intact. | Raw wall speed now uses asymmetric time-based smoothing and WebGL caches CSS dimensions instead of reading layout every frame; deterministic model and 60 FPS local Browser evidence pass. Tesla motion remains decisive. |

## Degraded-network recovery — milestone row 6

| ID | Status | Test | PASS condition | Linked work |
|---|---|---|---|---|
| `R6-01` | NOT RUN | With a cold cache and intentionally poor connectivity, select JUNCTION once and wait through loading/recovery. | A harmonic safety bed remains; a true stall states the exact reason; playback recovers after cooldown without a second selection. | Shared `45 s` transfer / `56 s` JUNCTION readiness boundary and abort/retry state machine. |
| `R6-02` | NOT RUN | Repeat the same cold-cache degraded-network run with NIGHTSHIFT. | A harmonic safety bed remains; failure is explicit; playback recovers without selecting NIGHTSHIFT again. | NIGHTSHIFT parity for abort, cooldown, retry, and safety bed. |

## Soundtrack mechanics — milestone row 7

Tests `R7-01`–`R7-09` are runnable on the canonical build recorded above.
`R7-10`–`R7-13` are canonical in build `20260901-2232`; regression test
`R7-14` is canonical in build `20260902-1954`.
Office/live Browser tests cannot
substitute for the audible physical-cabin verdict.

| ID | Status | Test | PASS condition | Linked work |
|---|---|---|---|---|
| `R7-01` | NOT RUN | Play Soundtrack, then use NEXT and PREVIOUS at normal intervals; let one track end naturally. | Every manual change is an audible nominal `450 ms` equal-power crossfade with no click or silence; natural end advances once from the next track's beginning and never replays the ended deck. No transition remains indefinitely on FADING if the browser briefly interrupts its audio clock. | Transition wiring plus `57fed11` hard track-end boundary and stale-target restart; suspended-clock wall-time settlement `7085941`. |
| `R7-02` | NOT RUN | While a fade is active, perform `NEXT → PREVIOUS → NEXT` rapidly, then pause/resume. | The audible mix never drops; the licence/credit card never names a non-audible track, settles on the final target, and pause/resume does not revive an older timestamp or identity. | Rapid-retarget atomic commit plus deterministic 120-action transport stress in `57fed11`. |
| `R7-03` | NOT RUN | With a cold cache and slow connection, tap **Soundtrack** and immediately tap **Play the Road** before loading ends; repeat in the other direction, then allow each load to settle. If practical, briefly interrupt connectivity while audio is already playing. | The selected pane changes on the tap, states `Loading Soundtrack…` or `Loading Play the Road…` only while needed, and never waits behind the old pane. The outgoing source stops immediately; a late request cannot switch the pane or restart old audio. Existing audible playback survives a failed replacement truthfully, and recovery stays in the selected source. | Final build `20260901-0933` retains optimistic source state and revision guards from `137ddeb`; transactional catalogue/preload recovery remains `57fed11`. |
| `R7-04` | NOT RUN | Scan the compact QR for the current track, change track, and scan again. | Each QR opens the exact current public track page; no stream URL or stale prior-track destination is exposed. | Published in build `20260831-1241`; compact direct-content QR handoff. |
| `R7-05` | NOT RUN | From the passenger seat, operate track rows, PREVIOUS/PLAY/NEXT, licence links, and the QR at `773 × 601`. | Controls are comfortably reachable; transport targets are at least `48 px`; the complete licence/artist credit remains legible. | Published in build `20260831-1241`; Soundtrack drawer touch and attribution treatment. |
| `R7-06` | SUPERSEDED BY `R4-07` | Do not run the retired four-pad Chorus/Echo matrix. Use `R4-07` for the current eight-effect Performance FX deck across all five sources. | Historical identity remains traceable without asking the owner to test controls that no longer exist. | The current roster and audible cabin matrix are owned by stable test `R4-07`. |
| `R7-07` | FAIL 2026-08-31 · FIX LIVE `20260901-0012` · RETEST | Start Soundtrack and note the Jamendo Library track and three cover previews. Select Illobo Featured three times at normal intervals, use NEXT/PREVIOUS, then press the complete **Jamendo Library / Choose your route** card to return. Repeat Illobo → Jamendo once quickly before loading settles. | Every Featured press starts a different complete Illobo recording at `0:00`; NOW PLAYING, highlighted row, credit/QR, `LO` footer mark and Tesla mini-player title agree. Both path cards and all three Jamendo cover previews remain visible during Illobo playback. Returning to Jamendo immediately restores pace, genre and all visible album rows, starts a Jamendo recording, changes the footer to `JM`, and a late Illobo response never retakes the pane or audio. NEXT/PREVIOUS remain inside the selected source and one failed source cannot poison later tracks. | `0660d71` restores the Jamendo → Illobo → Jamendo round trip and source-owned artwork; `1a47e23` supplies the separate verified 29-track owner catalogue; `236f2c9` guarantees complete-track head starts; `57fed11` hardens failure/navigation; `2c0f5f8` corrects provider identity; `7085941` prevents suspended audio clocks from leaving FADING stuck. All 29/29 remote Illobo masters were fully reverified for final build `20260901-0933`; exact live Browser QA passed the Illobo start and complete return to all 15 Jamendo genres. |
| `R7-08` | NOT RUN | At `773 × 601`, open Music → Play the Road and inspect all cards; switch to Soundtrack, tap the empty area of several Pace/Genre chips rather than the icon itself, then start two visible tracks. Enter Illobo and use NEXT twice before returning to Jamendo. | The two primary selectors stay horizontal at the top. Play the Road shows exactly JUNCTION and NIGHTSHIFT as two sampled first-row cards and FRACTURE as one full-width responsive-generative card below, with distinct covers and useful descriptions. Soundtrack shows Pace as one vertical three-item rail, all 15 full genre names as a readable `5 × 3` grid, two rows of six tracks, and the complete player/credit area without scrolling. The whole chip and whole track row respond on one tap; selected, loading, playing, and paused states remain unambiguous. Illobo rows and Now Playing use distinct title-specific covers, while the playlist card retains the animated LOBO identity. | Owner-selected Generated image 35 checkpoint `8450109`, one-tap Music refinement `856232b`, Soundtrack composition `5c498ac`, and transition recovery `7085941`; exact canonical `773 × 601` no-scroll Browser QA passes on final build `20260901-1041`. Tesla touch/visual acceptance remains open. |
| `R7-09` | NOT RUN | Open Music and alternate the horizontal **Play the Road** / **Soundtrack** selectors several times. In Soundtrack move between Jamendo and Illobo, start and pause a track, then inspect the Featured card, Now Playing and Track Credit at `773 × 601`. | Each top selector changes destination in one tap and never hides the return route. The Featured card reads `FEATURED ARTIST`, `Lobo Playlist`, and describes music written and performed by Illobo. A four-band activity mark animates only while the selected track is actually playing. The QR and complete credit never overlap, crop, or require scrolling. | Horizontal source-selector checkpoint `8450109` plus bounded player/credit composition in `5c498ac`; exact canonical QA on final build `20260901-1041` proves complete Play the Road/Soundtrack switching with zero scroll and an empty console. Tesla touch acceptance remains open. |
| `R7-10` | CANONICAL PASS `20260901-2232` · TESLA PENDING | While any visual runs, use the persistent previous / play-pause / next transport repeatedly with Play the Road, Illobo, and Jamendo; let chrome retract and return. | The controls remain reachable in the measured bottom-centre lane, reflect playing/paused state, preserve the renderer/focus, and do not cover GPS, palette, FX, attribution or map controls. | Exact local/live `773 × 601` Browser QA and shared transport source tests pass; Tesla touch/occlusion remains. |
| `R7-11` | CANONICAL PASS `20260901-2232` · TESLA PENDING | With a playing fixed track, press play/pause, previous, and next in Tesla's browser-labelled mini-player wherever exposed. Repeat rapidly during one transition. | Every exposed control invokes the shared transport exactly once; previous/next stay in the source; unsupported actions are absent or harmless; metadata remains coherent. | `play`, `pause`, `previoustrack`, and `nexttrack` handlers plus MediaMetadata are registered defensively; Tesla exposure remains the acceptance gate. |
| `R7-12` | CANONICAL PASS `20260901-2232` · TESLA PENDING | Change tracks by row tap, persistent transport, automatic end, Illobo Featured and Tesla Media Session. Include a failed/stale replacement. | Every committed identity shows one `3.2 s` notice with title, album/source and artist; failed, stale or preloaded targets show none; the overlay is glance-readable and does not pin chrome. | The notice subscribes to committed current identity and exact local/live viewport QA passes. |
| `R7-13` | CANONICAL PASS `20260901-2232` · TESLA PENDING | Open Music → Soundtrack → Jamendo at `773 × 601`; compare Pace, 15 genres, six tracks and Now Playing while stopped/loading/playing/paused. | Redundant library/authored headings are absent; Pace/Genre are vertically separated; fresh mix stays right-aligned; filters/titles/artists are larger; the compact activity mark follows real playback; no page scroll appears. | Exact local/live no-scroll Browser QA passes with all 15 genres, six tracks, player and credit visible. |
| `R7-14` | REGRESSION FIX LIVE `20260902-1954` · TESLA RETEST | On a cold or weak connection, start Jamendo, use NEXT to request another track, then exercise PREVIOUS/NEXT again. If one requested track stalls, wait at least ten seconds; repeat once after recovery and let one track end naturally if practical. | A healthy target starts normally. A stalled start never blocks transport indefinitely: within about ten seconds the prior audible identity is restored with a recoverable error, later controls work, and a delayed stale track never starts by itself. Natural end advances once from the new track's beginning. | `1ef48be` adds an owned wall-clock deadline, atomic rollback, incomplete-record disposal and exact-record stale guard. Deterministic never-settling and late-resolution tests plus canonical `NEXT → PREVIOUS → NEXT` pass; weak-network Tesla behavior remains this test. |

## Illobo identity and Tesla media title — milestone row 8

| ID | Status | Test | PASS condition | Linked work |
|---|---|---|---|---|
| `R8-01` | NOT RUN | On the final build recorded above, open Music → Soundtrack and watch the Illobo Featured cover for at least ten seconds. | A clearly white-on-black solid state dissolves into the original black-on-graphite outline state and back. Each full dissolve takes about four seconds, continues without a static hold, and has no flicker, clipped outer line, rounded corner, border, or layout movement. | Milestone row 8 · base `05a754b`; perceptual correction `6218f98`. |
| `R8-02` | NOT RUN | Start one Soundtrack recording, inspect Tesla's browser-labelled mini-player, then pause it. | During play it shows `16 - Artist - Track title`; pausing restores the normal sedicivalvole page title. Audio, QR, and in-drawer credit remain on the same track. | Milestone row 8 · `05a754b`; Tesla browser behavior is the acceptance gate. |
| `R8-03` | CANONICAL PASS `20260901-2232` · TESLA PENDING | Cold-load Soundtrack, open Illobo and use NEXT through several covers while observing network and sharpness. | Every drawer request uses a matched 512 px WebP; no HD PNG is downloaded; all 29 covers stay distinct/sharp; original masters remain local. | Deterministic mapping/header/size tests and canonical WebP 200 / retired PNG 404 pass; public WebP set is about `380 KB` versus about `9.6 MB` for local PNG masters. Tesla network/sharpness remains. |

## ATLAS Navigator Plaque — milestone row 9

| ID | Status | Test | PASS condition | Linked work |
|---|---|---|---|---|
| `R9-01` | NOT RUN | Drive with ATLAS through several real changes of direction, including one crossing of north if the route permits. | The filled arrow points in the actual travel direction and moves smoothly; cardinal and degrees agree with it; crossing north takes the short rotation without a near-full spin; the road name follows the current named road and disappears rather than inventing copy when rendered data has no name. | Selected Navigator Plaque, continuous heading unwrapping, eight English cardinal sectors and rendered `transportation_name` lookup in `79d9c9b`. |
| `R9-02` | NOT RUN | From the passenger seat, observe the plaque with the side panel open and collapsed, then pan, rotate, tilt and zoom the map. | Arrow, cardinal, degrees and available road name remain legible and subordinate at a glance; the plaque neither blocks map gestures nor moves with the passenger panel; map interaction, attribution, route and pulsing point remain intact. | Milestone row 9 exact-viewport layout and interaction boundary in build `20260831-1653`. |
| `R9-03` | NOT RUN | With real GPS, keep the Drive Lab open for at least three minutes across standstill and ordinary driving. Compare its Speed with the navbar; inspect Distance, Moving time, Average speed, and Elevation. | Both speed values agree at a glance; Distance and Moving time grow truthfully; Average speed changes plausibly. Elevation is plausible, its smoothed trace retains labelled metre/time axes, and the source says `LIVE · OPEN-METEO / COPERNICUS`; a network interruption retains the last value only as `STALE` or shows `UNAVAILABLE`, never fresh invented data. | Corrected Drive Lab `7c9df06` preserves the `7debfe3` terrain freshness boundary and session-only journey model; exact live `773 × 601` QA proves the complete first line and full-width Elevation. |
| `R9-04` | NOT RUN | Drive one sequence containing standstill, progressive acceleration through several speed bands, a clear brake, a renewed acceleration and several turns including north if practical. Observe every Drive Lab strip/history from the passenger seat. | The full-width Accel/Braking trace crosses its labelled zero axis in the correct direction without claiming `50/50` at rest; the low Speed-band strip redistributes visibly; smoothed Heading follows the road and crosses north without a long jump; the low Moving/Stopped strip shifts plausibly. Legends, axes, labels and percentages remain readable in one glance and no chart freezes or obstructs the map. | One density-capped Canvas2D surface in `7c9df06`; deterministic checks cover motion totals, five speed bands, moving/stopped weighting, normalized heading, direct legends/axes and quadratic path smoothing. Real-cabin legibility and signal behavior remain open. |
| `R9-05` | NOT RUN | Tap the range pill through `15 MIN → 1 H → SESSION → 15 MIN`, then use the icon-only right-midpoint tab to collapse and reopen the Drive Lab several times while stopped. | Every tap advances exactly one range and all five telemetry views update together. The `36 × 30 px` icon tab is understandable and touchable without text; collapse returns the full map and keeps the tab available; reopen restores the same range and complete no-scroll dashboard. No action locks header/footer focus, loses the route, or degrades a long session. | Shared range and bounded weighted rollups plus corrected hierarchy checkpoint `7c9df06`; exact live range/collapse/reopen QA passes. |
| `R9-06` | CANONICAL PASS `20260901-2232` · TESLA PENDING | Cold-load ATLAS parked with no fix, a coarse fix, then a refining fix/first metres. Repeat with network available. | ATLAS begins from the best truthful session point or shows explicit GPS waiting/recovery; refinement does not restart, lose route or duplicate the marker. | Non-blocking recovery presentation and best-session-position behavior are deterministic; real parked/moving Tesla remains. |
| `R9-07` | CANONICAL PASS `20260901-2232` · TESLA PENDING | Grant GPS, start with a non-ATLAS visual, drive, then open ATLAS. Switch visuals, open/close DISCOVER, and return. | One GPS collector continues; first ATLAS is hydrated; DISCOVER uses the latest reliable point; route/summary/history survive remount; reload starts clean with no coordinate persisted. | App-scoped latest point, bounded route and Drive Lab journey pass remount/privacy tests. Real GPS continuity remains. |

## DISCOVER passenger index — milestone row 10

| ID | Status | Test | PASS condition | Linked work |
|---|---|---|---|---|
| `R10-00A` | CANONICAL PASS `20260902-1905` · TESLA PENDING | From a fresh load, press PLAY THE ROAD and inspect the initial VISUAL choices before START. Select DISCOVER 07 and start with any ready Music choice. | The right Visual grid contains all eight primary choices, including DISCOVER 07 and one GRADIENT 08 family. START opens the Passenger Index; CLOSE returns to Aperture and the normal header/footer can retract. | Shared Visual catalogue and launch destination boundary; exact local/canonical `773 × 601` Browser QA passes; physical flow remains. |
| `R10-00B` | CANONICAL PASS `20260902-1905` · TESLA PENDING | During any running visual, open the footer VISUAL library and press the visible DISCOVER 07 OPEN card. Close Discover and reopen the Visual library. | All eight cards remain reachable without page scroll. Discover opens immediately; closing returns to the same rendered visual, which remains ACTIVE in the library. No false Discover renderer is saved, no blank field appears, and focus/chrome recovery remains normal. | Running Visual library, canonical catalogue and renderer-preservation regression pass. Physical touch flow remains Tesla-owned. |
| `R10-01` | NOT RUN | With real GPS, open DISCOVER from the running header without changing the browser language first. | The left index and always-open reader appear immediately; nearby content uses the browser's first supported language automatically. The screen exposes no coordinates and a reload starts a fresh session. | Localized, session-only `0.05°` discovery boundary in `5c498ac`. |
| `R10-02` | NOT RUN | Compare NEARBY, AHEAD and REGION; use search; then inspect and scroll the complete left rail. | Each filter produces a coherent order; search narrows the existing sources without a network race. The language selector is self-explanatory without a redundant label; the three scope buttons stay comfortable at `38 px`; every result title and enlarged distance/ETA remain readable in one glance. Results fill the available first fold, the inline counter states the exact hidden number, pressing it reveals the next row, and all loaded results up to 15 remain in one scroll. | Measured fold capacity and continuous scroll in `f843ea6`; rail density and `11.5 px` metadata in `f8f554b`. |
| `R10-03` | CANONICAL PASS `20260901-2300` · TESLA PENDING | Select at least three places with differently shaped images/cards. For each, scroll well beyond the introduction; inspect headings, images, information cards and links, then switch the Discover language and use Google Maps once. | The full localized article is the native responsive Wikipedia Minerva page in dark mode, not a product-restyled extract. Its `1.2×` large presentation remains readable and internally scrollable at driving distance; Minerva cards, lead media, chapters and links reflow without overlap or one-character text columns. Language changes update both results and Wikipedia interface. Distance/ETA remain subordinate and Google Maps stays destination-only. | Native `useskin=minerva&minervanightmode=1` reader and outer large-reading scale in `cdccbd7`; exact live `773 × 601` English/Italian QA and canonical byte identity pass. Physical cabin reading remains. |
| `R10-04` | NOT RUN | Change the internal language selector to Italian and one other supported language, then select and scroll one result in each. | Results and complete article move together to the selected edition; no stale or unrelated article appears because page IDs are never reused across editions. The selector remains readable/reversible and source links follow the active language. | Language-scoped article identity and localized MediaWiki endpoint in `f843ea6`. |
| `R10-05` | NOT RUN | Confirm Discover has no ATLAS action, then close it and touch nothing for five seconds. Open ATLAS separately from its own Visual choice and confirm it has no Discover handoff. | Discover and ATLAS remain independent. Neither surface changes to or links to the other; closing Discover returns focus to the unchanged real visual and header/footer retract after about 4.2 seconds. | Removed reciprocal handoff in `f843ea6` plus shared post-action focus recovery in `9daf8f6`. |
| `R10-06` | CANONICAL PASS `20260901-2232` · TESLA PENDING | Select a Discover place, open its navigation QR, scan by phone, share to Tesla, then repeat through Tesla app `Locations → Navigate → Send to Car`. | Discover stays open and shows a scannable destination-only QR plus concise official handoff guidance; no origin/current coordinate/tracking token enters it; the car receives the intended place where supported. | Exact local/live `773 × 601` QR overlay and URL model pass; phone/car receipt remains the acceptance gate. |

## 2026-09-01 evening–09-02 vehicle notes — mandatory insert 10C

| ID | Status | Test | PASS condition | Linked work |
|---|---|---|---|---|
| `R10C-01` | CANONICAL PASS `20260902-2106` · TESLA PENDING | Change Visual, palette, Music route, Soundtrack filters and FX; leave and return through browser history, then reload. Use RESET SAVED STATE from both splash and REPORT. | Safe product preferences restore, both reset actions return defaults, and no coordinate, route, transient audio URL, autoplay state or automatic vehicle envelope enters storage. | Existing versioned preference boundary plus exact local reload restoration and canonical publication pass; real embedded-history behavior remains open. |
| `R10C-02` | CANONICAL UI PASS `20260902-2106` · TESLA NETWORK PENDING | On a cold weak/offline connection, choose Play the Road and Soundtrack in turn, select a Visual and press START. Repeat with Mute. | START is never silently disabled by remote audio readiness. The visual opens immediately, constrained/offline or pending-data copy explains the state, prepared music joins automatically, and Mute follows the same launch path rather than bypassing a lock. | Independent visual launch, prepared-state resume, bounded error retry and canonical pending-data UI pass; target-Tesla network transitions remain open. |
| `R10C-03` | CANONICAL MODEL PASS `20260902-2106` · TESLA PENDING | Start Soundtrack, inspect the Tesla mini-player, then use native previous/play-pause/next during a temporary network loss. | Three transient media roles remain prepared, adjacent covers are prefetched, supported native actions move the committed queue, and title/artist/cover update together without claiming durable offline audio. | Three-deck/media snapshot, artwork prefetch tests and canonical source identity pass; actual Tesla Media Session artwork/control behavior remains the gate. |
| `R10C-04` | CANONICAL PASS `20260902-2127` · TESLA PENDING | Run Soundtrack at `773 × 601`, wait at least five seconds without touching, then accelerate, lift off and brake without touching the display. Finally touch the visual once. | The black readout contains only speed/source—no FLUX, BPM or energy. Previous/play-pause/next retract with the footer; GPS/demo speed changes, acceleration, regeneration, braking and passive pointer movement do not wake any chrome. One deliberate pointer press/touch returns it without changing a value or obscuring the visual. Intentional keyboard focus remains accessible. | Exact base geometry remains `116 px`; checkpoint `92e581b`, protected build `20260902-2127`, byte identity and rendered local/canonical `controls-resting → ArrowUp → controls-resting → visual click → controls-awake` pass with no warning/error. Confirm the same behavior on the physical Tesla. |
| `R10C-05` | CANONICAL PASS `20260902-2106` · TESLA PENDING | Open Discover at `773 × 601` and compare header, rail and result numerals while using Nearby/Ahead/Region. | The heading measures `38 px`, the left rail `246 px`, and the numeral track `16 px`; controls and article remain readable with no overlap or lost result scrolling. | Exact canonical measurements pass; physical touch/readability remains open. |
| `R10C-06` | CANONICAL MODEL PASS `20260902-2106` · PHONE/TESLA PENDING | Scan a selected-place QR and inspect the first Google Maps screen before choosing any route. | Maps opens the place/search surface without `dir_action`, origin or automatic navigation; the passenger can refine, explore, share, or explicitly choose Directions, then optionally send the final place to Tesla. | Canonical URL model and copy tests pass; phone/Tesla handoff remains physical acceptance. |
| `R10C-07` | CANONICAL SOURCE PASS `20260902-2106` · TESLA PENDING | Hold APERTURE near `40 km/h` and inspect all four diagonal seams during motion. | Ceiling, floor and side-wall longitudinal cuts meet at the corners without phase-shifted rows; no geometry or frame-pacing regression appears. | Shared shader origin plus exact local/canonical `773 × 601` visual comparison pass; moving Tesla acceptance remains open. |
| `R10C-08` | CANONICAL PASS `20260902-2106` · TESLA PENDING | Inspect both Visual catalogues and cycle every choice-owning visual. | Drivey states `3 VIEWS · 2 RENDERS`, PRTCL states `3 TYPES`, Gradient states `3 VARIANTS`; single-state visuals show no fabricated count and every in-visual selector agrees. | Registry/presentation tests and canonical launcher QA pass. |

## ShaderGradient visuals, PRTCL surfacing, safety, and response ceiling — milestone row 11

The three selected LAB starting points are variants of one public `GRADIENT 08`
family. The protected workbench remains milestone insert `11A`; its exhaustive
control test complements but does not replace the `R11-*` physical-Tesla
acceptance of Japanese Mist, Acid Orchard, and Chromatic Silk.

| ID | Status | Test | PASS condition | Linked work |
|---|---|---|---|---|
| `R11A-01` | CANONICAL GATE PASS `20260902-1341` · TESLA PENDING | While parked, open the authenticated `https://sedicivalvole.app/lab/`, choose `SHADERGRADIENT / LAB`, try at least one project study, one official preset, each geometry, and the Road speed control. Scroll the inspector through its complete height and return to a PRTCL surface. | The protected gate opens; the clean `/lab/` URL remains; the shader never becomes blank or visibly corrupt; every requested change renders; the stage remains useful beside the inspector; scrolling does not move the whole page or hide the header; returning to PRTCL succeeds without reload. Record any thermal warning, Chromium tab reload, severe stutter, or unusable control size. | Milestone `11A`; exact MIT dependency boundary, protected inline bundle, shared canonical route, and local `773 × 601` layout. |

| ID | Status | Test | PASS condition | Linked work |
|---|---|---|---|---|
| `R11-01` | CANONICAL PASS `20260902-1905` · TESLA PENDING | With Play the Road, select GRADIENT 08 once, then use its persistent VARIANT button to cycle Japanese Mist, Acid Orchard, Chromatic Silk, and back to Japanese Mist. For each, drive progressively through rest, urban, medium, motorway, and the legal-road response ceiling. | The catalogue contains one family entry and needs no preliminary variant picker. The button stays reachable on the running visual, reports the current variant, cycles in the declared order without reload, and restores the last variant after leaving and returning. Every variant moves slowly but visibly at rest, accelerates continuously with road speed, retains its exact colour/grain identity, never blanks, and keeps chrome readable. | Exact local/canonical `773 × 601` WebGL/interaction QA plus zero warning/error pass for the family, remembered start and observed transitions; complete physical motion/cycle remains. |
| `R11-02` | CANONICAL MODEL PASS `20260902-1905` · TESLA PENDING | On each of the three visuals with Play the Road active, compare a quiet passage and a strong authored passage at the same steady speed. Repeat with Soundtrack at the same speed and different recordings. | Play the Road can visibly broaden/brighten with audio while speed still owns the main identity. Soundtrack does not pump or reshape from recording loudness and remains speed-only. | Deterministic shared-response tests and canonical source identity prove the boundary; cabin perception remains. |
| `R11-03` | CANONICAL MODEL PASS `20260902-1905` · TESLA PENDING | Display a valid speed above `130 km/h` only where legal/safe evidence permits; otherwise use QA while parked. Compare all three fields and energy at 130 and above. | The tachometer reports the truthful higher value, while energy remains `100%` and every visual, score tempo, motion, density, and effect shows no new post-130 behavior. Do not exceed legal road speed to test this. | Model QA proves identical clamped profiles at and above 130; canonical assets are byte-identical. |
| `R11-04` | CANONICAL CODE PASS `20260902-0103` · TESLA PENDING | On each PRTCL family, establish its natural size at a steady speed, brake into UNDERWATER, then resume speed and watch surfacing. | Braking shrinks the complete form dramatically to roughly one fifth, not merely its points or glow. Release returns quickly and smoothly to the exact natural size owned by that speed, reinforcing audio surfacing without a pop. | Deterministic collapse/release tests and exact local natural/UNDERWATER screenshots pass; canonical assets are byte-identical. |
| `R11-05` | CANONICAL PASS `20260902-0103` · TESLA PENDING | At `773 × 601`, inspect the splash disclaimer, use every edge swatch in the widened palette, and operate only as passenger while moving. | Safety copy clearly says configure while parked, leave moving interaction to a passenger, and that response reaches 100% at 130. The `226 px` palette remains bounded, readable and touchable. | Exact local measurement, live splash Browser QA and canonical identity pass. |
| `R11-06` | CANONICAL READY `20260902-1905` · TESLA PENDING | Run the three Gradient 08 variants for at least 20 minutes total while music and FX are active; repeatedly cycle VARIANT, leave/return to Gradient, switch among other visuals, and include background/foreground cycles. | No sustained frame collapse, thermal blanking, WebGL context leak, audio degradation, stale variant, duplicate canvas, or grain-induced shimmer. Context recovery and reduced motion remain honest. | Automated lifecycle/build checks and short local/canonical switching pass; sustained target-Tesla evidence is required. |

## Shared appearance and enlarged Tesla palette — milestone row 13

These planned codes confirm that `LIGHT / DARK / AUTO` remains accepted work;
they are not runnable until row 13 has a canonical build.

| ID | Status | Test | PASS condition | Linked work |
|---|---|---|---|---|
| `R13-00` | LOCAL RETEST PASS · CANONICAL/TESLA PENDING | At exact Tesla `773 × 601`, reveal the footer palette, select every row/edge swatch and compare its physical size. | The palette measures `226 px` wide with larger swatches; every colour stays reachable; footer/visual/attribution retain bounds and the separate appearance trigger remains future work. | Exact local viewport measurement passes; canonical and physical Tesla touch remain. |
| `R13-01` | PLANNED · NOT IMPLEMENTED | Use the direct top-rail appearance trigger to select LIGHT, DARK, AUTO, LIGHT; reload after each manual choice. | The control is always understandable and reachable without confusing appearance with the Flux palette. LIGHT and DARK change shared UI surfaces only; manual choice persists; visual colours and selected palette do not change. | Promoted `FI-005` / X10 preference and Road Sheet token application. |
| `R13-02` | PLANNED · NOT IMPLEMENTED | Select AUTO, change the browser/system colour scheme where supported, then exercise the documented offline/no-position fallback and twilight boundary. | AUTO follows a supported browser appearance signal first; otherwise it uses the bounded solar fallback without storing coordinates, flickering near twilight, or switching during an active gesture. Its source/state remains truthful. | Prepared appearance resolver and capability adapter. |
| `R13-03` | PLANNED · NOT IMPLEMENTED | Combine every appearance mode with representative light/dark Flux palettes, all drawers, DISCOVER, ATLAS, diagnostics, offline/degraded network states, and reload/storage failure. | Text, controls, focus, status and attribution remain legible; no renderer is recoloured; quiet healthy network treatment stays subordinate and degraded/offline/recovered states remain explicit. | Row 13 complete appearance/network matrix. |

## Evening closeout

After the run, transfer every result into the matching milestone row. A browser
or automated PASS cannot replace the Tesla result for rows marked `TESLA`.
Open a new diagnostic only for a failure that needs timing, GPS, network,
memory, or frame evidence; do not collect coordinates.
