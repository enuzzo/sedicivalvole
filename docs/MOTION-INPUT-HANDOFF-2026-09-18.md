# Motion input continuation — September 18, 2026

## Selected XYZ phone companion — September 18

The owner subsequently authorized QR integration, immediate relative-pose TARE,
live XYZ/gyro readings, diagnostic integration and selected **direction 1, XYZ
Cross**. The [companion implementation and physical test guide](PHONE-MOTION-COMPANION-2026-09-18.md)
supersedes the local-only/pending-UI statements below for this scope. Canonical
**20260918-2335.9b18086** is published and verified; implementation, local tests
and delivery evidence are recorded in that document. No upload remains. Actual iPhone/Tesla acceptance remains open. GPS/audio are
unchanged. Aperture tilt/curvature is queued after physical evidence. R06 remains
incomplete; Conditions stays deferred and dynamic Aerial retired.

## September 18 continuation result — read before the original brief

The requested isolated N03 sensor layer and N04 synthetic direct-link probe are
implemented and locally verified. [Current evidence, execution and physical
protocol](MOTION-FEASIBILITY-2026-09-18.md) supersede the original implementation
starting point below. Neither probe enters the production bundle or controls
product speed/audio. No deploy is required. Next work needs trusted HTTPS on the
real devices and measured iPhone/Tesla evidence; do not recreate this foundation
or launch idle follow-up tasks. R06 still has missing original artifacts.

## Owner authority and execution boundary

The owner explicitly requested autonomous continuation of the remaining work,
with each context closed, documented, committed, pushed and appropriately
published before starting a fresh task in the saved local project. This
supersedes earlier handoff-only/no-task-launch instructions for this continuation.
Use one writer in the existing Dropbox checkout. A fresh task is sequential,
not a parallel implementation worker or a worktree.

Conditions **N02 / FI-006 remains deferred** by the owner's latest explicit
instruction. Dynamic Aerial **FI-002 / D4 remains retired**. Do not reopen either.
Follow the current contracts, pinned-source integrity, credential boundaries,
physical-acceptance distinctions and new-product-design selection gate. Broad
continuation does not select a new UI composition, change external access, buy
services or authorize test mail to real recipients.

## Closed product and starting evidence

- Starting repository checkpoint: `22b3049`, clean and synchronized `main` before
  this handoff-only documentation. Recheck the actual HEAD on entry.
- Latest product: `12f338d`; build source `721274c`; canonical release
  **20260918-2212.721274c**, VERSION **0.0.0**.
- N01 Drivey is complete: familiar fixed forward camera, one Normal/Wire control,
  shared palettes, old camera choices discarded while render preference remains.
- Previous Soundtrack readiness and catalogue/recovery corrections are also live.
- Latest product checks: **891/891** complete native tests, **20/20** focused
  Drivey/session tests, **51** unchanged upstream hashes, **827** package hashes,
  **189/189** credits and **1,466** tracked-text hygiene checks before this file.
- Preserve-existing FTP completed: **37 files / 6,311,497 bytes**, 825 static files
  and 29 fully verified recordings reused, one previous asset retained,
  `remote_writes=ROOT_UPLOAD_ONLY`.
- Canonical bare/cache-busted HTML, manifest and ten assets match local bytes;
  internal-browser Normal/Wire toggle and controlled reload pass without
  console warnings/errors. Local QA also covers palette and render persistence
  at 773 × 601 and 844 × 390. No physical acceptance is inferred.
- All agent-created local QA servers and browser tabs were closed. Do not kill
  unrelated processes or assume a particular free port.

[Health evidence](HEALTH-CHECK-2026-09-18.md#drivey-forward-only-continuation--september-18)
and [owner ledger](OWNER-DIRECTION-2026-09-18.md) are authoritative. Historical
unchecked rows are not a new request to rebuild already shipped features.

## First fresh-context objective — N03 sensor feasibility

Make a bounded, reproducible measurement capability before designing the public
motion feature. Use existing project conventions and inspect the current LAB/QA
packaging before choosing where an owner-operated probe belongs.

1. Recheck clean Git/remote state; read root/local AGENTS plus relevant motion,
   diagnostics/privacy, interface and delivery contracts. Research current W3C,
   WebKit or official browser documentation for APIs actually used.
2. Define a normalized sample boundary for acceleration and rotation rate,
   monotonic receipt timing, missing axes, screen-orientation changes and stale
   input. Keep GPS as the speed reference; never integrate an unqualified
   accelerometer into invented km/h or change the 130 km/h product boundary.
3. Implement and test the isolated measurement/controller layer if inspection
   supports it: explicit permission request from a user gesture, truthful
   unavailable/denied/no-sample states, bounded in-memory observations, cadence
   and freshness summaries, clean stop/teardown and foreground/rotation handling.
   Do not silently attach sensor collection to ordinary product launch.
4. A development-only probe may reuse the existing technical QA grammar and
   explicit synthetic labelling. Keep synthetic unit/browser results separate
   from real hardware measurements. No raw sample persistence or automatic
   diagnostic transmission. Avoid new production UI before the required choice.
5. Produce a short real-device protocol: stationary phone, secure mounting,
   orientation/remount, permission rejection, foreground/background/screen lock,
   ordinary motion and comparison with GPS. State exact readings still needed
   from iPhone Safari. Do not claim access to the owner's phone or vehicle.
6. If a new public permission/calibration/source-selection interface is ready
   for discussion, present exactly three concrete directions and obtain the
   selection while continuing independent non-visual work where possible.

Research already established: DeviceMotion distinguishes acceleration and
rotation; the gyroscope alone does not measure longitudinal acceleration.
HTTPS/permission requirements and target-device behavior still need validation
in the selected execution path. No app sensor module currently exists; verify
that state rather than treating this handoff as a frozen architecture inventory.

## Next unit — N04 phone-to-Tesla feasibility

After establishing N03, investigate a bounded connection probe and normalized
fresh-sample protocol. WebRTC data channels can carry messages directly, with
signaling to establish the connection and possible TURN relay when required.
Do not assume Tesla RTCDataChannel support, existing-host signaling/WebSocket/
TURN availability or a latency improvement. Measure compatibility, sample age,
jitter, disconnect/reconnect and stale/out-of-order rejection before integration.
Pairing should be deliberate and revocable, with expiring sessions and visible
connection/source state; GPS remains primary if supplemental motion is lost.
No public relay, account system, paid service, credentials or external settings
are provisioned merely because this architecture is under consideration.

Primary starting references (recheck relevant current details):

- [W3C Device Orientation and Motion](https://www.w3.org/TR/orientation-event/)
- [WebRTC peer connections](https://webrtc.org/getting-started/peer-connections)
- [WebRTC data channels](https://webrtc.org/getting-started/data-channels)
- [Motion permission semantics](https://developer.mozilla.org/en-US/docs/Web/API/DeviceMotionEvent/requestPermission_static)

## Remaining queue and stops

- R06: historical scan is in discovery and its registered temporary artifacts
  are missing. Capability preflight was ready, but refreshing preflight checks
  was rejected outside the preflight phase. Recover original evidence/state;
  do not duplicate, reset or falsely finish the scan. A replacement scan needs
  a deliberate explicit decision if recovery is impossible.
- Physical S04/P05: Tesla native transport, continuity/weak network, sustained
  visible Air Atlas/Fly With and GPU/thermal evidence; iPhone Safari rotation,
  safe areas, native audio/PDF; remaining Mono/Rosso/Touring cases. Prepare
  concrete protocols and collect supplied evidence; do not simulate a pass.
- The Engine source-bank research handoff is preserved. Do not reintroduce
  rejected voices or the rejected FMOD timbre. Additional sourcing requires
  the existing rights/admission and listening evidence.
- Broader curated authorship, flight schedules, persistent diagnostic outbox
  and passenger remote controls remain deferred; this continuation prioritizes
  N03/N04 and does not silently select every historical optional idea.
- Production SemVer release remains an explicit owner decision. Do not bump
  VERSION merely because software checks pass.

## Verification, delivery and next handoff

Run package commands from `prototype/drive-lab`. For credential-free QA/build:
`SEDICIVALVOLE_NO_LOCAL_ENV=1`. Native full gates need Python 3.11 first in PATH:
`PATH=/usr/local/opt/python@3.11/libexec/bin:$PATH npm run test:native` (combine
with the no-local-env setting). Use `npm run build:native` for the release.
Current native/lockfile state was established during this parent session;
recheck drift/host rather than trusting Dropbox-native packages blindly.

Use only the Codex internal browser for UI QA; no external Chrome/CDP fallback
is authorized. Disable automatic diagnostic sending in QA. Never inspect .env
or secret variants. The official deploy script alone may load its configuration
internally. Publish agreed production changes with
`python3.11 scripts/deploy_drive_lab_ftp.py --publish --preserve-existing`, then
verify bare/cache-busted HTTPS, exact build/assets and actual browser behavior.
Do not deploy failed, interrupted or unfinished work. Documentation-only or
excluded local-probe work does not require a redundant production upload;
record the exact non-deploy reason instead.

Before this context becomes crowded, finish a coherent tested unit, update the
relevant contracts, evidence and SESSION-HANDOFF, commit/push, publish applicable
product changes, and leave a clean checkout. Only then create one fresh task in
this same saved local project with the next bounded objective and the updated
handoff. Do not copy the whole chat as the new prompt. Transfer sole write
ownership to the new task and cease writing in the parent. If the only remaining
work needs an owner design choice, hardware evidence or external authority,
report that concrete dependency instead of generating an endless chain of idle
tasks.
