# Changelog

All notable changes to `sedicivalvole` are documented here. The format follows Keep a Changelog and the project uses Semantic Versioning. `VERSION` is the only SemVer source of truth.

## Unreleased

### Added

- 2026-08-30 15:47 `[7e75a86]`: Explored the live FeralUI Gradient Builder and ColorFlow editors and checked their official usage boundaries as companion references for the proposed speed-responsive Shader Field. Feral contributes family-based authoring, named palette roles, perceptual colour profiles, contrast feedback and separated motion/geometry/finish controls, but its unpublished Gradient Builder demo is not one of the site's MIT packages and is not licensed for copying or redistribution. ColorFlow contributes a WebGL control mesh, curved handles, perceptual interpolation, procedural and interactive envelopes and optional effects; its generated work may be used freely, including commercially, but its editor, runtime, presets, embed and effects are not open-source inputs. The preferred first spike is now a project-authored one-pass planar direct-WebGL2 mesh with a small perceptually precomputed palette, while displaced depth and restrained aurora/flow remain the other two mandatory visual directions. No third-party source, runtime, embed, preset, export, shader, asset, product code or deployment changed.
- 2026-08-30 15:36 `[7f59b60]`: Navigated the live ShaderGradient customizer and audited its official MIT-licensed source to record how its moving gradients are built: coherent-noise mesh deformation in the vertex shader, three-colour spatial mixing through a physical material, environmental lighting/reflection, camera/object framing, and an optional halftone grain post-pass. Added a proposed speed-responsive `FIELD / ABSTRACT` visual to the planning ledger, driven only by the shared bounded T1 state and audio macro envelopes. The preferred first spike is project-authored direct WebGL2 at pixel density `1` and one render pass, with no admitted upstream package, shader, preset, asset, or dependency; a stable owner ID, exactly three visual directions, source admission and measured Tesla comparison remain mandatory before implementation. No product code or deployment changed.
- 2026-08-30 15:26 `[0dd8cee]`: Grounded the proposed Light appearance in the current official Tesla Model 3 touchscreen illustration instead of a generic Tesla-like grey. Broad flat-region sampling records provisional image-derived targets of approximately `#F4F4F6` for the primary vehicle/control surface, `#DDDEE2` for the map/secondary canvas, and `#000000` for the persistent dock. They are not presented as unpublished Tesla design tokens: the official manual says appearance varies by software, market and configuration, so exact acceptance requires a native current-vehicle screenshot and pixel comparison. AUTO now tests first whether Tesla's native `Dark` / `Light` / `Auto` setting reaches the browser through `prefers-color-scheme`, retaining session-only solar context only as a no-storage fallback.
- 2026-08-30 15:18 `[21d2418]`: Reframed the growing seven-row Visual picker as a grouped direct-button library awaiting the exactly-three-direction gate: `ROAD / PLACE` contains VERTIGO, MERIDIAN, ATLAS and DRIVEY, while `FIELD / ABSTRACT` contains APERTURE, PRTCL and PRIMORDIAL, with no nested category navigation or change to stable renderer identity. Added a separate `LIGHT` / `DARK` / location-aware `AUTO` interface-appearance proposal that never recolours the active visual palette, stores no coordinates, falls back to the browser preference and avoids twilight flicker. Restored weather to the queue as deferred `CONDITIONS`: an exception-led DISCOVER/ATLAS companion for meaningful precipitation, gust, visibility or temperature changes plus an optional 6/12-hour and tomorrow detail, rather than duplicating Tesla's destination forecast and optional precipitation overlay. No product code or deployment changed.
- 2026-08-30 15:13 `[8839538]`: Verified the prepared Jamendo read access without exposing credentials: the API returned success, three distinct tracks and artists, complete catalogue/credit fields, and a minimal stream-range request returned `206 audio/mpeg`. Recorded DISCOVER as a proposed passenger companion outside permanent ATLAS chrome: Nearby, Ahead, and Region views reuse session-only location/heading, rank diverse Wikipedia/Wikimedia cards, retain source/image attribution, provide explicit Google Maps search and QR handoffs, and keep exact coordinates out of persistence and diagnostics. The new surface awaits an owner-assigned stable ID and the mandatory exactly-three-direction visual gate; no product code or deployment changed.
- 2026-08-30 15:05 `[5caa9f5]`: Refined the planned Jamendo integration to the minimum read-only scope: client ID only, no OAuth secret or redirect unless a future approved feature requires them, and a transparent application registration that discloses the optional project-support link. SOUNDTRACK now uses three transient browser-media slots—previous, current and next—with stream URLs and browser-native `preload=auto`; it never creates or persists complete audio blobs, download files, Cache Storage, IndexedDB, or service-worker audio entries. Buffer exhaustion pauses/retries SOUNDTRACK without the explicitly rejected automatic PLAY THE ROAD fallback. The rich artist/track/cover/licence/link/QR card is closed by default behind a compact navbar or corner control to avoid driving clutter.
- 2026-08-30 15:02 `[d3e8a30]`: Converted the proposed three-track Jamendo offline cache into a terms-compliant rotating metadata queue. Current public API terms prohibit applications designed for caching or offline content access, so the plan retains no complete Jamendo audio blob, download URL, or Cache Storage entry: only normal current/next media buffering is allowed, with a visible proposed PLAY THE ROAD handoff if the buffer empties during network loss. Consumed slots refill with fresh eligible tracks and bounded recent track/artist memory prevents immediate repetition. A first-class credit control now opens a source-correct cover, artist, track, optional album, licence, textual provider credit, direct content link, and matching QR, fulfilling the product goal of returning attention to independent artists as well as Jamendo's attribution requirements.
- 2026-08-30 14:59 `[bc790da]`: Added truthful session network observability to the X2 navbar revamp and M13 diagnostics plan. The navbar will stay quiet when healthy and expose compact active-transfer, degraded, offline, and recovered states; DIAG will separate observed app download/upload payloads, rolling and peak rates, active transfers, failures, and recovery from limited browser downlink/RTT estimates. Acceptance explicitly excludes unrelated Tesla traffic and refuses to invent cache, opaque cross-origin, request-header, TLS, or otherwise unavailable bytes.
- 2026-08-30 14:57 `[ffcba51]`: Removed the retired metered generative source from every active planning reference while preserving the immutable historical changelog. M5/M5b/M5c remain as source-neutral closed traceability IDs. Prepared a local-only Jamendo credential form with empty client ID, client secret, redirect URI, application name, and plan fields; the existing `.env.*` rule keeps it ignored, and verification reports it only as an ignored file rather than repository content.
- 2026-08-30 14:54 `[1e3430d]`: Closed Magnific M5/M5b/M5c as explicitly deferred after current public pricing, API, and AI-product terms confirmed shared-plan credit consumption and model-specific music restrictions. The plan now forbids Magnific login, private-file audition, API integration, credit spend, and recurring evidence reminders; it also prohibits every metered generative API from running per listener or playback event. Jamendo is the owner-selected first SOUNDTRACK path, beginning with a read-only operational and rights audit while its prepared credential remains outside Git, browser code, logs, screenshots, and documentation.
- 2026-08-30 14:40 `[1a3bca5]`: Converted two manually submitted, coordinate-free real-Tesla diagnostic reports into explicit planning evidence without versioning their private `_references/` payloads. The field baseline confirms exact `773 × 601` operation, approximately 10 Hz GPS, zero recorded runtime issues, near-60-FPS non-ATLAS render phases, bounded JUNCTION decoded memory and live take/rhythm variety. Phase 0 now separately tracks ATLAS's repeated approximately 23-FPS result, one unattributed 12.741-second main-thread stall, cold-cache adaptive-bank readiness and fallback reasons, significant-event retention beyond GPS-ring saturation, and truthful output-latency availability.
- 2026-08-30 14:34 `[6326b52]` `build 20260830-1427`: Published the active identity correction to the canonical root. The exact source names `enuzzo` as the sole original creator and public licensor, presents `A project by enuzzo` in the Signal Gate, and contains no active studio attribution while preserving immutable historical changelog evidence and all third-party credits. Twenty focused identity/documentation checks and all 351 checks available on this Intel Mac pass; the sole unavailable fixture remains the known local PHP executable requirement. The 132-module production build, read-only preflight, 90-file / 15,774,999-byte upload, exact-cleanup gate and no-write postflight pass. Bare and cache-busted canonical HTML plus the live JavaScript bundle are byte-identical to the verified local build; exact `773 × 601` Browser QA confirms the `github.com/enuzzo` link, successful launch into DIAG, and zero warning/error. This identity correction does not alter the repository's `AGPL-3.0-or-later` commercial-use permissions.
- 2026-08-30 14:26 `[d90fd33]`: Corrected the active project identity everywhere from the retired studio attribution to sole original creator and public licensor `enuzzo`. The Signal Gate now links `A project by enuzzo` to `github.com/enuzzo`; package metadata, notices, licence scope, current specifications, source-admission records and tests agree while preserving third-party credits. This identity correction does not alter the repository's `AGPL-3.0-or-later` commercial-use permissions. Historical changelog entries remain immutable evidence of the copy displayed by earlier builds.
- 2026-08-30 00:43 `[44a3a42]` `build 20260830-0038`: Published the accepted PRIMORDIAL field and unmistakable OPEN intake to the canonical root. The exact pushed source passes all 351 tests and the 132-module build. Read-only preflight and postflight both report network/login/directory/listing/identity PASS with `remote_writes=NONE`; publication uploaded 90 files / 15,775,006 bytes, passed the dynamic-root and exact-cleanup gates, and retained two prior content-addressed assets for cache overlap. Bare and cache-busted HTML return `200`, explicit no-store/no-cache and proxy `MISS`, and match the local entry byte-for-byte at SHA-256 `0a95a3623264faae4c70bed624a617c1ab7ed5e0e15932353ecbd4087ccd1d8e`; the live main bundle, PRIMORDIAL chunk and CSS also match exactly. Muted live Browser QA at `773 × 601` selected PRIMORDIAL 08, opened its Scale/Flow/Warp tuner, changed Palette to MINT 07, found zero warning/error, and DIAG reported WebGL2 Primordial fluid field at 59.63 FPS / 18.2 ms p95, zero runtime issues and identity `v0.0.0 · 44a3a42 · build 20260830-0038`. Real-Tesla visual, touch, thermal and listening acceptance remain open.
- 2026-08-30 00:08 `[9b733f6]` `build 20260830-0008`: Added the local `PRIMORDIAL 08` clean-room review candidate without copying the referenced CodePen's HTML, CSS, JavaScript, shader, Three.js runtime, or separately attributed noise fragment. One project-authored WebGL2 fluid field with a bounded Canvas2D failure path maps road speed to convergence, music to flow/colour/agitation, all ten Sedici Valvole palettes to five native colour channels, and OPEN / UNDERWATER / BLOOM to fluid-field responses. A compact `94 × 44 px` `TUNE / FIELD` disclosure exposes only persisted Scale, Flow, and Warp ranges, contains no select/dropdown, closes with Escape, and keeps the product controls awake. Exact muted `773 × 601`, `390 × 844`, and `601 × 390` Browser QA passes with deterministic reduced motion, live pointer deformation, distinct effect identities, 59.62 FPS / 18.3 ms p95, zero runtime issue, and zero warning/error. All 347 tests and the 132-module production build pass. This checkpoint is local only; fresh human visual approval, push, canonical deployment, and real-Tesla acceptance remain open.
- 2026-08-29 23:46 `[b88070c]` `build 20260829-2337`: Published the human-approved PRTCL checkpoint together with the recovered DRIVEY automatic road following, dual-channel palettes, WAKE retirement and ATLAS desktop camera controls. The final clean source passes 339 tests and a 128-module build; read-only preflight, the 89-file / 15,756,155-byte upload and repeated post-publication identity gate pass with `remote_writes=NONE`. Bare and cache-busted HTML plus the main bundle and key assets are byte-identical to the verified build. A first otherwise-correct upload exposed a stale uncached Drivey shell response, so Drivey's project-owned iframe now carries the build stamp; the corrective publication verifies the exact shell. Muted live Browser QA at `773 × 601` cycles all three PRTCL families independently of Palette, finds zero `select` elements, measures TYPE at `94 × 34 px`, verifies the six-item catalog without WAKE, and reports zero warning/error. Drivey cycles Normal/Wire from its build-stamped iframe; DIAG reports WebGL Original Drivey Wireframe at 59.42 FPS / 17.6 ms p95 with zero runtime issue. Real-Tesla acceptance remains open.
- 2026-08-29 22:57 `[fe2a9a5]` `build 20260829-2257`: Made the ATLAS manual camera testable from a desktop without changing its touch contract. The previously transparent `.experience` surface was intercepting every map hit above the MapLibre canvas; ATLAS now keeps that chrome pointer-transparent while its actual controls remain interactive and correctly stacked above the passenger panel. Primary-button drag rotates with horizontal travel and tilts with vertical travel, while a bounded custom wheel/trackpad path zooms from `3–20.5`; both gestures share the existing six-second manual lease and fresh eased return. The in-field hint now names `DRAG: ROTATE / TILT` and `WHEEL / PINCH: ZOOM`. Browser QA at `1280 × 720` and exact `773 × 601` changed heading/pitch from `022° / 60°` to `080–082° / 35–38°`, changed zoom in both directions, retained the manual zoom at `4.5 s`, returned to `16.2` after the lease, closed the GPS popup above the passenger panel, produced no overflow and logged no warning or error. Twenty-two focused ATLAS checks, the complete 343-test suite and the exact 131-module production build pass. This checkpoint is local only; push, deployment and real-Tesla touch/trackpad acceptance remain open behind the pending PRTCL visual-approval boundary.
- 2026-08-29 22:22 `[9f177fa]` `build 20260829-2222`: Added the locally verified `PRTCL 07` environment as a bounded project-authored WebGL2 renderer around directly authorized adaptations of the Fractal Frequency, Murmuration, and Axiom formulas pinned at PRTCL `2a22f33b`. One `94 × 34 px` text-only `TYPE` button cycles `FRACTAL → MURMURATION → AXIOM → FRACTAL` with no dropdown or panel, while the existing ten Sedici Valvole palettes remain independent. Road speed controls point scale, depth, and travel; music controls palette motion and pulse; OPEN, UNDERWATER, BLOOM, and reduced motion use native bounded responses. Exact `773 × 601` and `390 × 844` Browser QA measured the control, proved zero `select` elements, captured three current families and three distinct macro states, reproduced byte-identical reduced-motion frames, sustained `59.2–60.0 FPS` with no interval above `34 ms`, and found zero warning or error. Eight focused checks, the complete 343-test suite, and the exact 131-module production build pass. Human visual approval, push, deployment, and real-Tesla acceptance remain open.
- 2026-08-29 21:13 `[ba215be]` `build 20260829-2110`: Published the accepted source-faithful DRIVEY recovery to the canonical root from the exact clean commit on `origin/main`. A pre-final upload exposed the earlier verified `2b9e724` bundle identity and was deliberately not accepted; the app was rebuilt from `ba215be`, passed another no-write preflight, and was republished as 89 files / 15,746,290 bytes. Bare and cache-busted HTML are byte-identical, return `200`, explicit no-store/no-cache headers and cache `MISS`; the live main bundle, CSS, both worklets, Drivey shell, manifest, runtime and three.js module are byte-identical to the local build. Muted live QA at `773 × 601` measured both controls at `94 × 34 px`, completed every View and Render cycle with no panel/select, reported WebGL Original Drivey at 59.41 FPS / 17.5 ms p95 with zero runtime issue, warning or error, and exposed `v0.0.0 · ba215be · build 20260829-2110`. Real-Tesla acceptance remains open.
- 2026-08-29 20:55 `[2b9e724]`: Replaced the rejected clean-room DRIVEY implementations with the actual Rezmason Drivey runtime pinned at `5104cdade2a3158786b05b9b0680a50e942830cf`. Fifty-one upstream files are protected byte-for-byte by a SHA-256 manifest, while the project-owned iframe shell and bridge map road speed, music, OPEN / UNDERWATER / BLOOM and all ten Sedici Valvole palettes onto the original runtime. The native Driver, Chase and Satellite choices and original colour controls are absent from the product surface: two compact `94 × 34 px` text-only buttons now cycle `HOOD → REAR → AERIAL` and `NORMAL ↔ WIRE` directly, with no dropdown or panel. Nine DRIVEY checks, the complete 335-test suite, the 128-module production build, exact muted `773 × 601` and `390 × 844` Browser QA, and the read-only canonical deployment preflight pass; real-Tesla touch, motion and thermal acceptance remain open.
- 2026-08-29 18:02 `[77c0914]`: Defined the implementation contract for `DRIVEY 06`, `PRTCL 07`, `INFINITE 08`, and `PRIMORDIAL 09` before touching runtime code. PRTCL and InfiniteTubes remain one catalog environment each with contextual variants; the compact `TUNE` control occupies the measured upper-left gap below the `68 px` Tesla header and above the `64 px` low control plane, disappears for existing visuals, and moves above the `224 px` portrait footer. Road speed, musical features, and OPEN / UNDERWATER / BLOOM keep separate native mappings, while bounded renderers, reduced motion, cleanup, accessibility, source comparison, and real-Tesla acceptance gates are explicit.
- 2026-08-29 17:58 `[45f7f33]`: Completed the pre-implementation source and licence gate for Drivey `5104cdad`, local PRTCL `2a22f33b`, InfiniteTubes `a3b831b6`, and CodePen `NXGbBo`. Exact rendered-source audits selected PRTCL Fractal Frequency, Murmuration, and Axiom while rejecting Electromagnetic Field's already-rejected starburst language. The resulting integration boundary admits only original AGPL project code: no upstream runtime, shader text, texture, model, font, screenshot, or other asset enters the product; the unidentified InfiniteTubes Star Wars texture and separately attributed Primordial Soup noise fragment are explicitly excluded. `THIRD_PARTY_NOTICES.md`, `NOTICE`, `LICENSE-SCOPE.md`, and the licensing decision log now preserve the source credits and evidence before implementation begins.
- 2026-08-29 17:35 `[ab42b2f]`: Kept the explicit Milan demo uninterrupted after a denied-location retry. App-level demo ownership now suppresses automatic GPS-help reopening until a real trusted position arrives, while the GPS control can still reopen the same non-modal recovery panel on demand. Browser QA confirmed the demo stays active with the popup absent; the targeted GPS presentation and ATLAS model checks pass.
- 2026-08-29 17:31 `[45ab8d9]`: Rebuilt ATLAS interaction and GPS recovery from a fresh exact-viewport Product Design audit. One pointer rotates and pitches, two pointers pinch from zoom `3–20.5`, and a six-second manual lease eases back to the latest live position, bearing, pitch and automatic zoom rather than a stale snapshot. A MapLibre compass and numeric heading occupy the upper left; the latest ephemeral in-memory fixes form a direction-ordered, speed-scaled `line-progress` road pulse that never enters diagnostics. The blocking no-GPS splash is gone: top navigation shows status and metre accuracy, while a non-modal accessible popup offers retry, privacy context and the fixed Milan demo. Full-width Wikipedia imagery, a larger description, five Tesla-viewport choices, scroll-safe QR and persistent collapse handle improve passenger reading. All ten palettes now apply despite continuous pulse repaint and enforce map-specific contrast floors. The complete 301-unit/6-WAKE/9-packaging suites and production build pass; muted browser QA covers denied retry, demo, collapse/reopen, ten palettes and `773 × 601` / `601 × 390` with no new warning or error. Real-Tesla multitouch, live GPS recovery, map matching and cabin readability remain open.
- 2026-08-29 17:28 `[7c3d196]`: Superseded JUNCTION PARK's first breathing automation after an 110-second real-Chromium stop exposed a few milliseconds of overlapping `setValueCurveAtTime()` duration that the fake `AudioParam` incorrectly allowed. Five-point breathing now uses cancellable linear ramps and starts each unequal hold from the real transition time, so scheduler drift cannot compress two breaths together. A fresh 86-second muted browser run reached seven voicing changes with beat and bass still false and zero warnings or errors; the 65 focused JUNCTION/ATLAS checks pass. Musical listening acceptance remains open.
- 2026-08-29 17:13 `[8f27c35]`: Replaced JUNCTION's literal single-voicing PARK branch with a clockless six-voicing form drawn from its existing `Emin9 – Cmaj7 – Amin7 – Bmin9` grammar. Unequal `9.8–15.4 s` holds, `3.6 s` crossfades, breathing expression and per-voicing filter colour provide slow harmonic and timbral motion without beat, bass, source-bank decoding or an exposed pulse; PARK position survives repeated stops instead of restarting from one chord. Consonance, register, reachability, non-repetition and no-beat/no-bass checks pass, while the deliberately conservative unfiltered reference measures `-54.290 dBFS` RMS and `-44.313 dBFS` sample peak. The complete 295-unit/9-packaging suite, production build and a muted exact-viewport runtime progression from `Cmaj7` to `Bmin9/D` pass with zero browser warnings or errors; low-volume listening and a long real-Tesla stop remain open.
- 2026-08-29 17:05 `[0e563f2]`: Reopened WAKE after live review found that its deformation still read as draping in place rather than road travel. A separate integrated longitudinal phase now advects width, camber, relief, twist and centerline compression down every ribbon, with matching restrained surface cues and an animated Canvas2D fallback. PARK completes one extremely slow pass in roughly 83 seconds, while the `130 km/h` ceiling approaches one pass in two seconds; reduced motion freezes the conveyor phase. Exact muted `773 × 601` sequences at equal 900 ms intervals measure field change of `0.0620132` RMSE at `20 km/h` versus `0.205038` at `130 km/h`. Six model checks, the production build and browser console gate pass; real-Tesla visual acceptance remains open.
- 2026-08-29 16:59 `[f9a12aa]`: Rebuilt DIAG as a readable operational instrument at the Tesla `773 × 601` viewport. A concise frame/GPS/audio/issues strip leads four aligned evidence sections in locally packaged IBM Plex Mono; Orbitron remains product identity. Actions now stay in normal flow, the inert JUNCTION audition row is absent, and complete raw JSON uses one 12 px scroll context. A dedicated accessible README carries telemetry/privacy boundaries, audio provenance and ownership status, licensing, source links and build identity while essential coordinate-free and explicit-send disclosure remains beside SEND. Side-by-side Product Design review cleared all identified P0/P1/P2 differences; keyboard focus, Escape, console, complete 293-unit/5-WAKE/9-packaging suites and production build pass. Real-Tesla touch, cabin-distance reading, font rendering, screen reader and 200% zoom acceptance remain open.
- 2026-08-29 16:46 `[ba9dd55]`: Recomposed FRACTURE's exposed low-speed pulse as three authored half-time rhythm families instead of one repeated tick. `SILK PULSE`, `BROKEN PULSE`, and `RHYTHM WEAVE` rotate four distinct two-bar cells over an eight-bar form with accents, rests, ghost articulations, and four compatible synthesized timbres. Bar-boundary progression prevents a GPS leap from skipping families; the native full break cannot arm before `88 km/h`, remains stable down to `82 km/h`, and diagnostics report the tactus actually articulated. A complete silent ascent/descent fixture proves `30 km/h` at `85–87 BPM`, WEAVE through `87 km/h`, full-time only at `90 km/h`, hysteretic release at `81 km/h`, `-16.775 LUFS`, and `-1.319 dBTP`. Seven new rhythm checks join 283 existing unit checks, five WAKE checks, nine packaging checks, and a passing production build; low-volume listening and real-Tesla acceptance remain open.
- 2026-08-29 16:34 `[5b3d224]`: Added the user-approved `WAKE 05` Flux environment as a project-authored WebGL2 renderer with a Canvas2D fallback and no imported runtime or asset. Seven broad tapered material sheets preserve the selected `773 × 601` reference composition while theme colour, cross-section depth, restrained grain and fold lighting keep the complete Sedici Valvole control plane intact. OPEN, UNDERWATER and BLOOM alter WAKE through its own width, pressure, fold and light grammar.
- 2026-08-29 16:34 `[5b3d224]`: Implemented WAKE's two live-review motion corrections in geometry rather than light alone. At low speed the surfaces are slow velvet drapes; increasing road energy progressively transports compression, width, folds and twist along their splines, then permits faster crossings and temporary knots. Three-frame browser sequences at `20`, `60` and `130 km/h` measure monotonically rising field-only RMSE (`0.072862`, `0.161393`, `0.254572`), and the final source-versus-runtime comparison clears every blocking P0/P1/P2 item in `design-qa.md`. Five WAKE model checks, 283 existing unit checks, nine packaging checks and the production build pass; real-Tesla visual acceptance remains open.
- 2026-08-29 16:14 `[48d4228]`: Replaced derivative-based hard-launch recognition with a documented rolling-window detector calibrated against current official Tesla Model 3 AWD and Performance specifications. OPEN now crosses once on a supported `+30 km/h` trajectory inside `2.2 s`, accepts one-second GPS cadence, rejects single spikes and fixes worse than `25 m`, releases on braking, normalization, timeout or stale input, and observes a five-second refractory period. Deterministic tests cover full-throttle and ordinary urban curves, GPS gaps, inaccurate fixes and boundary reversals.
- 2026-08-29 16:14 `[48d4228]`: Made OPEN, UNDERWATER and BLOOM visible in every active visual through its native language: Aperture changes tunnel projection and tile light, Vertigo uses only the external bridge's permitted time/FOV/colour controls, Meridian changes corridor projection, flow, fog and rail energy, and Atlas changes MapLibre camera and paint properties. The byte-identical Interstate 7 source remains unchanged. Added a development-only muted `qaEffect` latch and three exact `773 × 601` Meridian captures.
- 2026-08-29 01:59 `[7e9b2e9]`: Added `BLOOM`, a rare hard-launch gesture shared by FRACTURE and JUNCTION. A rapid `1.5 → 4.0 m/s²` crossing inside OPEN sweeps a four-point-Hermite feed-forward delay from `8` to `0.8 ms` over 400 ms; band replacement confines the bend to `300 Hz–8 kHz`, preserves the sub within 0.3 dB, adds less than 0.5 dB peak on the offline fixture, nulls after release, observes a 25-second refractory period and yields to UNDERWATER over 250 ms.
- 2026-08-29 01:59 `[7e9b2e9]`: Added explicit JUNCTION selection and rendered-transition audits. The selection report proves all 24 current performances reach only chord-hit index zero; the audio-only pass measures 63 ordinary printed boundaries without pitch proposals and flags 22 for calibrated listening review, led by EASE and `Cmaj7 → Amin7`. Independent-source reports now retain bounded null floors and implied ratios, while proposal-only voicing reports expose octave, register-centroid and one-/thirteen-semitone collision evidence without admission verdicts.
- 2026-08-29 01:59 `[7e9b2e9]`: Added exactly three proposal-only REGISTER replacements — `PLUMB`, `SLIP`, and `WAKE` — with rest motion, road-speed mapping, OPEN/UNDERWATER response, minimum prototype and terminal retirement criteria. None is selected or implemented pending product-owner choice.
- 2026-08-29 00:58 `[0b9a870]`: Added the user-selected `REGISTER 05` Flux environment: three deterministic Swiss-print layout families change only on complete eight-bar score boundaries, acceleration samples `2–6` physical pixels of two-plate misregistration, braking aligns the current page over approximately `400 ms`, and reduced motion preserves a static composition. Exact local `773 × 601` Browser QA covered misregistered, aligned and complete product states.
- 2026-08-29 00:58 `[0b9a870]`: Added the shared `OPEN` hard-acceleration performance macro for FRACTURE and JUNCTION. Two consecutive readings above `3 m/s²` and `15 km/h` trigger a level-compensated `320 Hz` scoop, `9 kHz` air shelf and up-to-`4 dB` side expansion; the gesture reaches its target in `350 ms`, holds for at most four seconds, releases in one second, observes a six-second refractory interval and always yields to UNDERWATER braking. Local Browser QA observed the real OPEN state at `43 km/h` with zero warnings.
- 2026-08-29 00:58 `[0b9a870]`: Corrected the harmony pilot so calculated lower-source hypotheses and measured spectral peaks are separate fields, withdrew the derived `-1.955 cents` claim, and removed the falsified NNLS residual from classifier input entirely. The known-truth grid now passes only by enforcing abstention while preserving the failed `0.666667` false-positive rate and `-0.006729` margin as diagnostic evidence. The production build, 176 of 177 unit checks and four Sites packaging checks pass; the one remaining PHP mail fixture is unavailable because this Mac has no `php` executable.
- 2026-08-29 00:21 `[2114eb4]`: Demoted the proposal-conditioned harmonic residual from decision authority after adding the omitted F-sharp2 source falsified its first fixture (`0.666667` false-positive rate, `-0.006729` margin). Missing proposer sources now produce an explicit `unknown/no_proposed_source_candidate` abstention instead of a false voiced verdict. Added an independent, review-only sustain-spectrum search over harmonics 2–16: both `Bmin9` files propose the same B–D–F-sharp–A–C-sharp pitch classes in different registers, while only `WaveStrings` exposes a qualifying F-sharp3 hypothesis beneath the disputed C-sharp5. Seven focused checks and the production build pass; the broader suite remains 167/168 because the existing local PHP executable is unavailable.
- 2026-08-28 23:57 `[9d34d1e]`: Animated the Signal Gate's `PROJECT SPARKS` value from zero to its current playful total over a fixed four-second duration, independent of target size. The isolated counter component updates only when the rendered integer changes, cancels its animation frame when dismissed, completes immediately for reduced-motion users and exposes the final value once to assistive technology instead of announcing every step. Deterministic tests cover `15` and `1,500`; local Browser QA at `773 × 601` and `390 × 844` confirmed `000 → 015`, exact final-state semantics and zero console warnings or errors.
- 2026-08-28 23:49 `[9553643]`: Added a deterministic harmonic-residual arbiter and a known-truth C-sharp collision grid spanning four target levels and three saturation drives. The controlled fixture reaches `1.0` recall and `0.0` false-positive rate with a narrow `0.012246` separation margin; the corrected eight-file real-audio pass deliberately retains `pitch_set_midi: null` and `chord_label: unknown` because every Basic Pitch proposal crossed the calibrated threshold. A regression check protects relative-level evidence from consumed one-shot iterables, and the next validation boundary now includes shared ADSR, detune, chorus, spectral slope and saturation before the arbiter may gate material.
- 2026-08-28 23:38 `[3be0e69]`: Added a development-only sample-harmony pilot for the eight chord hits actually reachable by JUNCTION. The ignored Python 3.11/CoreML environment records source hashes, adaptive attack/sustain/tail segmentation, tuning and Basic Pitch 0.4.0 high-recall proposals while deliberately retaining `pitch_set_midi: null` and `chord_label: unknown` until a deterministic harmonic-residual arbiter passes synthetic ground truth. The first complete run analysed all eight source buffers without moving or rewriting them; three structural guard tests, 166 available unit checks, four packaging checks and the production build pass. The only unavailable unit check remains the pre-existing local `php` executable requirement.
- 2026-08-28 23:02 `[a18efd7]`: Added a compact top-left Signal Gate support control for the verified `buymeacoffee.com/enuzzo` profile. Its accessible on-screen panel presents the supplied byte-identical QR, a payment-independent suggestion invitation, a runtime-reconstructed email address and an explicitly non-transactional `PROJECT SPARKS` signal starting at 15. Exact local Browser QA passed at `773 × 601` and `390 × 844`, including keyboard, close-button and backdrop dismissal, with zero overflow or console warnings/errors.
- 2026-08-28 22:06 `[3bbe6be]`: Added compact Signal Gate links for collaborator Illobo (`github.com/illobo`) and the public `enuzzo/sedicivalvole` source repository. The existing Netmilk Studio credit and quiet local-processing disclosure remain directly below the launch action, with keyboard-visible focus and regression coverage.
- 2026-08-28 11:30 `[3cf0eaa]`: Added `ATLAS 05`, a lazy-loaded MapLibre/OpenFreeMap bird's-eye city environment with a minimal palette-owned vector style, height-driven 3D buildings, nearby Italian Wikipedia context, four passenger reading choices and a locally generated QR. Current position remains ephemeral; the diagnostic report and persistence stay coordinate-free, while the required third-party map and nearby-search requests are disclosed explicitly.
- 2026-08-28 11:30 `[3cf0eaa]`: Added deterministic ATLAS location, camera, content-normalization and style tests; MapLibre GL JS and the QR generator are isolated in dynamic chunks and recorded in the third-party notices.
- 2026-08-27 22:31 `[023717c]`: JUNCTION is now selectable and plays as an adaptive eight-section Jungle / Rave production. One 1.9 MB Opus bank and one browser decoder replace the development-only render; energy and sustained braking choose complete authored sections at their musical boundaries.
- 2026-08-27 22:31 `[023717c]`: Added a deterministic JUNCTION bank builder, parser and selection tests. The published resource contains one mixed, processed 64-bar work rather than loose or disguised source-pack samples, and FRACTURE remains audible until the bank is genuinely ready to play.
- 2026-08-27 22:06 `[31f8dc4]`: Added `docs/CURRENT-STATE.md` as the concise authoritative inventory of the implemented product, active constraints, verified boundaries, open work and documentation map. Added a documentation-consistency test that checks the implemented catalogs, rejects superseded claims in active overview documents and validates relative Markdown links.
- 2026-08-27 22:06 `[12e2eaa]` `build 20260827-2204`: Added the Git commit identity to the diagnostic report alongside the SemVer and build stamp, with a test covering the complete build-identity pipeline.
- 2026-08-27 20:06 `[cbeada7]` `build 20260827-2006`: **First publication in which the authored score actually plays.** `FRACTURE` runs live at `https://sedicivalvole.app/`, with the bundled AudioWorklet verified byte-identical on the canonical root.
- 2026-08-27 20:20 `[cbeada7]`: `docs/MUSIC-CRAFT.md`, a living record of the project's musical knowledge — consonance rules that matter and the ones a naive checker gets wrong, tempo and arrangement doctrine, mixing technique with sources, and every musical defect this codebase has made and fixed. `AGENTS.md` now requires it to be updated in the same session a fault is diagnosed or a technique is found, and requires a musical rule to be asserted in a test wherever it can be.
- 2026-08-27 20:20 `[cbeada7]`: `JUNCTION`, a sampled score built from the pack's own breaks, basslines and chord one-shots, with an eight-section arrangement and a progression of Emin9 - Cmaj7 - Amin7 - Bmin9 stated by a different synth each section. Development-only so far: it renders offline and has no runtime yet.
- 2026-08-27 20:20 `[cbeada7]`: `src/score/dsp/instrument-channel.js`, the channel strip that stops a sampled instrument sounding like a cheap keyboard: per-note variation of gain, start offset and detune, saturation, drifting mid/side width, and a pre-delayed filtered send into the shared room.
- 2026-08-27 20:20 `[cbeada7]`: The score library now states how each entry makes its sound, `GENERATIVE` or `SAMPLED`, with a mark as well as a word.

- 2026-08-27 18:42 `[4174d6a]`: The authored `FRACTURE` score now plays in the product. The engine in `src/score/` — arranger, transport, ported textStep DSP and the composition itself — was complete but had never been connected; a separate hardcoded worklet played instead.
- 2026-08-27 18:42 `[4174d6a]`: `src/score/worklet/score-processor.js`, a thin AudioWorklet shell owning only the render quantum, the message port, the mute ramp and the brake filter.
- 2026-08-27 18:42 `[4174d6a]`: `scripts/vite-audio-worklet.mjs`, which bundles the worklet entry into one self-contained file with esbuild. A `?url` import copies the entry without following its imports, so a multi-module worklet loads in development and breaks once deployed.
- 2026-08-27 18:42 `[4174d6a]`: `scripts/render-reference.mjs`, an offline Node render of a speed programme to a WAV with an arrangement timeline, and `scripts/analyse-render.mjs`, which reports band balance, stereo width and crest. Listening review needs no browser and no vehicle.
- 2026-08-27 18:42 `[4174d6a]`: A stereo plate reverb, a pad chorus and a mid/side width trim that stays mono below 140 Hz. The mix measured 3.6% wide, which is mono.
- 2026-08-27 19:13 `[81fecf1]`: A six-section form — `HOME`, `LIFT`, `TURN`, `DARK`, `OPEN`, `RETURN` — advancing one section per phrase, so twenty-four bars pass before anything repeats. One four-bar cycle repeating forever was previously the whole piece.
- 2026-08-27 19:13 `[81fecf1]`: A four-voice pad. The pad was monophonic and played a single note of each chord, so the harmony the score is written around was never actually sounding.
- 2026-08-27 19:13 `[81fecf1]`: `tests/score-harmony.test.mjs`, which checks the writing rather than trusting it: nothing leaves the key, no theme note forms a minor second or minor ninth against either chord it is heard over, and the bass resolves its degrees per chord. It found five real faults while the form was being written.
- 2026-08-27 19:13 `[81fecf1]`: `tests/score-brake.test.mjs`, which drives six seconds of real score through the underwater filter and asserts what level actually survives it.
- 2026-08-27 19:13 `[81fecf1]`: A truthful `SCORE` control and score library panel. Every direction is shown, including the five with nothing authored behind them, which read `IN PREPARATION` and cannot be selected.
- 2026-08-27 19:13 `[81fecf1]`: Five body colours beyond the vehicle's own — `NEON`, `MINT`, `SODIUM`, `SIGNAL`, `SULPHUR` — with a two-colour swatch where the theme genuinely uses two.
- 2026-08-27 19:20 `[a1cf5db]`: `tests/score-audition.test.mjs`, which auditions all eleven voices on a stopped vehicle and asserts each one makes a sound and releases on its own.

- 2026-08-26 17:14 `[0369bda]` `build 20260826-1714`: A structured, Git-ignored `_references/` library preserving the original bootstrap ZIP and extracted contents.
- 2026-08-26 17:14 `[0369bda]` `build 20260826-1714`: Versioned product specification, source audit, adversarial review, technical direction, deployment notes, licensing decision log, and roadmap.
- 2026-08-26 17:14 `[0369bda]` `build 20260826-1714`: Root and prototype `AGENTS.md` rules, including the English-only repository/interface convention.
- 2026-08-26 17:14 `[0369bda]` `build 20260826-1714`: A neutral Tesla capability harness for viewport, DPR, GPU, WebGL2, storage, GPS speed, AudioContext, AudioWorklet, and event reporting without coordinates.
- 2026-08-26 17:14 `[0369bda]` `build 20260826-1714`: Exactly three visual directions and the selected direction 1 luminous-axis asset.
- 2026-08-26 17:14 `[0369bda]` `build 20260826-1714`: A touch-first Drive Lab with WebGL2 motion, hue, reduced-motion fallback, GPS/demo source abstraction, keyboard simulator, Stop/Mute, Brake, and integrated diagnostics.
- 2026-08-26 17:14 `[0369bda]` `build 20260826-1714`: A 16-step synthesized audio spike with saturating tempo, arrangement layers, motion cues, delay, limiter, and deterministic signal tests.
- 2026-08-26 17:14 `[0369bda]` `build 20260826-1714`: SiteGround passive-FTP deployment scripts that parse `.env` as data and sanitize output.
- 2026-08-26 17:14 `[0369bda]` `build 20260826-1714`: A continuous low energy wave whose frequency and gain rise smoothly with speed, plus extra speed-gated kick and hi-hat subdivisions.
- 2026-08-26 17:14 `[0369bda]` `build 20260826-1714`: A compact-view `DIAG` entry point, a scrollable diagnostics panel, split/expanded viewport history, richer GPS/audio/runtime evidence, and a bounded chronological event log.
- 2026-08-26 17:14 `[0369bda]` `build 20260826-1714`: A same-origin PHP `Send Diagnostic` endpoint with a fixed recipient, schema/body validation, coordinate-key rejection, rate limiting, and sanitized responses.
- 2026-08-26 17:14 `[0369bda]` `build 20260826-1714`: A clean public GitHub repository at `github.com/enuzzo/sedicivalvole`, initialized from a reviewed snapshot without private bootstrap history.
- 2026-08-26 17:25 `[4d00946]`: A confirmed two-mode product architecture: `Engine` for selectable engine emulation and `Flux` for speed-reactive music and generative WebGL fields.
- 2026-08-26 17:45 `[44753b7]`: An active `AGPL-3.0-or-later` license for code and documentation, with a clear scope file, contributor notice, and explicit exclusions for brand and standalone media assets.
- 2026-08-26 18:07 `[859ec3a]`: A tracked recovery record for requirements that remained queued in the desktop client while the conversation continued from mobile.
- 2026-08-26 19:02 `[0d91917]`: A provenance, mechanics, performance, and license study of the Codrops/Tympanus Infinite Lights reference, preserved locally in the ignored reference library.
- 2026-08-26 19:02 `[0d91917]` `build 20260826-1902`: The approved Modular Aperture Flux direction: a procedural rectangular field that moves from a calm plane to a centered tunnel and releases coherently during deceleration.
- 2026-08-26 19:02 `[0d91917]` `build 20260826-1902`: Curated Pearl, Graphite, Red, Blue, and Silver `BODY COLOR` themes that tune both the field palette and interface accent.
- 2026-08-26 19:02 `[0d91917]` `build 20260826-1902`: A Canvas2D Modular Aperture fallback for devices without WebGL2.
- 2026-08-26 19:27 `[d261b57]` `build 20260826-1927`: A v3 extended diagnostic report with aggregate canvas frame pacing, long-task timing, connection history, navigation/resource metrics, heap/storage/battery hints, and a larger bounded event history.
- 2026-08-26 21:38 `[6695f09]`: A pinned study of `illobo/textStep` covering its synthesized drum/synth engine, 32-step patterns, Jungle/D&B presets, scene queue, browser portability, and authorized adoption plan.
- 2026-08-26 22:22 `[4c1c73b]`: Prominent project-level credit to Lobo (`illobo`) as the creator of textStep and its sequencer, synth, DSP, scene, and performance architecture, with linked provenance and an explicit future-derived-code attribution policy.
- 2026-08-26 22:29 `[c980189]`: A confirmed multi-environment Flux model binding generative effect, musical genre, palette, geometry, and speed response, plus three new selection-gated directions informed by the Interstate 4 and Interstate 7 mechanics study.
- 2026-08-26 22:55 `[98e8729]` `build 20260826-2255`: The user-selected `VERTIGO 02` visual environment, now using the byte-identical upstream Interstate 7 runtime with deterministic source-integrity and speed-bridge tests.
- 2026-08-26 23:38 `[78c9a5c]` `build 20260826-2338`: The approved Signal Gate splash: paired vermilion and ice-blue generative lanes bend into a central vertical gate, with a Canvas2D fallback and reduced-motion support.
- 2026-08-27 09:45 `[ca21e40]` `build 20260827-0945`: A documented Model 3 Long Range AWD Highland reference-motion model for time-based Demo acceleration, held braking, and soft GPS outlier tolerance.
- 2026-08-27 10:44 `[d415db8]` `build 20260827-1044`: A coordinate-free in-memory driving flight recorder with two-second speed/GPS/input/audio/frame/network samples, full-session motion aggregates, bounded runtime issues, payload sizing, and deterministic retention tests.

### Changed

- 2026-08-30 00:37 `[9b733f6]` `[ca5ffe9]`: Accepted the locally verified PRIMORDIAL clean-room field and the revised score-derived OPEN intake for canonical publication under the user's delegated product judgment. The decision is based on the recorded exact-viewport fidelity, responsive, fallback, reduced-motion, performance, signal, peak and mono-evidence gates; it does not convert either result into real-Tesla acceptance. Push, exact committed build, canonical deployment and post-publication verification remain the next gates.
- 2026-08-30 00:31 `[ca5ffe9]` `build 20260830-0031`: Replaced the acoustically ambiguous OPEN EQ/width lift with an unmistakable score-derived intake gesture. Every confirmed hard-acceleration event now crosses a minimum macro floor and sweeps a parallel band from `480` to `3200 Hz` during the existing `350 ms` attack; the added band is soft-limited independently, while stereo widening is reduced to `0.5 dB`, the main score retains a restrained `-1 dB` trim, and no oscillator, noise, melody, or replacement composition is introduced. An eight-second FRACTURE A/B raises mono difference RMS from `0.0110` to `0.0166`; a full-render hold lowers the previous over-scale `1.069` sample peak to `0.973`. The runtime graph, minimum activation, sweep direction, quiet-detail transparency, mono audibility, level and peak constraints are covered by tests. All 351 tests and the 132-module production build pass; muted local Browser QA shows OPEN at `35 km/h` with zero warning/error. This checkpoint is local only; human headphone/cabin listening, push, canonical deployment, and real-Tesla acceptance remain open.
- 2026-08-29 23:32 `[f616bd9]`: Corrected the publication identity gate after it safely stopped before writing when the project-owned DRIVEY bridge no longer matched the prior live bridge. Remote upgrades are now allowed only for the recognized `drivey/sedicivalvole.html` shell and its integration-boundary note, and only when both the existing remote payload and new local payload contain the pinned Sedici/Drivey identity markers. Every byte-identical upstream runtime file, every other third-party path, unknown content, and unexpected remote entry remains fail-closed. Fourteen focused deployment checks pass.
- 2026-08-29 23:26 `[9f177fa]`: Recorded product-owner visual approval for the bounded PRTCL implementation and opened its exact-source publication gate. The approval covers Fractal Frequency, Murmuration, Axiom, the separate text-only TYPE cycle, Sedici Valvole palettes, and the documented clean-room source boundary; real-Tesla motion, touch, frame pacing, and thermal acceptance remain open. PRIMORDIAL is authorized as the next local candidate under the already admitted project-authored clean-room boundary: the public [CodePen reference](https://codepen.io/shubniggurath/pen/NXGbBo) and official MIT-default policy are linked, while its HTML, CSS, JavaScript, shader, Three.js runtime, and separately attributed noise fragment remain excluded.
- 2026-08-29 23:22 `[633d526]` `build 20260829-2322`: Restored DRIVEY's automatic road and curve following through the pinned upstream `Input` class instead of a plain-object substitute, disabled manual steering, and removed only the player car's random lane weaving while preserving all 51 byte-identical vendor files. Every one of the ten Sedici Valvole themes now keeps its native `accent` and `secondary` colours as simultaneous material channels: Normal uses a four-stop dark/primary/secondary/light ramp and Wire separates the same pair across the road field. WAKE was rejected after repeated live review and removed from the catalog, runtime, fallback, tests, and current QA captures; stale `wake` preferences resolve to Aperture, while DRIVEY and PRTCL now occupy slots `05` and `06`. Exact muted `773 × 601` Browser QA confirms road alignment, dual-colour Normal/Wire rendering, six catalog entries and no WAKE; a 22-second `130 km/h` stress pass held the visible curve at `60.0 FPS`, `18.6 ms` p95 and zero intervals above `34 ms`. Ten DRIVEY checks, the complete 338-test suite, the exact 128-module production build carrying `633d526`, and the upstream integrity gate pass. The canonical FTP preflight was read-only and correctly reports a content mismatch with the unchanged live build; push, deployment and real-Tesla acceptance remain open behind the pending PRTCL approval boundary.
- 2026-08-29 16:14 `[48d4228]`: Increased OPEN's air shelf from `2.0` to `2.8 dB`, side expansion from `4.0` to `4.5 dB` and feed-forward trim from `-0.55` to `-0.2 dB`; increased BLOOM's band-replacement depth from `0.42` to `0.48`. A three-second mastered stereo fixture now proves OPEN remains below `0.9` sample peak, within `-1.2/+0.5 dB` RMS and above an objective difference floor, while BLOOM remains below `0.6 dB` in-band peak lift and preserves the sub. UNDERWATER audio is unchanged.
- 2026-08-29 02:03 `[4ea0bd1]` `build 20260829-0200`: Published BLOOM and the JUNCTION analysis tooling to the canonical root. The preflight and repeated post-publication FTP identity gates passed; 29 files / 8,316,609 bytes were uploaded. Bare and cache-busted HTML return `200`, `no-store` / `no-cache`, cache `MISS`, and byte-identical HTML, JavaScript, CSS and 6.6 kB BLOOM worklet assets. Visible live Browser QA confirmed the build stamp and ordinary APERTURE/FRACTURE launch; a qualifying hard launch and target-Tesla listening remain the perceptual acceptance boundary.
- 2026-08-29 01:28 `[a47142c]` `build 20260829-0121`: Published the complete REGISTER removal to the canonical root. Final FTP and HTTPS identity gates passed; bare and cache-busted HTML plus referenced JavaScript and CSS are byte-identical to the verified local build, which reports the correct build and commit identities. Visible live Browser QA confirmed the stale-selection fallback to APERTURE, the four-item Visual library and zero application warnings/errors; exact `773 × 601` QA confirmed the same selector without visible overflow. The only console request failure is the pre-existing missing `/favicon.ico`.
- 2026-08-29 01:20 `[b8dd697]`: Removed the rejected `REGISTER 05` visual from the active catalog, application renderer, development QA and unit suite after live review found it static, inexplicable and outside the driving mood. Its source and historical checks remain only in the rejected-visual archive; stale stored selections fall back to APERTURE, the visual-only JUNCTION boundary counter is gone, and OPEN remains unchanged. Browser QA confirmed the four-item Visual library, a working MERIDIAN selection and zero console warnings or errors; 172 of 173 active unit checks and the production build pass, with only the known unavailable local PHP fixture excluded.
- 2026-08-29 01:10 `[f6b6423]` `build 20260829-0107`: Superseded the first REGISTER publication with the persistent-page boundary correction. Preflight, 28-file upload, post-publication FTP and HTTPS identity gates passed; live exact `773 × 601` QA crossed revision `1 → 2` with all eight elements present in the first captured frame and then observed OPEN under held acceleration. The console has zero warnings and only the existing missing `/favicon.ico` request.
- 2026-08-29 01:07 `[d018907]`: Kept one persistent REGISTER page across eight-bar revisions instead of remounting the complete field. A live local boundary test observed revision `1 → 2` with every composed surface present in the first captured frame, removing the transient black page caught during post-publication acceleration QA. A regression check rejects future page-key remounts; 178 active unit checks are now declared.
- 2026-08-29 01:02 `[09a6c10]` `build 20260829-0059`: Published REGISTER 05, the shared OPEN acceleration macro and the corrected abstaining harmony evidence to the canonical root. Preflight, 28-file upload, repeated post-publication FTP identity and HTTPS identity gates passed; bare/cache-busted HTML plus the selected JavaScript, CSS and QR are byte-identical to the verified build. Exact live `773 × 601` Browser QA selected REGISTER and observed OPEN at `39 km/h`; the console has zero warnings and only the pre-existing missing `/favicon.ico` request.
- 2026-08-29 00:02 `[0bd3af3]` `build 20260828-2359`: Published the fixed-duration `PROJECT SPARKS` count to the canonical root. Preflight, upload and repeated post-publication identity gates passed; bare and cache-busted HTML, JavaScript, CSS and QR are byte-identical to the verified build. Exact live `773 × 601` Browser QA observed `000 → 004 → 008 → 015`, the stable accessible final-value label and zero console warnings or errors.
- 2026-08-28 23:05 `[557251c]` `build 20260828-2303`: Published the Signal Gate support panel and supplied Buy Me a Coffee QR to the canonical root. The post-upload identity gate passed; live HTML, JavaScript, CSS and QR are byte-identical to the verified build, and exact `773 × 601` Browser QA confirms the real destination, accessible dialog, `015` project-sparks signal, zero overflow and zero console warnings/errors.
- 2026-08-28 22:51 `[85aea3b]` `build 20260828-2249`: Published the linked Netmilk Studio credit, light splash-link hover state and monochrome GitHub source mark to the canonical root. The post-upload identity gate passed, live HTML/JavaScript/CSS are byte-identical to the verified build, and exact `773 × 601` Browser QA reports zero overflow or console warnings/errors.
- 2026-08-28 22:48 `[3f770cd]`: Linked the Signal Gate's Netmilk Studio credit to `https://netmilk.ch`, kept every splash link light and readable on hover, and added an 11 px monochrome GitHub mark to the public source link. Exact local Browser QA passed at `773 × 601` and `390 × 844` with zero overflow or console warnings/errors.
- 2026-08-28 22:21 `[e042344]` `build 20260828-2219`: Published the raised, readable Signal Gate metadata hierarchy and fail-closed support-link preparation to the canonical root. Repeated identity gates passed; exact live `773 × 601` Browser QA confirms `12.5 px` credit, `11.5 px` source, `10.5 px` privacy copy, zero overflow, zero console warnings/errors and a working launch transition. The support control is correctly absent because no real destination has been configured.
- 2026-08-28 22:19 `[e5f6164]`: Raised the complete Signal Gate launch group and enlarged its metadata from the former `8–9 px` scale to a `10.5–12.5 px` hierarchy with stable shadow contrast over the moving light lanes. Prepared a restrained `BUY ME A COFFEE` link behind a fail-closed, HTTPS-only `buymeacoffee.com` build setting; no button appears until the real profile is supplied.
- 2026-08-28 22:08 `[946c035]` `build 20260828-2208`: Published the compact Signal Gate collaborator and source links to the canonical root. The upload and repeated read-only identity gates passed; live Browser QA at exactly `773 × 601` confirms the Netmilk Studio / Illobo credit, both GitHub targets, zero document overflow and zero console warnings or errors.
- 2026-08-28 17:47 `[aa6fdb3]` `build 20260828-1745`: Published the four-environment Flux catalog to the canonical root after retiring Latitudes. Preflight, upload and post-publication identity gates passed; cache-busted HTML points to the expected main JavaScript and CSS, all three are byte-identical to the verified local build, and the live main bundle contains no Latitudes runtime. Exact `773 × 601` Browser QA confirms `APERTURE 01`, `VERTIGO 02`, `MERIDIAN 03`, `ATLAS 04`, a working Atlas-to-Aperture selection and zero console warnings/errors.
- 2026-08-28 17:45 `[b135650]`: Retired the rejected `LATITUDES` environment from the active Flux catalog, renderer, QA harness, test suite and production bundle; `ATLAS` now occupies slot `04`, while a stale stored `latitudes` preference safely falls back to `APERTURE 01`. The complete Latitudes source and its 13 passing model tests remain in an explicitly non-product historical archive. Exact `773 × 601` Browser QA confirmed four selectable visuals, a working ATLAS-to-APERTURE transition, no Latitudes entry and zero console warnings/errors; 159 active unit tests and four packaging tests pass. Removing the static renderer saves 12.26 kB minified / 3.30 kB gzip from the main JavaScript bundle.
- 2026-08-28 17:38 `[d048fdd]` `build 20260828-1736`: Published JUNCTION's four-second rhythm entrances and releases to the canonical root. Preflight, upload and post-publication identity gates passed; cache-busted HTML points to the expected main JavaScript and CSS, both byte-identical to the verified local assets. The 38.7 MB bank remains unchanged and cacheable because the new cancellable envelope lives in the browser mixer. Real-Tesla listening remains the acceptance boundary for the perceived transition.
- 2026-08-28 17:36 `[ff3c2b3]`: JUNCTION now treats the return of rhythm as a four-second musical entrance instead of exposing the first kick at full authored level. A decision to return to beatless rest releases the active rhythm-locked mix toward a near-silent floor over four seconds; the ambient section recovers gently, and re-acceleration cancels the descent without restarting the phrase. Complete eight-bar scheduling, deck balance, tempo mapping and source selection remain unchanged. Coordinate-free telemetry records `fade-in`, `fade-out`, `quiet` and `steady`; 171 unit and four packaging tests pass, including a simulated AudioParam timeline for entrance, release and cancellation.
- 2026-08-28 17:28 `[0c106e2]` `build 20260828-1725`: Published the ATLAS selected-place Wikipedia context card to the canonical root. Preflight, upload and post-publication identity gates passed; cache-busted HTML points to the expected assets, and the main JavaScript, CSS and ATLAS chunk are byte-identical to the verified local build. Live `773 × 601` Browser QA loaded the free thumbnail and abstract, changed selection with the QR target, retained exact panel fit and reported zero console warnings/errors.
- 2026-08-28 17:25 `[b90b255]`: Added a compact selected-place Wikipedia context card to the ATLAS passenger panel. The existing coarse nearby request now also asks PageImages for a free-license `320 px` thumbnail, so changing place updates title, image, two-sentence abstract and QR without a second content request; missing or unsafe image URLs fall back to the abstract alone. Exact `773 × 601` Browser QA confirmed a visible image, coordinated selection changes, `465/465 px` panel fit, working collapse/reopen and zero console warnings/errors; 170 unit and four packaging tests pass.
- 2026-08-28 17:18 `[d00d0eb]` `build 20260828-1715`: Published the rhythm-locked JUNCTION rebuild to the canonical root. The read-only preflight, upload and post-publication identity gates passed; cache-busted HTML references the expected JavaScript and CSS, and those assets plus the 38,712,125-byte JUNCTION bank are byte-identical to the verified local build. Local Browser QA at `773 × 601` loaded and ran JUNCTION with zero runtime issues; the simulated Web Audio handoff proves complete eight-bar boundaries and matched rhythm identities, while real-Tesla listening remains the acceptance boundary for perceived blend and variety.
- 2026-08-28 17:14 `[825a653]`: Rebuilt JUNCTION around the newly supplied nested Rave Synths library: 160 self-contained eight-bar clips, 20 takes per energy state, ten rhythm groups per state and five chord-derived exact-note multisampled motifs in a 38.7 MB segmented bank. Live pairs now require matching tempo, harmony and `rhythmId`; the secondary deck remains subordinate, stateful DSP and voices reset per clip, eight-millisecond edges prevent cuts, and a four-entry recent window prefers unheard families, rhythms and takes. Coordinate-free diagnostics now retain both rhythm identities so the next Tesla report can prove the mixer stayed locked. The native 127/135 BPM urban pacing, beatless rest, six-decoded-clip ceiling and source-pack non-redistribution boundary remain intact; 169 unit and four packaging tests pass, including a simulated sample-accurate complete-phrase handoff.
- 2026-08-28 16:46 `[72e31b8]` `build 20260828-1642`: Published the compact ATLAS attribution and collapsible passenger panel to the canonical root. Preflight, upload and post-publication identity gates passed; cache-busted HTML points to the expected build, and the main JavaScript, CSS and ATLAS chunk are byte-identical to the verified local assets. Live Browser QA at `773 × 601` confirms the `7 px`, `46%`-opacity attribution, `246 px` open panel, full-width `773 px` collapsed map, persistent midpoint handle, no document overflow and the correct build and commit identities.
- 2026-08-28 16:42 `[1ceb098]`: Reduced the mandatory ATLAS map attribution to a quiet `7 px`, `46%`-opacity strip positioned two pixels above the footer. Added a persistent, vertically centred right-edge handle that collapses the `246 px` passenger panel and expands the map from `527` to the full `773 px`; the hidden panel is removed from keyboard and assistive-technology interaction. Exact `773 × 601` Browser QA passed collapse, map resize, reopen, readable attribution, zero overflow and zero console warnings/errors.
- 2026-08-28 16:14 `[cac89a5]` `build 20260828-1613`: Published the stronger Signal Gate product hierarchy to the canonical root. Cache-busted live Browser QA at `773 × 601` confirms `sedicivalvole` at `38.65 px`, Orbitron `750`, zero added tracking, one-line fit, the unchanged `390 × 170 px` action, zero console warnings/errors and a successful transition into the running experience.
- 2026-08-28 16:12 `[c3f9a91]`: Increased the Signal Gate product wordmark from a maximum `30 px` to a responsive `32–40 px` range, yielding `38.65 px` at `773 × 601` while retaining Orbitron `750`, zero added tracking and one-line centering. The upper product band grows from `58` to `68 px`; the complete launch control remains exactly `390 × 170 px`, and the command retains its seamless wave and full touch area.
- 2026-08-28 16:08 `[e58ff12]` `build 20260828-1606`: Published the passenger-readable, direction-following ATLAS camera to the canonical root. Local `773 × 601` Browser QA covered `40` and `130 km/h`, steering and the complete panel; live QA confirmed the `246 px` panel, `12 px` reading hierarchy, `86 px` QR, map tiles, nearby content, build identity and zero console warnings/errors. Deterministic tests cover the retained high-speed 3D camera and GPS-heading fallback.
- 2026-08-28 16:04 `[2c8f0f6]`: Kept ATLAS strongly three-dimensional through the `130 km/h` ceiling by compressing only the upper camera pull-back: motorway view now remains at or above zoom `14.65` and pitch `55.5°` instead of flattening near zoom `13.8` / pitch `48°`. The map continues centering on every trusted position and now follows device heading or infers bearing from successive fixes; it no longer invents rotation when heading is unavailable.
- 2026-08-28 16:04 `[2c8f0f6]`: Enlarged the ATLAS passenger surface at `773 × 601`: the panel is `246 px`, reading and place text is `12 px`, the title is `21 px`, and the QR grows from `58 px` to `86 px` with a `192 px` source. Three-line context preserves all four choices and the QR inside the available 465 px height without overflow.
- 2026-08-28 15:55 `[3c85c7b]` `build 20260828-1553`: Published the centered Orbitron launch hierarchy, seamless one-period colour wave and isolated splash surface to the canonical root. Preflight, upload and post-publication read-only identity checks passed. Cache-busted live Browser QA at `773 × 601` confirms build identity, wordmark `750`, command `600`, zero added tracking, a `360 px / 4.2 s` infinite wave, splash layer `20` above ATLAS layer `5`, no text overflow and a successful transition into the active experience.
- 2026-08-28 15:52 `[6109f32]`: Kept the launch surface above every preloaded Visual overlay until the launch gesture completes. Live QA exposed ATLAS's no-GPS waiting layer above the splash when ATLAS was the persisted selection; the splash now owns an explicit higher layer, with a regression test proving it remains above ATLAS waiting and passenger panels without changing their running-state order.
- 2026-08-28 15:49 `[0193cac]`: Refined the single launch surface exactly as approved: centered lowercase `sedicivalvole` in Orbitron `750`, then uppercase `PLAY THE ROAD` in `600`, with zero added letter spacing on both. Replaced the percentage-positioned colour sweep with a 360 px repeating white-to-red field that translates by exactly one complete period every 4.2 seconds, eliminating the visible reset after repeated loops.
- 2026-08-28 15:49 `[c816b75]`: Removed the superseded `850` launch-weight token so the shared typography contract matches the rendered surface. Browser QA observed three complete wave boundaries across 222 samples: the raw background coordinate wrapped three times while its period-normalized visual step stayed bounded at 5.79 px, with no discontinuity.
- 2026-08-28 15:42 `[76f1b63]` `build 20260828-1538`: Published the Orbitron variable typography system to the canonical root. Live `773 × 601` Browser QA confirms the local font is loaded, the intended `520/620/700/850` hierarchy is computed, and primary chrome has no measured overflow; the served 11,800-byte WOFF2 is byte-identical to the packaged source at SHA-256 `c25a9f9da5d9f3db1bf2a01474722dc9b377675b7bbab6d0dfda6902794fd1ed`.
- 2026-08-28 15:42 `[fbbb794]`: Extended the fail-closed deployment identity gate to recognize the new canonical `fonts/` directory only when every existing remote entry belongs to, and is byte-identical with, the packaged font tree. The first post-upload check correctly rejected the previously unknown directory; the explicit verification path and its regression test now pass without weakening validation of any other root entry.
- 2026-08-28 15:37 `[6a6c55e]`: Replaced the product UI's system monospace stack with the locally hosted Orbitron variable font across launch, driving chrome, selectors, diagnostics, ATLAS panels, raw reports and map attribution. The hierarchy uses `450` for reading text, `520` for telemetry values, `620` for controls, `700` for operational labels and `850` only for the launch command; tracking is tightened where Orbitron's wider geometry needs it. The `773 × 601` Tesla surface has no measured text overflow, while the compact `390 × 844` layout now keeps the wordmark, APERTURE and JUNCTION fully visible.
- 2026-08-28 15:28 `[0d5153b]` `build 20260828-1520`: Published the adaptive visual refinements and silent JUNCTION launch to the canonical root. The preflight and post-publication read-only identity gates passed; live `773 × 601` Browser QA confirmed build identity, launch, the ATLAS Milan demo with 3D map, nearby passenger content and QR, and zero console warnings/errors. Deterministic local rendering covers APERTURE at `0`, `20` and `40 km/h`, LATITUDES and MERIDIAN across their speed range; real-Tesla frame pacing and listening remain the acceptance boundary.
- 2026-08-28 15:20 `[7ab86b2]`: Replaced Aperture's expensive full-frame flat/tunnel interpolation with the approved rigid square wall: it fills the frame at rest, recedes intact, and disappears at the existing terminus by `40 km/h`. Covered pixels evaluate only wall shading and revealed pixels only tunnel shading; the complete `0–40 km/h` retreat stays at one physical pixel per CSS pixel for the Tesla pressure band.
- 2026-08-28 15:20 `[7ab86b2]`: Reworked `LATITUDES 04` into eighteen thin, continuously phased oscilloscope contours whose waveform amplitude, compression and travelling pulses grow with road speed. Rebuilt `MERIDIAN 03` around deterministic Euclidean portals, blades, cantilevers and stepped monoliths, with segmented curved wind and translucent geometric cloud slabs moved above the corridor.
- 2026-08-28 15:20 `[7ab86b2]`: Added the production-safe ATLAS test path shown only without reliable GPS. `TEST FROM MILAN` starts a coordinate-free demo position, `ArrowUp`/`ArrowDown` retain the shared driving model, `ArrowLeft`/`ArrowRight` steer, and the camera rises toward bird's-eye with speed. Corrected the Atlas stacking order so the demo action and passenger place controls receive taps above the transparent experience layer.
- 2026-08-28 15:20 `[7ab86b2]`: Removed JUNCTION's audible launch cue without delaying readiness. The ambient rest performance still loads and schedules behind the splash, but its own output remains silent through `4 km/h` and fades to full level by `10 km/h`, before the quiet break enters near `13 km/h`; the global audio path and stopped-vehicle FRACTURE auditions remain unchanged.

- 2026-08-28 12:58 `[33529bb]` `build 20260828-1255`: Published the dense four-by-two Device evidence grid to the canonical root. Cache-busted HTML, main JavaScript and CSS are byte-identical to the verified local build; live `773 × 601` QA found one Device grid, all eight cards and zero console warnings/errors.
- 2026-08-28 12:55 `[11d8def]`: Reorganized the DIAG Device evidence as a smaller four-by-two matrix with 58 px minimum cards and reduced label, value and detail type. Exact `773 × 601` rendering retained all eight values in 153 px instead of approximately 287 px, recovering about 134 px while a two-column fallback protects sub-480 px displays.
- 2026-08-28 12:53 `[b2357a6]` `build 20260828-1249`: Published the compact DIAG action and feedback tray to the canonical root. Cache-busted HTML, main JavaScript and CSS are byte-identical to the verified local build; live `773 × 601` QA passed launch, DIAG opening, four-control presentation and zero console warnings/errors. A local failure simulation proved the complete warning remains visible; no artificial live diagnostic email was sent.
- 2026-08-28 12:50 `[81ca742]`: Compressed the Tesla DIAG panel into 68 px evidence cards, reduced heading/group/privacy spacing and placed all four actions on one 48 px row. Send success and failure copy now lives inside the same bottom-sticky tray directly above the buttons, so the result remains fully readable at `773 × 601`; deterministic presentation tests cover its DOM ownership and compact layout.
- 2026-08-28 12:41 `[1099581]` `build 20260828-1238`: Published the long-drive diagnostic transport correction to the canonical root. Cache-busted HTML, main JavaScript and CSS are byte-identical to the verified local build; a non-mailing 262,144-byte live probe crossed the retired 192 KiB boundary and reached schema validation, proving the tenfold request gate is active. Fresh real-vehicle mail delivery remains the acceptance boundary.
- 2026-08-28 12:38 `[3057796]`: Raised the same-origin diagnostic request ceiling from 192 KiB to 1.875 MiB and made client fitting measure the exact compact POST envelope instead of comparing browser two-space JSON with PHP four-space attachment JSON. The accepted report remains an always-GZIP level-9 attachment; bounded in-memory rings and transport fitting discard oldest detail first while preserving full-session aggregates and recording original/transmitted counts. Deterministic coverage now exercises both the tenfold endpoint budget and a pathological oversized drive.
- 2026-08-28 11:33 `[33ae761]` `build 20260828-1131`: Published Kinetic Meridian, Topographic Latitudes, corrected NEON/ACID thumbnails and `ATLAS 05` to the canonical root. Bare and cache-busted HTML, main JavaScript/CSS and all ATLAS/MapLibre/QR chunks are byte-identical to the verified local build. Live `773 × 601` QA passed launch, five-item Visual selection, coordinate-free ATLAS waiting state and zero console warnings/errors; real GPS map loading and vehicle memory acceptance remain open.
- 2026-08-28 11:30 `[3cf0eaa]`: Rebuilt `MERIDIAN 03` around the selected Kinetic Architecture direction. Solid and translucent towers, facade light, cantilevers, floor plates and atmospheric convergence share the existing displacement, camera and wrap clocks; speed reveals architectural mass and detail without changing the approved corridor behavior.
- 2026-08-28 11:30 `[3cf0eaa]`: Reframed `LATITUDES 04` as Luminous Topography. Fourteen continuous ribbons sample the renderer's existing eight-second motion history, with palette-driven relief, contour glow and sparse particles replacing the former quantized zebra field.
- 2026-08-28 11:30 `[3cf0eaa]`: Corrected the `NEON 06` selector thumbnail to magenta/lilac while preserving the real magenta/green `ACID 08` pairing. Local `773 × 601` QA at fixed `80 km/h` measured Meridian at 59.99 FPS / 18.0 ms p95 and ATLAS at 60.00 FPS / 18.3 ms p95 with zero slow frames; ATLAS latest browser-exposed JS heap was 55.7 MB and its lazy MapLibre chunk does not affect initial visual loading.
- 2026-08-28 09:26 `[76d90d8]`: Diagnostic email now keeps a compact integrity summary in the body and attaches the complete accepted schema/report envelope as build- and timestamp-named gzip-compressed JSON. The summary records JSON/GZIP sizes and SHA-256 digests; a deterministic PHP round-trip test extracts the MIME attachment, decodes and decompresses it, and compares all 157 fixture samples without relying on Gmail's clipped message preview.
- 2026-08-28 09:29 `[2b15fdf]` `build 20260828-0927`: Published complete diagnostic attachments to the canonical root. Bare and cache-busted HTML plus referenced JavaScript and CSS are byte-identical to the verified build; one explicitly authorized coordinate-free 157-sample live submission returned `202 accepted_by_mail_transport`. Recipient-inbox attachment display remains a separate user confirmation.
- 2026-08-28 09:57 `[436e9f9]`: Converted JUNCTION's nominal clip count into audible form-level variety: five harmony-compatible musical families now carry distinct progressions and colour punctuation across 104 complete takes built from 142 source recordings. The runtime changes family only at eight-bar boundaries, mixes two compatible takes live, avoids immediate take and family repetition, retains the six-clip decoded-memory ceiling, and holds 40-60 km/h to the lower 127/135 BPM steps.
- 2026-08-28 09:57 `[436e9f9]`: Reduced Aperture's Tesla fragment workload by 36% specifically through the 18-40 km/h plane-to-tunnel morph while preserving its shader geometry, time mapping and motion. Diagnostics now identify this morph phase, measure real Web Audio RMS/peak, reject low-confidence GPS control samples, preserve phase continuity, throttle GPS events and report active family/take exposure.
- 2026-08-28 09:57 `[436e9f9]`: Refined the 64 px Tesla footer into the selected `PALETTE` direction: audio is an accessible icon-only control, redundant running text is gone, Visual and Music use vertical label/value hierarchy with real disclosure carets, and `BODY COLOR` is renamed `PALETTE`.
- 2026-08-28 10:06 `[f788544]` `build 20260828-1001`: Published the Tesla-informed Flux refinements to the canonical root. HTML, JavaScript, CSS, AudioWorklet and the 25,044,179-byte JUNCTION bank are byte-identical to the verified local build; live `773 x 601` Browser QA passed launch, compact footer, palette presentation and Visual-library interaction. Real-vehicle performance and listening acceptance remain open.
- 2026-08-28 01:22 `[c3c2bdb]`: Replaced the Signal Gate's skeuomorphic launch details with one flat `390 × 170 px` typographic surface. The enlarged `sedicivalvole` wordmark and full-width `PLAY THE ROAD` field are the only content; the command now carries an infinite horizontal white-to-red wave, with a static reduced-motion state.
- 2026-08-28 01:22 `[c3c2bdb]`: Every red and ice-white Signal Gate lane now has a short travelling interruption with independently hashed phase, cadence and width, so neighboring gaps never align. Eight restrained perspective-air rays add depth in WebGL2 and Canvas2D without changing the road geometry; deterministic presentation tests cover the simplified control, text wave and per-lane motion contract.
- 2026-08-28 01:27 `[f84905c]`: The deployment identity gate now recognizes the three retired launch textures by exact SHA-256 before allowing their remote `ui/` directory to be removed. Unexpected files or changed bytes still abort publication; the read-only canonical preflight passed with no remote writes.
- 2026-08-28 01:33 `[9d045ff]` `build 20260828-0127`: Published the flat Signal Gate to the canonical root. Bare and cache-busted HTML plus the referenced JavaScript and CSS are byte-identical to the verified local build; live `773 × 601` Browser QA confirmed the exact `390 × 170 px` text-only surface, successful launch transition and zero console warnings or errors. Exact origin cleanup removed the three retired textures; unreferenced bare texture URLs may remain temporarily available only from the provider edge cache.

- 2026-08-27 23:25 `[36efdd2]` `build 20260827-2323`: Published JUNCTION's native-tempo road pacing to the canonical root. Live `773 x 601` QA played the 5.7 MB bank in a beatless 127 BPM ambient rest state, changed take 3 to take 2 at the next eight-bar boundary, sustained 60 FPS / 17.5 ms p95 beside Vertigo, and reported zero slow frames or runtime issues.
- 2026-08-27 23:20 `[019368f]`: JUNCTION no longer plays a dominant 168 BPM break at every speed. Rest now contains only sparse harmony and atmosphere; a reduced-level native 127 BPM beat fades in near 13 km/h, followed by authored 135, 158, 164 and 168 BPM states. Break gain is bounded, extra layers sit behind the primary beat, four consonant bass roles vary with the progression where the native recordings allow it, and all take changes remain gapless, boundary-quantized and non-repeating.
- 2026-08-27 23:07 `[42149f8]` `build 20260827-2304`: Published the three-take JUNCTION bank to the canonical root with a build-versioned audio request. Live `773 x 601` QA loaded and played the 5.1 MB bank, changed take 3 to take 1 at the next eight-bar boundary, sustained 60 FPS / 17.6 ms p95 beside Vertigo, and reported zero runtime issues.
- 2026-08-27 23:04 `[b7163f5]`: JUNCTION requests its bank with the generated build stamp. A provider cache was still serving the previous 1.9 MB bank at the bare audio URL after the new 5.1 MB resource had been uploaded and verified; each build now receives its matching bank while repeat requests within that build remain cacheable.
- 2026-08-27 23:02 `[a292518]`: The deployment identity gate now recognizes an existing JUNCTION resource by its owned bank signature and constrained manifest, allowing a legitimate bank update without accepting an arbitrary audio tree. Publication still performs a byte-identical comparison against the newly built bank immediately after upload; a deterministic test covers valid and rejected identities.
- 2026-08-27 22:54 `[ef0c0d0]`: JUNCTION now carries three complete authored takes for every energy state: 24 eight-bar sections and 192 bars in one 5.1 MB bank. At each musical boundary the runtime chooses a different take without immediate repetition; break phrases, voicings, density, drive and space vary inside the score instead of shuffling exposed samples mid-phrase.
- 2026-08-27 22:46 `[fba4002]` `build 20260827-2245`: Published the grounded Vertigo entry framing and quadratic urban-speed response to the canonical root.
- 2026-08-27 22:34 `[f9bb395]` `build 20260827-2232`: Published the complete Flux performance checkpoint to the canonical root: 60 FPS Aperture scheduling, editorial-free themed Vertigo, the 64 px Tesla footer, and the selectable JUNCTION production bank.
- 2026-08-27 22:31 `[87362c5]`: Aperture now renders at the display cadence instead of accidentally halving a 60 Hz display to 30 FPS. Time-based easing preserves the approved motion exactly, while invariant shader controls move out of the fragment shader; desktop Tesla-viewport QA improved from 30 FPS / 34.51 ms p95 to 60 FPS / 17.7 ms p95.
- 2026-08-27 22:31 `[e57400d]`: Vertigo stays hidden until its external bridge removes the upstream title and editorial shell, then maps every sedicivalvole theme onto the original runtime colour channels without changing any vendored Interstate 7 file. `ACID 08` replaces the orange theme with true magenta and green; the existing magenta/dark `NEON 06` remains available.
- 2026-08-27 22:31 `[afc3001]`: The retracting Tesla footer is 64 px at `773 x 601`, down from 140 px, with Audio on one row, compact theme controls, and explicit caret-marked Visual and Music libraries. The visible `SCORE` label is now `MUSIC`, and Visual opens a real picker instead of cycling blindly.
- 2026-08-27 22:31 `[436300d]`: Aligned the README, current state, architecture, roadmap, handoff, licensing notices and agent guidance with the implemented Aperture, Vertigo, footer and JUNCTION behavior. `docs/MUSIC-CRAFT.md` now records why a container cannot turn redistributable source samples into an authored score.
- 2026-08-27 19:52 `[e85d1cd]`: Corrected `NOTICE`, `LICENSE-SCOPE.md` and `THIRD_PARTY_NOTICES.md`, which still stated that no textStep source had entered the product. That was true until the score engine shipped earlier today. The notices now carry the exact per-file provenance the previous notice required before publication, and record that the combination rests on Lobo's direct authorization rather than on GPL v2.0 compatibility.
- 2026-08-27 19:52 `[e85d1cd]`: Recorded the two MusicRadar SampleRadar sample libraries, their links and their credited creators, and the decision that follows from their terms: they stay git-ignored, they are used to compose the project's own music, and only sedicivalvole's own rendered arrangements ship. Individual loops are never served.
- 2026-08-27 18:45 `[1f012a8]`: Scene changes resolve the hysteresis thresholds to a fixed point instead of stepping one scene per phrase. Climbs are capped at two scenes so the arrangement still builds; thinning is not capped, because a vehicle that has stopped should not keep playing a full break.
- 2026-08-27 18:45 `[1f012a8]`: Lane exits are queued from the top of the arrangement down. In lane order the kick left while the break detail was still chattering over it, which reads as the music breaking rather than thinning.
- 2026-08-27 18:53 `[ae29fa5]`: The reese plays a six-note syncopated riff instead of two held notes, and the sub is rearticulated around the kick with a quiet square for presence on a dashboard speaker. The bass line was reported as almost absent.
- 2026-08-27 19:01 `[5bdebef]`: The score library moved into a panel rather than growing the command bar. Six entries with their state written on them cannot be legible at arm's length inside a 190px cell.
- 2026-08-27 19:13 `[81fecf1]`: The command bar is 81px, down from 136. The mute control is an icon and a state instead of a label stacked over a large blank square, which was what set the bar's height.
- 2026-08-27 19:13 `[81fecf1]`: The colour pills lost their housing and their individual outlines, are taller, and have more space between them. An outline on a coloured pill only competes with the colour it is there to show.
- 2026-08-27 19:13 `[81fecf1]`: At a standstill the piece plays a phrase and then leaves one. Holding the resting arrangement continuously is unbearable at a red light.
- 2026-08-27 19:20 `[a1cf5db]`: The working level follows energy across four and a half decibels. The piece hit the limiter at a standstill exactly as hard as it did at the ceiling, so an arrangement that gains six lanes across a drive gained no weight.
- 2026-08-27 19:20 `[a1cf5db]`: Demo mode holds the speed it is left at. With no driver input it previously drove itself, which made the score's response to the driver impossible to judge.

- 2026-08-27 11:31 `[9544629]`: Accepted the maintainer's direct attestation of Lobo's unrestricted textStep reuse authorization, removed the invented written-evidence gate, and retained explicit authorship, GPL-2.0 provenance, source-file, commit, and modification tracking requirements.
- 2026-08-26 17:14 `[0369bda]` `build 20260826-1714`: Reworked the audio after user listening feedback: reduced the dominant noise bed and replaced the weak continuous tone with speed-dependent rhythmic and harmonic layers.
- 2026-08-26 17:25 `[4d00946]`: Named the current adaptive music mode `Flux` and exposed that identity in the splash, live header, page metadata, and diagnostic report.
- 2026-08-26 17:14 `[0369bda]` `build 20260826-1714`: Changed the development publication target from an isolated diagnostics path to the canonical site root.
- 2026-08-26 17:14 `[0369bda]`: Changed all code, comments, documentation, operational text, and interface copy to English; Italian is reserved for direct user/assistant conversation.
- 2026-08-26 17:14 `[0369bda]`: Defined canonical-root deployment as the default workflow during the private development phase, with post-upload live verification still mandatory.
- 2026-08-26 17:14 `[0369bda]`: Defined frequent verified commits, push-when-configured, Dropbox-only implementation, and safe fresh-session handoff as durable agent workflow rules.
- 2026-08-26 19:13 `[113a7e5]` `build 20260826-1913`: Hardened canonical-root deployment to identify the existing app read-only and retain previous content-addressed assets during cache overlap instead of breaking clients with cached HTML.
- 2026-08-26 21:08 `[792d3fc]` `build 20260826-2108`: Added a no-store PHP root entry so the canonical URL can bypass stale static HTML while preserving content-addressed assets and the Sites-compatible HTML build.
- 2026-08-26 21:08 `[792d3fc]`: Documented SiteGround's stale canonical HTML behavior, retained both valid bundles, and stopped after the provider rejected an unauthenticated cache purge.
- 2026-08-26 19:27 `[d261b57]`: Replaced the community viewport assumption with photographed Tesla evidence: `773 × 601` split-view viewport, `1254 × 784` logical screen, and DPR `1.53`.
- 2026-08-26 17:45 `[44753b7]`: Replaced the provisional licensing recommendation with an operative mixed code-and-asset policy while leaving exact legal ownership and final brand rules open for legal review.
- 2026-08-26 19:02 `[0d91917]`: Reopened the Flux visual gate after rejecting the lens-flare-heavy direction-1 implementation; the next gate requires exactly three revised minimal, Swiss-influenced directions.
- 2026-08-26 19:02 `[0d91917]`: Marked the current Flux audio spike as musically rejected and redirected the next iteration toward curated environments, authored adaptive arrangement, and one fixed road-energy domain.
- 2026-08-26 19:02 `[0d91917]` `build 20260826-1902`: Replaced the rejected luminous-axis/starburst rendering with an independent WebGL2 Modular Aperture shader and a flat Braun-influenced, Swiss, slightly brutalist control system.
- 2026-08-26 19:02 `[0d91917]`: Replaced primary `Atmos`, `Harmonics`, and `Pulse` controls with purposeful visual, score-roadmap, and body-color surfaces.
- 2026-08-27 12:57 `[95f8c54]` `build 20260827-1257`: Rebuilt the Flux audio spike as a four-section authored arrangement with a tempo knee, bar-quantized entries, hysteresis, two-bar dwell, crossfades, controlled deceleration, kick, low end, percussion, harmony, motif, delay, and conservative dynamics.
- 2026-08-26 19:02 `[0d91917]`: Removed the permanent on-screen Brake control; keyboard Space remains a deterministic bounded test event.
- 2026-08-26 19:27 `[d261b57]`: Configured the user-confirmed diagnostic recipient in ignored local/server configuration and kept transmission behind the explicit `SEND DIAGNOSTIC` gesture.
- 2026-08-26 19:42 `[32c9a1d]` `build 20260826-1942`: Replaced the bright tunnel terminus with a dark central void and made the header/footer retire fully off-canvas at rest, leaving only the speed readout visible until the next interaction.
- 2026-08-26 19:58 `[5420a3f]` `build 20260826-1958`: Reversed Modular Aperture travel so high speed flies outward from the vanishing point, made flow strongly non-linear near full energy, and let Demo reach and hold a true standstill before restarting.
- 2026-08-26 19:58 `[5420a3f]` `build 20260826-1958`: Made acceleration and deceleration explicit audio envelopes: acceleration now raises rhythm, low end, motion, and brightness, while deceleration reduces rhythmic pressure and opens a controlled spatial release without repeated transition effects.
- 2026-08-26 22:00 `[b5d16ec]` `build 20260826-2200`: Framed the persistent speed readout in a compact opaque high-contrast rectangle with fixed padding and no glass treatment.
- 2026-08-26 21:17 `[649216a]` `build 20260826-2117`: Separated visual velocity from saturated energy so tunnel recognition can begin at urban speed while perspective, travel, and radial stretch continue toward the fixed `130 km/h` ceiling.
- 2026-08-26 21:50 `[1d11b2b]` `build 20260826-2150`: Rebuilt Modular Aperture as one continuous shape field: large complete four-color squares at rest shrink, bend, and recede into the centered tunnel as speed rises, then reverse the same geometric transformation during deceleration without a crossfade.
- 2026-08-26 22:00 `[b5d16ec]` `build 20260826-2200`: Integrated the speed readout into the header grid with matching boundaries and stroke, then retained it as a fixed detached module when the surrounding chrome retires.
- 2026-08-26 22:00 `[b5d16ec]` `build 20260826-2200`: Introduced a restrained shared 6 px corner radius across framed controls and diagnostic surfaces while preserving the structural Braun/Swiss grid.
- 2026-08-26 22:15 `[fd54616]` `build 20260826-2215`: Refined the single-field Flux transformation so low-speed squares preserve their proportions, adjoining tunnel walls bend continuously, and perspective deformation grows progressively into the existing high-speed bars without opacity fades.
- 2026-08-26 21:38 `[6695f09]`: Rejected the current approximately 115 km/h soft-club Flux result and queued a data-driven AudioWorklet synth/sequencer with a high-energy Jungle identity and deceleration memory.
- 2026-08-26 22:29 `[c980189]`: Added a direct `ENVIRONMENT` switch between Aperture and Vertigo while preserving the shared speed, energy, palette, Brake, reduced-motion, and diagnostic contracts.
- 2026-08-26 22:55 `[98e8729]` `build 20260826-2255`: Translated the Interstate 7 deep-distortion mechanics into configurable Vertigo geometry: a power-curve floor-to-wall fold, paired lateral waves, longitudinal travel, speed-responsive perspective, and the same surface unfolding during deceleration without an opacity transition.
- 2026-08-26 23:11 `[82cc321]` `build 20260826-2311`: Removed the user-adjustable full-energy slider, fixed the shared road-energy ceiling at `130 km/h`, and remapped Aperture so tunnel depth is already visible near `40 km/h` before progressing continuously to the full Plaid field.
- 2026-08-26 22:29 `[c980189]`: Replaced the threshold surface with distinct `VISUAL` selection and a truthful `SCORE / PROTOTYPE / TEXTSTEP · NEXT` roadmap slot; no unimplemented genre is presented as active.
- 2026-08-26 22:55 `[98e8729]`: Increased Vertigo line weight, layered glow, and stationary breathing while preserving its original-inspired floor-to-wall fold, lateral wave, and independent renderer provenance.
- 2026-08-26 23:38 `[78c9a5c]` `build 20260826-2338`: Replaced the square-grid startup view with the selected Signal Gate direction, moved `PLAY THE ROAD` into a larger centered launch control, and added the centered `A project by Netmilk Studio` credit.
- 2026-08-27 09:17 `[d640ba7]` `build 20260827-0917`: Replaced the rejected independent Vertigo translation with the complete original Interstate 7 road, repeated side light sticks, opposing car trails, bloom, fog, camera, distortion, and vendor dependencies.
- 2026-08-27 09:17 `[d640ba7]`: Mapped the original Interstate 7 clock and FOV controls continuously to the fixed `0–130 km/h` road domain outside the byte-identical vendor runtime; the maximum uses the original non-boosted `1×` rate instead of the click-driven `3×` boost.
- 2026-08-27 09:17 `[d640ba7]`: Extended the canonical deployment identity gate to verify the complete third-party tree recursively before any subsequent vendor overwrite.
- 2026-08-27 09:45 `[ca21e40]` `build 20260827-0945`: Replaced the one-shot Space accent with a continuous held-brake simulation: press to decelerate from the exact displayed speed, keep holding to stop, and release to resume the Demo after a short settle.
- 2026-08-27 09:45 `[ca21e40]`: Replaced fixed-step Demo motion with elapsed-time integration calibrated to the official `4.4 s` zero-to-100 km/h figure; the estimated moderate braking curve uses a `1,824 kg` reference mass and a progressive pedal ramp.
- 2026-08-27 09:45 `[ca21e40]`: Applied the same reference dynamics as a soft GPS plausibility envelope while preserving real numeric GPS samples as the source of truth and never synthesizing vehicle movement.
- 2026-08-27 10:07 `[5328fb4]` `build 20260827-1007`: Replaced the fixed-step Arrow simulator lease with pedal-like input: hold `ArrowUp` to accelerate, release it to enter progressive regenerative deceleration, and return to GPS only after simulated and observed speed converge instead of dropping to the GPS value after a timer.
- 2026-08-27 10:07 `[5328fb4]`: Added a distinct nominal lift-off model with a `0.45 s` ramp, estimated `1.7 m/s²` peak regenerative deceleration, low-speed taper, rolling resistance, and Vehicle Hold capture below `0.8 km/h`; automatic Demo deceleration now uses the same release model.
- 2026-08-27 10:07 `[5328fb4]`: Split GPS deceleration tolerance into expected lift-off and wider plausible service-brake bands so ordinary regenerative slowdown remains responsive while impossible sensor jumps stay bounded.
- 2026-08-27 14:34 `[eacabf0]` `build 20260827-1434`: Rebuilt the Aperture Flux environment with an analytical 3D ray-tunnel projection, ensuring straight perspective lines, clean 45-degree diagonal miters, exactly 7 depth levels at 20 km/h matching benchmark reference geometry, a solid dark central terminus void at cruising speeds, a smooth physical end-wall transition to a full-screen flat square mosaic at 0 km/h, and a high-speed Terminal Velocity mode above 120 km/h with continuous fine white/red light streaks.
- 2026-08-27 14:34 `[eacabf0]` `build 20260827-1434`: Extended the QA field harness and diagnostic overlay with the active commit hash for exact build traceability.
- 2026-08-27 14:34 `[eacabf0]` `build 20260827-1434`: Extracted pure Aperture model calculations into `aperture-model.js` with comprehensive deterministic unit tests.
- 2026-08-27 14:36 `[4991269]`: Updated project instructions (`AGENTS.md` and prototype `AGENTS.md`) establishing the strict rule that the changelog must be progressive/append-only without altering past entries, and every entry must record the date, local time, and short commit hash.
- 2026-08-27 14:42 `[1fbbf73]` `build 20260827-1442`: Replaced the separate end-wall cap with a continuous progressive field deformation from the 2D square mosaic at 0 km/h into the 3D perspective corridor at 20 km/h, tightened Terminal Velocity speed streaks above 120 km/h to razor-thin laser lines, and eliminated phantom audio-pulse radial flash artifacts by strictly gating pulses to active visible tile bodies.
- 2026-08-27 14:51 `[3caa6a0]` `build 20260827-1451`: Eliminated diagonal sector seams and broken triangular tiles at standstill by preserving the pure 2D Cartesian grid across the full-screen end wall at 0 km/h, with smooth physical recession and tunnel emergence as speed rises to 20 km/h.
- 2026-08-27 14:54 `[1b0b846]` `build 20260827-1454`: Expanded tunnel formation across a smooth 0–55 km/h curve (visible by 35–40 km/h and reaching full depth at 55 km/h), eliminated low-speed motion dead zones with a lively progressive flow-rate curve, and added a progressive launch throttle ramp for natural, cinematic acceleration from standstill.
- 2026-08-27 14:55 `[39029e6]` `build 20260827-1455`: Completely eliminated ghost red flashes in the tunnel terminus void by gating terminus darkening as the final pipeline step after all pulse and brake lighting effects.
- 2026-08-27 15:02 `[c85b70c]` `build 20260827-1502`: Replaced the central floating wall cap with direct continuous geometric morphing between the 100% flat square 2D grid at 0 km/h and the 4 perspective tunnel walls at 35 km/h, enabling the grid to seamlessly fold into walls/ceiling/floor and unroll back on deceleration.
- 2026-08-27 15:10 `[9b9fb06]` `build 20260827-1510`: Permanently codified in `AGENTS.md` and prototype instructions that `CHANGELOG.md` is strictly append-only/progressive (never rewrite or delete past entries under any circumstances) and must always record the date, local time, commit hash, and build stamp when deploying.
- 2026-08-27 15:11 `[ee09e9a]` `build 20260827-1511`: Unified the entire Aperture visual field into a single seamless continuous coordinate deformation, where the exact same concentric square tiles physically stretch, curve, and fold into the 3D perspective tunnel walls without layer crossfades or floating end-wall caps.
- 2026-08-27 15:16 `[4da0614]` `build 20260827-1516`: Calibrated gentle low-speed depth and warp progression with an S-curve across 0–36 km/h, preventing sudden camera pull and ensuring braking smoothly flattens back to standstill without feeling rushed.
- 2026-08-27 18:01 `[b0e5bbb]` `build 20260827-1801`: Removed automatic looping from demo mode (it is now purely manual keyboard control). Mapped ArrowDown to brake. Intensified the underwater brake filter.
- 2026-08-27 17:50 `[f112ea4]` `build 20260827-1751`: Replaced harsh distorted bass with a smooth, deep sub-bass. Fixed a bug where Pads would not audition in the diagnostic preview.
- 2026-08-27 17:47 `[24b5454]` `build 20260827-1748`: Added an Audio Preview page accessible from the Diagnostics menu to audition individual DSP voices (Kick, Snare, Pad, Arp, etc).
- 2026-08-27 17:45 `[14e8491]` `build 20260827-1745`: Smoothed out Lead synth waveform and Arp decay to remove harsh button-click transients.
- 2026-08-27 17:38 `[48744fd]` `build 20260827-1738`: Implemented full 4-bar harmonic progression (Em9, Cmaj9, Am9, Bm7), rolling syncopated bassline, and soaring lead melody for rich arrangement variety.
- 2026-08-27 17:35 `[75e1d72]` `build 20260827-1736`: Fixed CSS overlap issue where the new expanded telemetry readout collided with the mode selector.
- 2026-08-27 17:28 `[3fe976f]` `build 20260827-1728`: Redesigned topbar telemetry for Tesla ergonomics. BPM and Energy are now displayed at the same scale as road speed.
- 2026-08-27 17:24 `[eef89ef]` `build 20260827-1724`: Added real-time visual UI badge indicating when the UNDERWATER brake effect is active.
- 2026-08-27 17:21 `[5e0ad92]` `build 20260827-1721`: Moved energy and BPM telemetry into the top navbar and implemented continuous audio brake effect based on natural Tesla deceleration curve.
- 2026-08-27 17:14 `[a2f7736]` `build 20260827-1714`: Implemented textStep Phase 3 (Sidechain ducking, delay, SVF brake filter, arpeggio, and deceleration memory).
- 2026-08-27 16:50 `[796f217]` `build 20260827-1650`: Implemented textStep Phase 2 (Hex decoder, multi-synth DSP for Kick, Bass, Hat, Snare, Pad) inside AudioWorklet.
- 2026-08-27 16:21 `[bda869d]` `build 20260827-1621`: Replaced main-thread audio with textstep AudioWorklet skeleton for deterministic transport.
- 2026-08-27 15:50 `[4ecc6a3]` `build 20260827-1550`: Smoothed the resting grid color changes by applying a continuous 1.5s crossfade per tile instead of instantaneous blinking.
- 2026-08-27 15:36 `[7733ec4]` `build 20260827-1536`: Corrected the Aperture geometry to smoothly deform the initial pure Cartesian grid into the 4 tunnel walls (ceiling, floor, side walls) instead of receding as a solid wall. Restored the central void to the correct 12% proportion to ensure the floor exhibits exactly 7 tiles in depth by ~10 tiles in width.
- 2026-08-27 15:27 `[0d6d739]` `build 20260827-1527`: Implemented the precise 3D raycasted tunnel geometry for Aperture, starting from a pure Cartesian flat grid at 0 km/h, smoothly receding into a 3D tunnel up to 35 km/h, and fading into a pure black void at 7 levels of depth. Included the longitudinal flow and terminal velocity laser streaks as requested.
- 2026-08-27 23:40 `[da845b3]`: JUNCTION now mixes two distinct, tempo- and harmony-compatible complete takes live at every eight-bar boundary. Sample-accurate AudioBuffer starts feed equal-power gain ramps, bounded stereo and tone variation, and energy-scaled tempo delay; the previous primary take cannot repeat immediately. Its single 5.7 MB bank is segmented into eight independently decodable state blocks, with at most two retained in memory, while isolated sample-pack loops and stems remain absent. Local `773 x 601` QA beside Vertigo sustained 60 FPS / 18.2 ms p95 for 174 seconds with zero frames above 34 ms, zero long tasks, zero runtime issues, one 17.4 MB decoded rest block, and a live `3 + 1` take mix.
- 2026-08-27 23:44 `[c17700c]` `build 20260827-2342`: Published JUNCTION's live two-deck mixer to the canonical root. The live build and commit identity match, the 5,738,856-byte segmented bank is byte-identical at SHA-256 `f2d56e8a8c3e69be4f2b98e6db845b1024ed96487cb9a99ca92fe0e5498d16d6`, and `773 x 601` QA played take pair `2 + 3` beside Vertigo at 60 FPS / 18.5 ms p95 with zero slow frames, long tasks, or runtime issues.
- 2026-08-27 23:58 `[532b30e]`: Expanded JUNCTION from 24 sections to 104 independently decodable eight-bar clips: 13 variations for every adaptive state, composed from 126 distinct native breaks, matched basses, chord performances and exact chord-tone accents. The 24.8 MB blob remains one cacheable request while the browser retains at most six clips and mixes two live. Local `773 x 601` QA changed pair `3 + 11` to `7 + 4`, bounded decoded PCM at 34.8 MB, and sustained 60 FPS / 17.6 ms p95 beside Vertigo with no slow frame, long task or runtime issue.
- 2026-08-28 00:01 `[1238cbd]` `build 20260827-2359`: Published the 104-clip JUNCTION bank to the canonical root. The live 24,753,770-byte resource is byte-identical at SHA-256 `83fe3058d6a5466ecdc8439a61de2a4023d6ce977722407b2fb134798977f314`; live diagnostics confirm all 126 source recordings, pair `8 + 2`, the six-clip / 34.8 MB PCM cache bound, and 60 FPS / 17.4 ms p95 beside Vertigo with zero slow frames, long tasks, or runtime issues.
- 2026-08-28 00:58 `[5ed9981]`: Rebuilt `PLAY THE ROAD` as the selected Braun/Dieter Rams instrument plate. The `sedicivalvole` wordmark now lives inside the single semantic launch control, whose `390 × 170 px` Tesla layout, warm-ivory frame, technical vent, textured red safety insert, latch and black activation field were measured against the approved reference at `773 × 601`.
- 2026-08-28 00:58 `[5ed9981]`: Added bounded phase-specific performance summaries for the splash, every active Visual/Music pairing and the DIAG-open state. Each phase records FPS and frame-time distribution plus browser-exposed heap, JUNCTION decoded PCM and bank memory; unsupported memory APIs remain explicitly unavailable. A deterministic test covers separation and memory bounds.
- 2026-08-28 00:58 `[5ed9981]`: Removed the DIAG drawer's isolated left border and separated the panel with a shadowed edge. Local Demo QA crossed Aperture, Vertigo, FRACTURE, JUNCTION and DIAG with seven phase summaries, zero runtime issues, no coordinate keys and 133 + 4 passing tests.
- 2026-08-28 01:04 `[81824b8]` `build 20260828-0100`: Published the Braun launch plate and phase diagnostics to the canonical root. Live `773 × 601` QA crossed Signal Gate, Aperture + JUNCTION, Vertigo + JUNCTION and DIAG; the report recorded five phases, 60.38 FPS / 18.4 ms p95, 12 MB JavaScript heap, 33.2 MB decoded PCM, zero runtime issues and no coordinates. One authorized simulated report reached `SENT` with `accepted_by_mail_transport`; inbox delivery remains separate.
- 2026-08-28 09:57 `[436e9f9]`: Rebuilt JUNCTION around five rotating harmonic/colour families instead of 104 variations of one progression. The 104-clip, six-decoded-clip architecture now uses 142 distinct recordings in a 25,044,179-byte bank; two takes mix only within one compatible family, and the next eight-bar boundary avoids both the current primary take and musical family. Native pacing now holds 127 BPM at 40 km/h, 135 BPM at 60 km/h, and enters 158 BPM above approximately 65 km/h.
- 2026-08-28 09:57 `[436e9f9]`: Implemented the selected compact `PALETTE` footer: one icon-only 64 px audio cell without `RUNNING`, vertical category/value hierarchy for Visual and Music, truthful carets, and a shared palette label for field and UI accent. Browser QA at `773 × 601` measured `64 / 196 / 212 / 301 px` tracks with no overflow and zero console issues.
- 2026-08-28 09:57 `[436e9f9]`: Extended coordinate-free diagnostics with continuity-safe phase timing, an Aperture morph phase, throttled GPS events, GPS-confidence hold, real output RMS/peak, active Visual/Music, JUNCTION family/take/bank state, and unique exposure counts. The first complete 157-sample Tesla report is now documented as the evidence behind these changes.
- 2026-08-28 16:21 `[3afcf75]` `build 20260828-1621`: FRACTURE now launches with atmosphere alone instead of immediately stating its sub and keyboard-like principal hook. ROLL earns pulse and low end; the melody waits for BREAK and a phrase boundary. HOME has a new sparse four-note line and soft slow-attack `riffAir` timbre, while deterministic arrangement and harmony tests protect the ambient opening.
- 2026-08-28 16:24 `[9ef166c]` `build 20260828-1622`: Published the ambient FRACTURE opening to the canonical root. This append-only entry supersedes the preceding pre-publication build stamp: the live app identifies commit `9ef166c` and build `20260828-1622`; its 93,321-byte AudioWorklet is byte-identical to the verified local bundle at SHA-256 `ed505a5a0232c028733a61dc1f9b787f5a32950d2de1baf75f5f985423d1920e`.
- 2026-08-28 16:31 `[ef1ed80]`: Recalibrated the topbar for Orbitron: speed, tempo and energy units now sit below their values with matching right edges instead of competing for horizontal space. Exact `773 × 601` QA with `130 / 162 / 100` kept the 68 px topbar, contained every label, produced zero horizontal overflow and logged no browser warning or error.
- 2026-08-28 16:34 `[855fb0a]` `build 20260828-1633`: Published the Orbitron-calibrated telemetry stack to the canonical root after rebuilding from the exact release commit. The cache-busted live app and CSS are byte-identical to local at SHA-256 `baeeff2cc57b41c725e1172897df86dd9d43d5088d0780007b82125b4aaa3041` and `9905ba666dfd4ae31ad872f05544eacdbbdb3d257644c479867654c914e31926`; the app exposes commit `855fb0a` and build `20260828-1633`.

### Fixed

- 2026-08-28 09:57 `[436e9f9]`: Aperture keeps its exact shader, geometry and motion mapping but drops the Tesla-only 1.25× supersampling during the 18–40 km/h morph, reducing fragment work by 36% while retaining one physical pixel per CSS pixel. The 30 km/h harness sustained 60 FPS / 17.6 ms p95 with zero frames above 34 ms.
- 2026-08-28 09:57 `[436e9f9]`: Phase telemetry no longer counts time spent in another visual state as one enormous frame when a phase returns. JUNCTION string sections are retained instead of failing a numeric-only check, and the audio field measures the output rather than duplicating road energy.

- 2026-08-27 22:46 `[fba4002]`: The deployment identity gate now recognizes the published `/audio/` tree only when every remote entry is expected and byte-identical to the local build. Its existing but unreachable `--verify-only` path is now an explicit no-write command. The first publication attempt stopped safely after seeing Junction's legitimate audio directory; no remote write occurred before the corrected gate passed.
- 2026-08-27 22:42 `[359e6c6]`: Vertigo now enters on an existing Interstate 7 phase where the road already fills the Tesla viewport instead of appearing as a small shape at the bottom. Its external speed/FOV bridge uses a quadratic road-speed response — approximately `0.095×` at 40 km/h and `0.213×` at 60 km/h — while preserving the original geometry, distortion, camera system and `1×` ceiling behavior.
- 2026-08-27 22:06 `[e7adfb0]` `build 20260827-2204`: Prevented the persistent speed source readout from overlapping the `ENGINE` / `FLUX` mode selector at the photographed Tesla split viewport. Browser QA at `773 x 601` measures zero horizontal overlap.
- 2026-08-27 22:06 `[31f8dc4]`: Recorded an append-only traceability correction for four superseded short hashes retained in earlier entries: `7733ec4` maps to reachable commit `45d73a8`, `4ecc6a3` to `34b54af`, `e85d1cd` to `6970eb1`, and `cbeada7` to `140b6fc`. The historical lines remain untouched.
- 2026-08-27 20:20 `[cbeada7]`: The publication script ignored every argument, so `--help` ran a real deployment. It had already done so once and the previous evidence entry said it must not happen again. `parse_arguments` now prints usage and publishes nothing for `--help` or any unrecognised argument.
- 2026-08-27 20:20 `[cbeada7]`: A sampled melody was folded into an instrument's range one note at a time, so notes needing different numbers of octaves inverted the interval between them: a line rising from B4 to D5 played as B3 falling to D3. One whole-octave offset is now chosen for a whole line.
- 2026-08-27 18:53 `[ae29fa5]`: The principal theme was transposed twice and played a semitone off twice per harmonic cycle. The chord root was already carried by `bassMidi`, and a second `rootOffset` moved the theme again on top of it: over `Dbmaj7` it played A-C-E-G against Db-F-Ab-C. The theme and its response are now absolute pitches in F natural minor and are not transposed at all.
- 2026-08-27 18:42 `[4174d6a]`: The brake latched on indefinitely. `keyup` handled `Space` but never `ArrowDown`, and the brake amount was only ever updated from a speed *change*, so a stopped or cruising vehicle froze it wherever it stood. It now runs on its own 40 ms clock with hysteresis both ways and never engages below 3 km/h.
- 2026-08-27 18:42 `[4174d6a]`: The `BPM` readout reported a tempo the music never played. It now reports the transport's own tempo.
- 2026-08-27 19:01 `[5bdebef]`: Restored the retracting two-bar control layout. A stylesheet edit had cut the file from the first match of `.control-slab {`, which fell inside `.controls-resting .control-slab {`, leaving half a selector dangling and swallowing everything after it.
- 2026-08-27 19:13 `[81fecf1]`: The underwater brake effect no longer empties the level. Sweeping to 200 Hz removed so much energy that it read as the volume being cut; the corner now stops at 430 Hz, the signal is driven into a soft shaper and trimmed after, and the surviving level is measured rather than guessed.
- 2026-08-27 19:20 `[a1cf5db]`: The voice preview threw on every synth voice, because `audition` still called `harmonyForBar` with the pre-form signature, and the ghost and clap were inaudible because the audition lift was keyed by lane and neither has one.
- 2026-08-27 19:20 `[a1cf5db]`: Below thirty km/h there was practically nothing to hear. The standstill rule gated the whole half-time band rather than a genuine stop, and the low band had quarter-note hats and no ghost field at all.
- 2026-08-27 11:17 `[3d7c64b]` `build 20260827-1117`: Prevented PHP from expanding short telemetry decimals into 53-digit binary representations, which caused long Tesla flight-recorder reports to exceed the server mail limit after browser-side fitting; diagnostic send failures now expose a sanitized actionable reason.
- 2026-08-27 09:45 `[ca21e40]`: Moved keyboard focus to the live experience after launch so held simulator input reaches the parent app reliably beside the non-interactive Vertigo iframe.
- 2026-08-26 17:14 `[0369bda]`: Corrected the SiteGround remote path after confirming the FTP account home sits above `sedicivalvole.app/public_html`.
- 2026-08-26 17:14 `[0369bda]`: Removed the previously uploaded wrong FTP-home diagnostics tree after exact-name validation and explicit authorization.
- 2026-08-26 17:14 `[0369bda]`: Prevented keyboard simulation from intercepting controls that own arrow or Space input.
- 2026-08-26 23:11 `[82cc321]` `build 20260826-2311`: Added smoothing, deadband, saturating energy/tempo, and a discrete brake cooldown to avoid jitter and frantic high-speed tempo.
- 2026-08-26 19:02 `[0d91917]` `build 20260826-1902`: Corrected GLSL portability issues found by rendered browser QA and made the fallback redraw continuously with energy and theme changes.
- 2026-08-26 19:58 `[5420a3f]` `build 20260826-1958`: Removed repeated motion-transition cues during sustained speed changes and added automatic envelope decay when speed settles.
- 2026-08-26 21:08 `[792d3fc]` `build 20260826-2108`: Restored canonical bare-root convergence after the SiteGround cache flush; `/` now returns the current no-store PHP entry and content-addressed assets.
- 2026-08-26 21:50 `[1d11b2b]` `build 20260826-2150`: Removed diagonal wall-boundary cuts and transition flashes by deriving the flat plane and tunnel from one interpolated coordinate field; resting modules are now intact squares and the dark aperture opens only as tunnel depth forms.
- 2026-08-26 19:13 `[113a7e5]` `build 20260826-1913`: Added a non-destructive `--preserve-existing` FTP publication mode for deployments that are authorized without legacy-file deletion.
- 2026-08-26 17:25 `[4d00946]`: Updated the read-only deployment identity gate to recognize the current Flux root markers instead of relying on obsolete `Drive Lab` copy.
- 2026-08-26 21:28 `[428a3cd]` `build 20260826-2128`: Removed visible resting seams, black gaps, and random sparse occupancy; all zero-speed cells are present and distributed evenly across the active four-color palette.
- 2026-08-27 12:28 `[1e9f741]` `build 20260827-1228`: Removed low-speed triangular tears caused by an abrupt tunnel-wall selector and an over-amplified central perspective singularity.

### Security

- `.env`, local variants, and `_references/` are ignored by Git.
- FTP credentials remain in memory and are never printed or placed in process arguments.
- GPS coordinates are discarded and never included in the report.
- Common full and abbreviated coordinate keys are rejected recursively by the v3 endpoint; technical metrics remain bounded and no automatic remote telemetry is enabled.
- Plain FTP on port 21 remains a known risk because credentials and content are unencrypted.
- The diagnostic recipient is stored in an ignored local PHP configuration rather than public source code.

No dated releases exist yet.

### 2026-08-28 Meridian and music correction

- 2026-08-28 19:50 `[b33663b]`: Rebuilt `MERIDIAN 03` against the newly selected oblique-blade fidelity contract. The conventional tower/city grammar, repeated posts and high cloud slabs are gone; sparse large red/white/secondary planes and longitudinal shoulder structures now occupy one low corridor. Field lift and roll are tightly bounded, while FOV, depth compression, peripheral stretch, parallax and flow grow monotonically with speed. Exact local Browser comparisons cover `0`, `40`, `90` and `130 km/h` at `773 × 601`; a sustained WebGL2 pass measured 60.15 FPS / 18.1 ms p95 with zero runtime issues. Real-Tesla visual acceptance remains open.
- 2026-08-28 19:50 `[91fd4b8]`: Removed FRACTURE's recurring `riff` and `response` lanes from normal playback rather than revoicing them; atmosphere, harmony, sub/reese, drums, dynamics and space now carry the production arrangement, while explicit parked audition remains available. Rebuilt JUNCTION as 24 complete eight-bar performances from 76 authorized recordings in a 5,812,755-byte `SVJCTN04` bank: one stable `Emin9 – Cmaj7 – Amin7 – Bmin9` identity, one tonal performance at a time, no automatic rave lead/multisample melody, complete-boundary changes, immediate-repeat avoidance, native 127–168 BPM states, four-second envelopes and a six-clip decoded limit. An exact-speed launch now seeds the new audio engine before score selection, so `130 km/h` starts in `FULL` at `168 BPM` instead of a stale `REST` block.
- 2026-08-28 19:50 `[91fd4b8]`: Added deterministic rejection tests for FRACTURE live melody lanes, multiple JUNCTION tonal identities, rave multisample/lead use, incompatible layering, primary repetition and unbounded Meridian motion/projection. The full gate passes with 156 unit tests, four packaging tests and the production build. Offline reference measurement reports FRACTURE at -16.0 LUFS / -0.8 dBFS true peak with zero clipped PCM frames and JUNCTION at -19.5 LUFS / -2.7 dBFS true peak; detailed evidence is recorded in `docs/AUDIO-QA-2026-08-28.md` and the append-only Product Design QA record.
- 2026-08-28 19:56 `[e314704]` `build 20260828-1950`: Published the Meridian and music correction build to the canonical root after a read-only preflight and repeated no-write identity gate. The 27-file / 8,200,466-byte upload exposes byte-identical canonical HTML, `index-rBa6H7HW.js`, `index-ArFavGwq.css` and the 5,812,755-byte `SVJCTN04` bank; bare and cache-busted HTML converge with no-store/no-cache behavior. Live Browser QA at exact `773 × 601` launches Flux, selects `MERIDIAN 03`, retains JUNCTION and palette controls, and reports zero warnings or errors. Real-Tesla visual and listening acceptance remains open.

### 2026-08-29 Low-speed life, runtime resilience and release safety

- 2026-08-29 15:24 `[892de32]`: Hardened static packaging and FTP publication against private-name variants, ancestor symlinks, validation-to-use replacement, malformed legacy JUNCTION references and browser/deploy parser mismatches. The local diagnostic recipient now remains outside every Vite-served tree, and operational deployment modes remain explicit.
- 2026-08-29 15:28 `[39396c8]`: Rebuilt the 24-clip JUNCTION bank on the corrected bar-closing grid and added a reproducible full-brake analyzer over every encoded performance. The tracked 5,812,361-byte bank is `0662ec081d7999c7dd365162d72abc63022d773037f175aac9199a7775fe69b5`; calibrated braking uses a 550 Hz low-pass, restrained resonance, parallel saturation and no pitch or playback-rate change.
- 2026-08-29 15:29 `[77da446]`: Bounded Flux renderer telemetry, stopped failed WebGL and iframe loops cleanly, guarded stale ATLAS requests, retained the byte-identical Interstate 7 runtime and kept APERTURE as the accepted fallback while no replacement direction is selected.
- 2026-08-29 15:31 `[82fd3bf]`: Replaced FRACTURE's perpetual PARK tone with six slowly evolving consonant voicings and added the shared clockless PARK, two-event DEPART and quiet CREEP/ROLL policy. JUNCTION stays below 100 perceived BPM through the displayed 20 km/h state, enters native pacing only beyond it, preserves one complete eight-bar performance, and enforces the real six-slot decoded/source-held memory union.
- 2026-08-29 15:32 `[f3f5e82]`: Integrated the low-speed score into the product UI, added muted exact-viewport QA, resilient score/visual fallback states and responsive control retention. Local Browser QA passed at `773 × 601`, `601 × 390` and `390 × 844`; the rejected PLUMB direction remains absent and no Flux replacement was selected.
- 2026-08-29 15:33 `[330c503]`: Pinned the Node/npm contract, moved build-only packages to development dependencies, updated Vite to 6.4.3 and rebuilt native Rollup/esbuild dependencies for Apple silicon. The lockfile audit reports zero known vulnerabilities.
- Listening acceptance remains intentionally open: FRACTURE PARK, DEPART, the 20→21 km/h hand-off, braking, perceptual loudness and the F-minor↔E-minor score transition still require low-volume review and a real-Tesla drive before being called musically accepted.
- 2026-08-29 15:42 `[5685de3]` `build 20260829-1536`: Published the low-speed score, resilient Flux runtime and release-safety checkpoint to the canonical root. All 30 files / 8,368,023 bytes passed the read-only identity gate before and after upload, with `remote_writes=NONE` on both verification passes. Bare and cache-busted HTML are byte-identical, return `200` with no-store/no-cache behavior and cache `MISS`, and reference byte-identical JavaScript, CSS, score worklet and the 5,812,361-byte JUNCTION bank. The live splash reports version `0.0.0`, build `20260829-1536` and commit `5685de3` with zero Browser warnings or errors; it was verified silently without activating playback. The 274 unit checks, nine Sites packaging checks, production build and zero-vulnerability lockfile audit pass. Low-volume listening and real-Tesla acceptance remain open.
- 2026-08-29 17:38 `[158eaf7]`: Recorded the exact remaining source and publication boundaries after the verified WAKE, FRACTURE, JUNCTION PARK, DIAG and ATLAS checkpoints. The tracked tree contains only the mixed `junction.svb` audio artifact and no source recordings, while the requested WAV library remains inside the protected ignored reference tree and was not inspected; the third 1980s score therefore requires an approved source path outside that boundary. The canonical live product remains version `0.0.0`, commit `5685de3`, build `20260829-1536`; no deployment was attempted because the only current publication script reads `.env` unconditionally even in no-write mode, which this task expressly forbids.
- 2026-08-29 17:41 `[3aa5c4d]`: Traceability correction for the preceding append-only entry: `158eaf7` is the last audited product/evidence checkpoint, while `3aa5c4d` is the documentation commit that actually recorded the protected-source and no-`.env`-read deployment boundaries. The earlier line is retained unchanged as historical evidence.
- 2026-08-29 17:53 `[dd6bb5e]`: The product owner explicitly superseded the earlier no-inspection boundary for `_references/`; the prior source-blocked entries remain unchanged as historical evidence. Added a reproducible offline audit of all 684 authorized 1980s WAVs with SHA-256, provenance, native BPM/root declarations, instrument and rhythm role, duration/bar fit, RMS/peak/crest, transient density, stereo correlation and cautious chord proposals. The admitted libraries are MusicRadar's 182 locally present 80s Pop Drums files and complete 502-file Eighties construction-kit pack, both royalty-free for use in music and prohibited from raw redistribution. Authorship now proceeds with the native drum ladder under one project-authored A-minor grammar; raw WAVs remain ignored and unpublished.
- 2026-08-29 18:04 `[f119184]`: Authored NIGHTSHIFT's production bank from the admitted 1980s sources: 18 complete eight-bar mixes, three takes at each native `85 / 95 / 110 / 120 / 130 / 140 BPM` state, one A-minor harmonic grammar and one primary drum recording at a time. The 5.5 MB segmented Opus bank contains no raw loop or stem, retains a six-clip decoded ceiling, measures `-21.4 LUFS` integrated / `6.6 LU` LRA / `-3.8 dBFS` true peak, and has enforceable harmony, pacing, repetition and source-exclusion tests.
- 2026-08-29 18:04 `[fdb63dc]`: Integrated NIGHTSHIFT as selectable Music `03` with a clockless six-voicing PARK form, native-rate complete-phrase scheduling, ascent/descent hysteresis, no fast drums before `82 km/h`, recent-take memory, observable boundary fallback and a three-renderer constant-power crossfade. The shared OPEN/UNDERWATER/BLOOM bus now receives all three scores; deployment identity admits only the complete mixed NIGHTSHIFT bank. Replaced the page title with `sedicivalvole — Adaptive Music for the Road`. Local objective evidence is not a listening or real-Tesla acceptance claim.
- 2026-08-29 18:09 `[2ac9ef1]`: Completed NIGHTSHIFT's muted runtime and documentation gate. Exact `773 × 601` Browser QA selected Music `03`, reported the native `110 BPM` MOTION state at `60 km/h`, retained `19.2 MB` decoded PCM, sustained approximately 60 FPS and produced zero warnings or errors. Corrected the first-lane descent hysteresis so GLIDE holds through `1.5 km/h`, then releases to PARK below it; the deterministic ascent/descent test now crosses and reverses every state boundary. The complete gate passes 302 unit tests, nine Sites packaging tests and the production build. Listening and real-Tesla acceptance remain open.
- 2026-08-29 18:15 `[2720a0a]` `build 20260829-1810`: Published the integrated WAKE, acceleration/effect, FRACTURE, JUNCTION PARK, DIAG, ATLAS and NIGHTSHIFT checkpoint to the canonical root after an authorized read-only preflight. The 34-file / 14,219,756-byte upload and immediate no-write identity gate passed. Bare and cache-busted HTML return `200`, explicit no-store/no-cache behavior and cache `MISS`; HTML, JavaScript, CSS, both audio worklets and both mixed production banks are byte-identical to the exact committed build. Muted live Browser QA at `773 × 601` selected NIGHTSHIFT, reported clockless PARK, 59.99 FPS / 17.8 ms p95, zero runtime issues and identity `v0.0.0 · 2720a0a · build 20260829-1810`. Low-volume listening and real-Tesla acceptance remain open.
- 2026-08-29 18:16 `[3bc2df6]`: Recorded the complete publication evidence and updated the authoritative current state after deploying source commit `2720a0a`, version `0.0.0`, build `20260829-1810`. This documentation hash is intentionally separate from the deployed source identity; the preceding append-only line remains the canonical deployment event.
- 2026-08-29 18:25 `[5b1696c]`: Added `DRIVEY 06` as an original bounded Canvas2D wireframe road environment after the pre-recorded source/licence gate. Speed independently owns road travel and perspective, musical level owns colour timing, and OPEN, UNDERWATER and BLOOM change native geometry/material behavior. A contextual upper-left `TUNE` surface exposes three persisted cameras and one clamped Structure control without entering the five existing visuals. Exact `773 × 601`, `390 × 844` and `601 × 390` Browser QA passes at 59.99 FPS / 17.9 ms p95 with zero runtime issue, warning or error; the complete 6 DRIVEY / 6 WAKE / 9 NIGHTSHIFT / 302 unit / 9 packaging gate and 128-module build pass. Real-Tesla motion comfort, touch and thermal acceptance remain open.
- 2026-08-29 18:28 `[ab7a00e]` `build 20260829-1826`: Published `DRIVEY 06` and its contextual TUNE control to the canonical root. The authorized read-only preflight, 34-file / 14,230,590-byte upload and immediate no-write identity gate passed. Bare and cache-busted HTML return `200`, explicit no-store/no-cache behavior and cache `MISS`; HTML, JavaScript, CSS and both worklets are byte-identical to the exact committed build. Muted live Browser QA at `773 × 601` selected DRIVEY and RED 03, opened the three-camera/Structure tuner, reported the Canvas2D road renderer at 55.56 FPS / 18.3 ms p95 with zero runtime issues, and exposed identity `v0.0.0 · ab7a00e · build 20260829-1826`. Real-Tesla acceptance remains open.
- 2026-08-29 18:29 `[09fa4ef]`: Recorded the complete DRIVEY publication evidence and updated the authoritative current state after deploying source commit `ab7a00e`, version `0.0.0`, build `20260829-1826`. This documentation hash is intentionally separate from the deployed source identity; the preceding append-only line remains the canonical deployment event.
