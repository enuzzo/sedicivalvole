// Every encoded adaptive-score performance enters its player at the same
// measured reference gain. Score-specific output buses may shape transitions
// and effects, but must not silently replace this shared sample calibration.
export const SAMPLED_SCORE_PERFORMANCE_LEVEL = 0.72;
